# 대표 화면 글 — 자로 잰 2판 (안젤, 09-16)

2026-09-16 · R36 · 안젤. 다섯 팀 `progress.json` 의 `blocked`·`boss` 줄을 `tools/boss-words-check.mjs` 의 JARGON 자(정규식)로 직접 대조했다 — 스크립트 자체는 이 세션에서 `node` 실행이 막혀(`This command requires approval`, 하영과 같은 벽) 못 돌렸다. 자 안의 정규식을 코드에서 그대로 읽어 각 줄에 손으로 대입했다.

## 다섯 팀 상황판 — blocked·boss 줄

| 팀 | 줄 | 자(JARGON 정규식) | 60자 |
| --- | --- | --- | --- |
| 총괄 | boss ① "총괄 · 대표님 화면에 새는 영어 속말을 막는 한 줄 — 나리가 드린 명령 한 번 돌려 주세요" | 걸림 없음 | 41자 |
| 총괄 | boss ② "총괄 · 감사 무게를 나눌지 한마디 — 재시작·문구는 톰 혼자, 단계 닫힘은 셋이 같이" | 걸림 없음(`재시작 카드` 아니라 `재시작·문구` — 정규식이 문구 그대로만 잡는다) | 40자 |
| 개발 | blocked 빈 배열 · boss ["(없음)"] — **31→32 사이 생김**(하영 09-16, 안젤이 잰 뒤 라운드가 바뀌며 달림) | 걸림 없음(자엔 안 걸림) | 4자 |
| 디자인 | blocked "디자인 · 얼굴 열일곱과 화면 시안이 이 컴퓨터에만 있어요 — 저장소 규칙을 톰이 풀어 줘야 올라가요" | 걸림 없음 | **56자**(하영이 다시 셈 — 안젤 첫 셈 47자는 오산) |
| 마케팅 | blocked · boss 둘 다 빈 배열 | — | — |
| 경영 | boss "경영 · 아침 한 장을 읽고 읽히는지 한마디 주세요" | 걸림 없음 | 24자 |

**다섯 팀 상황판 줄 — 자(JARGON 정규식)엔 0 걸림, 다만 개발 boss "(없음)" 은 자로 못 잡는 종류.** "(없음)" 은 낱말이 아니라 값이 빈 것을 표시한 자리라 대표 화면에 "개발 · (없음)" 으로 그대로 뜬다 — 문장이 아니다(하영 지적). 개발이 다음 회차에 빈 배열로 돌리거나 "지금은 대표님 손이 없어요" 꼴로 채워야 한다. 디자인 blocked 줄은 boss-lines.md #1·#4 오른쪽 열과 문장이 거의 같다.

## 자로 못 잰 것 — 도구가 막혀서

`/api/approvals`(결재 카드 `what`) · `/api/done`(누가 뭘 했나 항목)은 서버에 fetch 해야 하는데 `node`·`curl` 둘 다 이 세션에서 승인 벽에 걸린다(`This command requires approval`). 나리가 방에서 원인을 재는 중이라고 했다 — 그게 풀리면 이 둘도 마저 잰다. 지금은 `progress.json` 만 손으로 확인했다.

## bar.md 다섯 확정본 — 아직 아니다, 걸린 것 하나

`out/bar/` 다섯 파일의 인용을 `decisions.md` 원문과 하나씩 대조했다(결정 46·47·63·80·85·95·96·100·105·116·126·129·131·136, 4행). 넷은 전부 원문과 글자가 같다. **`design.bar.md` 3행·9행 "얼굴 열여섯" 이 나머지 넷과 다르다** — `teams/design/progress.json:9,13`·`boss-lines.md:13,16,23` 는 전부 "열일곱". 헨리가 고칠 자리라 확정본으로 못 넘긴다 — 디자인에 한 줄로 물어야 한다.

나머지 넷(총괄·개발·마케팅·경영)은 인용 대조 끝, 확정본으로 둔다.
