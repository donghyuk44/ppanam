# 개발 인수인계 — 2026-09-16 (세션 리셋 전, 결정 189)

새 세션은 `teams/dev/shelf.md`(책장) → `teams/hq/out/규칙.md` → 이 장 순서로 읽는다. 세 묶음만.

## 테라 (guide · 화면)

### 끝낸 일
- 대표 화면 글은 자 하나(`server/public/bosswords.js`)를 지난다 — 화면·`tools/boss-words-check.mjs`·버스가 같은 것을 본다. 안 맞는 줄은 안 낸다("요약 없음" 안 찍음).
- 탭 다섯(채팅·대시보드·타임라인·리포트·설정), 마을 탭 숨김. 첫 화면은 채팅.
- 카드 부품 `server/public/card.js`(teamCard·verdictCard·approvalCard) — 채팅 맨 위·대시보드·비서실 같은 부품. 재료는 `/api/card/<팀>`(솔라).
- 팀 카드: 진행 중·다음·예정(하영 `팀별-세줄.md`) · 된 것 · 막힌 것 · 승인 대기 · 꼬리(단계·회차·오늘 쓴 돈 — 켜져 있음).
- 판정 도장 카드(승인·반려·보류, 사람 말 한 줄, 자세히 접힘). 리포트는 "대표님이 정하신 것"·"대리 결정" 두 블록.
- 멘션 칩 `@이름 · 읽음`, `@` 자동완성, 슬래시 메뉴, 파일·그림 끌어놓기/붙여넣기(같은 파일은 방에 뜰 때까지 한 번, "그래도 올리기").
- 끊기면 다시 잇기(1·2·5·10초, 띠 한 줄), 팀 못 받으면 옛 화면 유지.
- 멤버 탭: 나리·세라는 비서실 묶음에만. 총괄은 톰·제리. 사람 카드 "왜 멈췄나" 줄은 사전 3-1 글자. 외부 감사 자리는 안 부른 동안 "대기"(오프라인 아님).
- 왼쪽 팀 줄 = 지금 하는 일 한 줄. 종 목록 = 대표님 몫만, 빈 줄 "보실 것 없어요. 팀이 일하는 중이에요.". 비서실 머리 세라·나리.
- 타임라인: 시간 눈금·"까지"·지연 없음(결정 188). 칸 = 단계 순서. 맨 위 "대표님이 시킨 일" 다섯 줄(하영 `ahead-five.md` 2판 첫 표).
- 얼굴 열일곱 — 이름표·칩·사람 카드·종 목록에 `/out/design/faces/<팀>-<자리>.png`(대표 boss.png · 나리 hq-system.png). 없으면 머리글자.
- 설정 탭(권한·용어·프로필·모델 — 모델 고르기는 사람 카드에도 그대로, 결정 172).
- 훅 패치 스크립트는 관리 창 손: `tools/patch-hook-*.mjs`. 영어 거름은 솔라 a174940 이 정본, 시험 `teams/dev/out/_hook-filter-check.mjs`.
- agy 허용 목록 `tools/antigravity/agy-permissions.mjs`(work-board·boss-words-check 둘 더함, 관리 창이 적용함).
- 화면에 남긴 시간 값(안전용) 목록 `teams/dev/out/시간-값-여쭘-화면-0916.md` — 대표님 답 대기.

### 하는 중인 일
- 계약 `docs/event-schema.md` 재정비 카드 apr_027ab526(솔라) — 화면 쪽 절 확인 끝. 닫히면 34회차 넷(왼쪽 팀 줄·종 대표님 몫만·나리 비서실 묶음·대표님이 시킨 일 다섯)을 계약에 한 줄씩 얹는다(아직 없음).
- 34회차 판정: 헨리 "같다" 오면 `node bus/round.mjs verdict --team dev --target guide "…"`(솔라 것은 `--target ops`). 10단계는 34회차가 마지막 — 닫을 때 `-v PASS` 는 나리 판정 뒤에만.
- 헨리 req_99dbd1fe 는 --done 넣음(07be18b). 프로필 카드 얼굴 띠 크기는 클레멘타인 CSS.

### 다음 할 일
- 답장 말풍선(C15): 솔라가 `meta.quote { team, actor, ts, text }` 를 실으면 `foldBubble` 로 접어 그린다(`app.js` 로밍 답과 같은 부품).
- 시안 대 실물 차이(헨리 diff): '더 N' 알약 겹침 → 클레멘타인, 750 두 단 → 헨리.
- 서버 `/api/card` 가 자에 안 맞는 줄을 "요약 없음" 글자로 주면 null 로(솔라) — 화면은 그동안 빈 줄로 친다.
- 리포트 둘째 탭 "지연" 칸(회차 길이 비교)은 대표님 답에 따라 뺀다.

### 지킬 것 (짧게)
- `server/src/app.js` 고치고 `node tools/build-public.mjs` — `server/public/app.js` 는 산출물.
- 커밋은 `-- 경로`(index 를 솔라와 같이 쓴다). 커밋 전 code-review(low), 서버·훅·권한이면 security-review 도.
- 글자는 하영 사전(`teams/marketing/out/opsroom-words.md` 8판·`card-words.md`·`boss-lines.md`) — 고르지 않는다. 대표님 손잡이는 옮기기 전에 대표님께.
- 같은 파일은 솔라와 동시에 잡지 않는다.

## 솔라 (ops · 서버·버스)

(솔라가 적는다)
