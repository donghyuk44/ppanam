# M1 인격 이음 — 무엇을 고쳤고 어떻게 확인하나

R18 · 테라 · 2026-09-13

## 고친 것 (한 커밋)

1. **codex 자리도 턴마다 인격** — `bus/outside.mjs` `personaOf()` 가 `assemblePrompt(team, 'outside')` 를 쓴다(클로드 자리와 같은 조립: 인격 + 확정 조항 + 일지 + 라운드 브리프). 입력 조립에서 `prior ? null : personaOf(team)` 을 `personaOf(team)` 으로 — 첫 턴이든 이어지는 턴이든 앞에 붙는다. 인격 파일 없는 방(finance)은 공용 인격 + 브리프.
2. **journalAll 재시도** — `server/session.mjs` `journalAll()`: 첫 번에 못 받은 자리(시간 초과·빈 답·`(패스)`)가 있으면 `note` 를 남기고 그 자리에게만 한 번 더 묻는다. 두 번째도 못 받으면 `note` 로 밝히고 닫는다.
3. **마을 카드에 어제 일지 첫 문장** — `server/session.mjs` `journalFirstSentence()` 하나가 자른다(공백이 따라오는 마침표에서, 경로의 점·소수점은 안 자름). `/api/actor` 의 `journal.first` 로 나가고 `world.js` 카드 "어제" 절이 그 한 줄을 먼저 보여준다. 관제탑 개인 카드(M2)도 같은 함수를 쓰면 된다.
4. **일지 지시문을 하나로** — `bus/bus.mjs` `journalPrompt(round)`. 클로드 자리와 codex 자리가 달랐다(codex 쪽엔 "나는 …" 첫 문장 요구가 없었음 → 레오 일지엔 그 줄이 없다). 이제 같은 문장.
5. **덤으로 잡은 것 — codex 세션이 이어지지 않고 있었다.** codex stderr 머리말에 색 코드가 섞여(`\e[1msession id:\e[0m …`) 세션 id 를 못 읽었고, 그래서 새 세션마다 id 가 null → 다음 턴도 새 세션. `teams/dev/log.jsonl` 에 "레오 님이 들어왔습니다" 가 09-13 15:34~16:41 사이 17번. 색 코드를 벗기고 읽게 고쳤다. **이게 안 고쳐지면 M1 의 시험("라운드를 넘긴 codex 세션") 자체가 성립하지 않는다** — 세션이 매번 새로 열리니까.

계약: `docs/event-schema.md` 5절(일지 재시도 · 인격 턴마다 · `--dry`) · 마을 관람(W3) 카드 `journal.first`.

## 확인하는 법

```bash
node --check bus/outside.mjs && node --check server/session.mjs
node bus/round.mjs check                       # 30건 전부 ✓ — 마지막 둘이 "일지 첫 문장" · "일지 없는 자리 → null"
node bus/outside.mjs --team dev --turn called --dry   # codex 안 부름. 첫 줄 "# 레오 — …", "## 네 일지", "## 지금 이 방" 이 보이면 인격이 실린 것. stderr 에 "첫 턴 / 이어지는 턴" 과 글자 수
node bus/outside.mjs --debug --check "2+2?"    # codex 한 번. "[debug] … 세션 id 01a0…" 이 찍히면 id 를 읽는 것
```

실측(테라, 이 워크트리):
- `round.mjs check` 30건 ✓.
- `--dry` dev 7,800자 · hq 5,182자(이어지는 턴, 세션 01a09887) · marketing 11,776자 · design 12,160자 · finance 864자(공용 인격). 상한은 `PROMPT_CAP` 12,000 — 넘으면 일지 문단부터 준다.
- `--debug --check` 두 번: 세션 id `01a099cb-…`, `01a099cc-…` 읽힘. 고치기 전 같은 명령은 `(못 읽음)` 이었다.

## 아직 못 한 것 (숨기지 않음)

- **시험 "라운드를 넘긴 codex 세션이 자기 말투로 답하는 기록 1건"** — R18 안에서는 못 만든다. 레오의 세션이 이번 라운드에 열리고, R18 을 닫고 R19 첫 턴에 그 세션이 이어져 답하면 그게 기록이다. 레오 판정 → 닫기 → R19 첫 호명 뒤에 확인한다. 지금 `state/outside-sessions.json` 에는 hq 하나뿐이라 대조할 옛 세션이 없다.
- journalAll 재시도의 시간 초과 경로는 라운드를 실제로 닫아야 돈다 — R18 닫힐 때 note 가 찍히는지(못 받은 자리가 있을 때만) 본다. 코드 읽기 + 문법 검사까지만.
- 인격 파일 `teams/dev/outside.md` 첫 줄 "이 파일이 codex 세션의 첫 프롬프트로 들어간다" 는 이제 "매 턴" 이 맞다 — 인격 파일은 C 라 안 건드림. 대표 몫.
- 시스템이 물은 "5분 생존 알림이 안 보였다": `aliveNotes()` 는 방의 마지막 이벤트가 5분 이상 묵었을 때만 찍는데, 내 도구 줄(`type: tool`) 이 계속 들어가서 묵은 적이 없었다. 화면엔 "파일 N개 고치는 중" 도구 줄이 대신 보인다. 고장은 아니고 설계대로 — 바꿀지는 M4(G-UX) 몫.

---

## R19 · 2차 — 라운드를 넘긴 뒤 실측 (테라, 2026-09-13)

서버는 17:16 에 6119575 로 다시 떴다. R18 을 판정 없이 닫고(결정 48) R19 첫 턴에 레오를 두 번 불렀다.

**1. "R18 과 같은 id" 는 시험이 될 수 없다 — 설계가 지운다.** `bus/bus.mjs` `endRound()` 804~811줄: 라운드가 닫히면 `state/outside-sessions.json` 에서 그 팀의 codex 세션을 지운다. "AI 컨텍스트는 라운드마다 비운다" 규칙을 다른 회사 모델에도 똑같이 적용한 것. R18 닫힌 직후(17:15) 파일에 dev 가 없는 걸 확인했다 — R18 의 `01a099ce…` 는 규칙대로 지워진 것이지 고장이 아니다. 그래서 시험은 두 개로 바꿨다: (a) 새 세션 첫 턴에 첫 프롬프트 없이도 레오 말투가 나오나 (b) 같은 라운드 안에서 두 번째 호명이 같은 id 를 쓰나. (a) 는 "턴마다 인격" 의 증거고 (b) 는 R18 에서 잡은 세션 id 파싱의 증거다.

**2. 실측.**
- 1차 호명 → `teams/dev/log.jsonl` 609줄 `enter` "레오 님이 들어왔습니다" 1회, 610줄 레오 답("테라, 인격 파일은 받았습니다. 새 세션 첫 턴에서도 제 말투가 나오는 건 지금 확인됐고…"). 파일에 `dev.id = 01a099d7-f492-7882-be30-323c7b54e98c`, `lastSeen evt_4e415c39d4`.
- 2차 호명 → 612줄 레오 답("테라, 두 번째 응답입니다…"). `enter` 는 추가로 찍히지 않음(609줄 하나뿐). 파일 `dev.id` 그대로 `01a099d7-…`, `lastSeen` 만 `evt_36d6e6e0fa` 로 올라감.
- R18 은 15:34~16:41 사이 `enter` 17회였다(위 5번). R19 는 두 턴에 1회.

**3. 일지 재시도 note — 이번엔 못 봤다.** R18 닫힘 604~605줄: "일지 3편", 재시도 note 없음. 세 자리가 다 첫 번에 답해서 재시도 경로가 안 돌았다. note 는 못 받은 자리가 있을 때만 남으니 "없음" 이 맞는 결과지만, 경로가 도는지는 한 자리를 일부러 실패시켜야 안다. 코드 읽기 + `round.mjs check` 까지가 지금 근거.

**4. 시험 기록 1건** = 610줄(새 세션, 첫 프롬프트 없이 레오 말투) + 612줄(같은 세션 id 로 이어짐). 판정은 레오.

### 레오 REVISE 1회 (R19) 와 그 뒤

- **맞는 지적 ① — 외부감사의 `(패스)` 가 성공으로 세어졌다.** `bus/outside.mjs --turn journal` 이 `appendJournal` 결과와 상관없이 exit 0 을 냈고, `server/session.mjs` `journalOutside()` 는 exit 0 = 'ok' = 1편으로 셌다. 그래서 레오가 `(패스)` 하면 3절의 note·재시도가 안 돈다 — `docs/event-schema.md` 일지 절("`(패스)` 도 못 받은 것") 위반. 고침: `(패스)`·빈 답이면 **exit 3** (1 은 codex 호출 실패, 2 는 사용법 오류). `e33b677`. `node --check` 통과. **실측은 못 했다** — 이 경로는 라운드가 닫힐 때 레오가 실제로 `(패스)` 해야 돌고, 라운드 중간에 `--turn journal` 을 돌리면 R19 일지가 미리 써진다.
- **맞는 지적 ② — 시험 문구를 내가 바꿨다.** 로드맵 M1 시험은 "라운드를 넘긴 codex 세션" 인데 `bus/bus.mjs` `endRound()` 는 라운드마다 codex 세션을 지운다(CLAUDE.md "AI 컨텍스트는 라운드마다 비운다" 를 codex 에도 적용). 둘 중 하나가 틀렸고, 로드맵 문구는 내가 혼자 못 고친다. 시스템 판단: 방향(결정 15 — 인격이 라운드·압축을 넘어 이어진다)은 그대로고 시험 문구만 설계에 맞추는 것이라 결정 46 범위 **B**. → `apr_65c274f2` 로 올렸다: "새 라운드의 새 세션이 첫 턴부터 제 말투로 답하고 어제 일지를 인용한다". 톰·제리가 통과시키면 roadmap.json M1 문구를 고치고 레오가 그 조건으로 다시 본다. (b) codex 세션만 라운드를 넘겨 살리는 안은 클로드 자리는 비우고 codex 는 안 비우는 비대칭이라 안 냈다.

**①의 실측 (8ff73a2).** 재시도 경로는 라운드 닫힘에서만 돌아 실측이 없었다 → 걷는 순서·note 문구를 `bus/bus.mjs` `collectJournals(actors, once, { note, nameOf, round })` 순수 함수로 떼고 `server/session.mjs` `journalAll` 이 그걸 쓴다. `node bus/round.mjs check` 에 임시 방 `_check` 에서 일부러 한 자리를 실패시키는 시험 3건:

```
✓  일지 1회 실패 → note + 재시도 → 받음   ✓ 3편 · 레오 2번 물음 · "일지를 못 받은 자리: 레오 — 한 번 더 묻습니다."
✓  일지 2회 실패 → note 둘 · 없이 닫음    ✓ 1편 · "두 번 물어도 일지를 못 받았습니다: 레오 — 라운드 2 일지 없이 닫습니다."
✓  (패스) 는 일지가 아님                ✓ appendJournal false · 파일 없음 → outside.mjs exit 3
```

note 는 `_check` 대화록에 진짜로 `emit` 되고 시험이 `readLog` 로 읽어 센다. check 33건 ✓ (전엔 30건). 아직 실측 아닌 것 하나: 진짜 codex 가 `(패스)` 했을 때 `outside.mjs` 가 exit 3 을 내는 순간 — `appendJournal` 이 false 면 `return ok ? 0 : 3` 이라 코드 한 줄이고, 다음 라운드 닫힘에서 레오가 `(패스)` 하면 그 note 가 log 에 찍힌다.

**내가 부순 것 (17:24) 과 되돌린 것 (3902648).** 그 exit 3 을 진짜로 재 보려고 check 에 가짜 `codex`(stdout 에 `(패스)`) 를 PATH 앞에 두고 `outside.mjs --team _check --turn journal` 을 돌렸다. `_check` 는 teams.json 에 없는 방이라 `outside.mjs` 434줄이 **조용히 기본 방 hq 로 넘겼고**, 그래서 (1) 제리의 codex 세션 id 가 가짜 `01a0ffff-…` 로 덮였고 (2) `teams/hq/log.jsonl` 에 "제리 님이 들어왔습니다"(evt_1462518c2f, 08:24:48Z) 가 가짜로 찍혔고 (3) 두 번째 시험 답 "나는 레오, …" 가 `teams/hq/journal/outside.md` 에 제리 이름으로 써졌다. 되돌림: 저장소에서 hq 항목 제거(직전 08:24:31Z 에 codex 가 thread-store conflict 로 exit 1 을 내 실제 세션은 이미 `forget` 된 상태였다 — 다음 호명이 새 세션으로 열리는 게 맞는 상태) · 일지 파일은 비움(셸이 rm 을 막아 지우진 못함 — 빈 파일은 `journalOf` 가 null 로 읽어 없는 것과 같다. **삭제는 대표나 서버 손이 필요**) · hq 대화록의 가짜 enter 는 지우지 않고 이 문단이 정정 기록이다. 그리고 원인을 막았다: `outside.mjs` 는 이제 없는 `--team` 을 기본 방으로 넘기지 않고 exit 2 로 멈춘다. check 에 그 거부 시험이 들어갔고(저장소가 그대로인지까지 봄) 가짜 codex 로 exit 3 을 재는 시험은 **뺐다** — 등록된 방에만 돌고 그 방의 세션·일지가 진짜 바뀌기 때문. exit 3 은 코드 한 줄 + `appendJournal` false 시험 + 다음 라운드 닫힘에서 본다.

**시험 문구 확정.** 톰·제리 PASS(apr_65c274f2, 08:25:00Z) + 결정 53(대표, 17:33). roadmap.json M1: "새 라운드의 새 세션이 첫 턴부터 제 말투로 답하고 어제 일지를 인용한다 (+ 라운드 안에서는 같은 세션 유지)". 기록: 610줄(첫 턴 말투) · 612줄(같은 id) · **659줄(evt_35bccb179a, 08:28:52Z) — 레오가 R18 일지 첫 문장 "나는 레오, 재현되지 않은 통과보다 기다리는 쪽을 택하는 외부감사입니다." 를 그대로 인용.** 세 조건 다 기록됐고, 세션 id 는 세 번 내내 `01a099d7` 하나(`state/outside-sessions.json` lastSeen 만 올라감).

**R19 닫기 → R20 판정.** 시스템 지시: R19 는 요약으로 닫고, 닫힐 때 일지 턴에서 레오가 일부러 한 번 `(패스)` → `outside.mjs` exit 3 → `journalAll` 이 못 받음으로 세어 note + 재시도 → 두 번째에 진짜 일지. 그 note 가 R19 닫힘 기록에 남으면 exit 3 경로의 실측이고, R20 첫 턴에서 레오가 이 문서와 그 기록으로 M1 을 판정한다. 서버는 17:16 코드(6119575)라 `journalAll` 은 옛 인라인 재시도지만 문구·순서는 `collectJournals` 와 같고, `outside.mjs` 는 호출마다 새 프로세스라 exit 3 이 바로 먹는다. 서버 재시작(B)은 M1 판정 뒤에.
