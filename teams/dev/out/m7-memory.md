# M7 기억 3층 마감 — 무엇을 만들었고 어떻게 확인하나

R26 · 테라 · 2026-09-15 밤

로드맵 M7 통과 조건 둘 — ① 세션이 죽었다 살아나도 어제 일을 인용하는 시험 1건 ② 자정 마감 한 장(팀마다 + 총괄실 자정 일지) + 결정 45 ③(대표용 — 문제 없으면 읽고 넘기고, 문제면 그때 개입). 톰이 착수 조건으로 건 M6 버그 둘(방 머리 살아 있음 표시 · 닫힌 방 채팅)도 여기 적는다.

**나리 결정(09-15 22:0x, 결정 136 위임 안)** — "7단계 기억 3층 마감 멈추세요, 대표님이 시키신 일이 아닙니다. 7단계는 헨리 시안대로 화면 다시 짜기." 아래는 멈추기 전까지 만든 것의 기록이다 — 커밋은 그대로 남고, 로드맵 문구는 B 카드로 바뀐다.

## 1. 되살아남 시험 — 기록 둘 (조건 ①)

세 층(인격 불변 · 라운드 컨텍스트 비움 · 일지 자람)은 `server/session.mjs assemblePrompt` 하나로 붙는다 — 인격 → 확정 조항 → 일지(최근 문단) → 브리프(상황판 포함). 새 프로세스가 뜰 때마다(`spawnFor`) 붙으니, 죽었다 살아난 세션이 어제를 아는 길은 이것뿐이다.

**기계 쪽 근거** — `node bus/round.mjs check` "기억 3층 조립(assemblePrompt)": 개발 실무의 조립 결과에 `## 네 일지` 와 일지 맨 위 문단의 첫 문장, `## 상황판` 절이 그 순서로 실제로 들어 있다(10,783자 · 일지 13문단). ce95d6e.

**실제 사례** — 09-15 21:44 서버 재시작(apr_b1304062)이 개발 방 세션 둘(테라·솔라)을 **턴 중간에** 죽였다.

| | 근거 |
| --- | --- |
| 죽음 | `teams/dev/log.jsonl` 2905행 evt_6c2d357ff3 "서버를 지금 켭니다 … 하던 턴은 서버가 한 번 다시 보냅니다. 21:44" · 2906행 evt_62301d0255 (안내) "서버가 다시 떴습니다 — 기다리던 차례 1건(테라)을 이어갑니다." 테라의 첫 턴은 Stop 훅 전에 죽어 그 턴의 말은 대화록에 없다(도구 줄만) — 결정 104 의 되살리기가 커서를 되돌려 같은 차례를 다시 줬다 |
| 살아남 | `state/sessions.json` dev:guide `b6af5941-…` · dev:ops `4ff6fb82-…` — 저장된 id 로 `--resume` |
| 인용 ① 테라 | 2926행 evt_932b25bc47 (21:53) — "제 R25 일지에 '상주 gemini 가 빈 답 이었던 게 아니라 답이 한 겹 밑에 들어 있었다 …' 라고 썼고, 창피한 건 진행 막대를 `.bar` 로 이름 붙여 방 머리를 통째로 눌러 버린 것" ↔ `teams/dev/journal/guide.md` 맨 위 문단(2026-09-15 · 라운드 25) 에 그 문장 그대로 |
| 인용 ② 솔라 | 2928행 evt_6f4a78fab9 (21:53) — "나는 솔라, 개발·운영이다. 오늘은 만든 밤보다 지키는 밤이었다." ↔ `teams/dev/journal/ops.md` 1~3행 첫 문장 글자 그대로. 파일 안 열고(그의 셸에서 열지 않았다고 본인이 적음) |

둘 다 **라운드 25 가 닫히며 세션이 비워졌고(reset — id 를 버린다) → 26 에서 새로 떴고 → 21:44 에 또 죽었다 살아난** 세션이 어제(R25) 일지를 글자 그대로 댄 것이다. 만든 사람이 판정하지 않는다 — 대조는 레오.

## 2. 자정 마감 한 장 (조건 ② · 결정 45 ③) — e2ec062

계약은 `docs/event-schema.md` 3절 "자정 마감 — 하루 한 장". 순수 함수는 `bus/nightly.mjs`(nightlyOf · nightlyHqOf · proxyLinesOf — check "자정 마감" 1건), 실행기는 `server/nightly.mjs runNightly`(서버 틱).

- 우리 시각 날짜가 바뀌면 **지난 하루**를 닫는다: 팀마다 `teams/<팀>/out/nightly/<날짜>.md`(라운드 · 판정 · 승인 · 막힌 것 · 다음, 맨 위 "문제 없음 / 문제 N건") · 톰의 자정 일지 한 문단 → `teams/hq/journal/chief.md`(총괄실은 라운드가 없어 한 번도 안 걷혔다) · 총괄실 대표용 한 장 `teams/hq/out/nightly/<날짜>.md`(문제 · 대표님 대신 정한 것 · 팀마다 한 줄 · 톰 일지).
- 문제 = 대표 손이 필요한 것만(FAIL 방 · 반박 상한 · C 대기 · 답 없는 대표 호출). 있으면 총괄실 note 뒤 톰에게 차례 — 대표께 **물어라**(종은 결정 52 그대로, 새 장치 없음). 없으면 note 한 줄 — 읽고 넘긴다.
- 한 날 한 번(파일이 진실) · 서버가 자정에 없었으면 뜬 뒤 늦게(late 표시) · 열린 라운드는 안 닫고 "아직 열림" · 실패하면 10분 뒤.
- `/api/report` 에 `nightly` — 보고서 탭 아래층 맨 위.
- 미리보기: `node bus/round.mjs nightly --day 2026-09-15` — 실데이터로 팀 넷 · 문제 1건(클레멘타인 09-14 13:18 유진 색 물음, 대표 답 없음 — 톰 아침 보고서 ⑥ 과 같은 것).

**실측 아직 없음** — 서버가 e2ec062 앞(46eac64)에서 돌고 있어 자정 마감은 다음 재시작 뒤 첫 틱에 09-14 를 늦게 닫는 것이 첫 실물이 된다. 나리가 M7 을 멈춘다 했으니 **이 코드를 켜 둘지 끌지는 나리 몫** — 켜 두면 재시작마다 그대로 돈다.

## 3. 톰 조건 둘 (M6 버그)

- **방 머리 살아 있음 표시** — 9ab8566(어젯밤): 진행 막대 `.bar` → `.pbar`. 나리 사진 `teams/dev/out/shots/r26-room-head-412.png`(21:58), 솔라가 둘째 줄 "테라 일하는 중 · 솔라 듣는 중 · 레오 자는 중" 눈으로 셈.
- **닫힌 방 채팅(127 ②)** — 46eac64: `conductor.crossPost` 가 idle 방이면 note 만 남기던 것을 /api/say 와 같은 길로(allowIdleChat → 사본 → 불린 사람에게 giveTurn 직접). `dispatch.mjs` 도 idle 을 안 막는다. 계약 4절. 21:44 재시작 뒤 산 코드 — 왕복 실측은 아직 0(그 뒤 총괄실이 닫힌 방을 부른 일이 없다).

## 4. 같은 라운드에 같이 한 것 (M7 밖)

관제탑 ② 사람 줄(톰·하영 req_0964bae9: ae7bfdd · 3bc9298) · '대표 차례' → '대표님이 보실 것'(3f66da4) · '더 보기' 오늘 안에서만(c5c3db8 — '어제까지' 는 내 R25 글자였지 정본이 아니었다) · 탭·방 이름·직책 열일곱 한 번에(4b90f9d, 결정 136 · apr_ee0c16a2).

## 5. 확인하는 법

```bash
node bus/round.mjs check                       # 89 ✓ — "기억 3층 조립" · "자정 마감" 포함
node bus/round.mjs nightly --day 2026-09-15    # 자정 마감 미리보기, 파일 안 씀
sed -n 2926p teams/dev/log.jsonl; sed -n 2928p teams/dev/log.jsonl   # 인용 둘
head -3 teams/dev/journal/guide.md; head -3 teams/dev/journal/ops.md  # 일지 첫 문장
```
