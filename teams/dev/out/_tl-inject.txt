const f=window.fetch;window.fetch=async(u,o)=>{const r=await f(u,o);if(String(u).includes('/api/timeline')){const j=await r.json();j.goal={slogan:'대표가 자리를 비워도 팀들이 스스로 달리고, 돌아온 대표는 첫 화면 한 번 훑어 무엇이 통과하고 무엇이 막혔는지 안다.',goalNow:'대표가 화면만 보고 다섯 팀이 무엇을 끝냈고, 그게 무슨 결과물이 됐고, 지금 무엇으로 이어지고 있는지 안다.',teams:j.teams.map(t=>({id:t.id,name:t.name,goal:'팀 목표 한 줄 본보기 — '+t.name,now:'지금 하는 일 본보기'}))};for(const t of j.teams){t.destination='팀 목표 한 줄 본보기 — '+t.name;for(const p of t.projects)for(const k of p.tasks)if(k.status==='대기'&&!k.ready)k.after=[{id:'x',what:'앞 작업 이름 본보기'}];}return new Response(JSON.stringify(j),{status:200,headers:{'content-type':'application/json'}});}return r;};location.hash='#dev/room';setTimeout(()=>{location.hash='#dev/dashboard'},120)
