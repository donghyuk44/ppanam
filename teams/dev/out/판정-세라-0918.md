# 세라 커밋 넷 판정 — 솔라, 2026-09-18

본 것: 12c4c8d(푸시 문) · 347f15e(중단 자리 안 띄움) · 57d4599(대리 카드 제리 칸) · 77682fe(낱말).
실측: `bus/bus.mjs` pushGateError·needsOf·hqOutsideSuspendedNow · `server/conductor.mjs` giveTurn 247~254행 ·
`teams/dev/out/_hq-suspend-check.mjs`(제리 실제 중단 상태로 needsOf 값 대조).

## 떨어뜨릴 이유 — 코드 자체엔 없음, 전파가 하나 빠졌었다(고침)

세 커밋 다 기존 결(needsOf 는 순수·셋째 인자로만 상태를 받는다, verdictSeats 재사용, suspended 필드는
이미 있던 것)을 그대로 따랐고 check 도 그대로다. 다만 `needsOf` 를 부르는 자리가 `listApprovals`(세라가
고친 곳) 말고도 셋 더 있었는데(`leftOf`·`server/notifier.mjs` 카드 안내 글·`bus/approve.mjs` 카드 올린 직후
CLI 출력) 거기는 기본값(false)에 머물러 있었다. 제리가 지금 진짜 중단 중이라 실측했더니 두 경로가 서로
다른 값을 냈다 — `listApprovals` 는 `['chief']`(맞음), notifier·approve.mjs 는 `['chief','outside']` (제리를
기다리라는 카드 글·CLI 안내가 나갔다). afe0dab 로 세 자리를 같은 값 보게 맞췄다 — 세라 것을 고친 게
아니라 그 옆에 안 따라온 자리를 채운 것.

## 하나씩

- 12c4c8d(푸시 문) — pushGateError 가 actor 배열을 받게 늘린 것과 verdictSeats 재사용 다 맞다. 기본값이
  여전히 'outside' 라 이 커밋 자체로 기존 동작은 안 바뀐다.
- 347f15e(중단 자리 안 띄움) — isOutside 안에서 이른 리턴, inflight 정리·note 다 giveTurn 의 기존 패턴 그대로.
- 57d4599(대리 카드 제리 칸) — needsOf 가 여전히 순수(셋째 인자로만 받음), hqOutsideSuspendedNow 로 실제
  cast 읽기를 한 곳에 모은 것도 맞다.
- 77682fe(낱말) — 주석·문구만, 코드 변화 없음.

## 남은 것 (급하지 않음)

- `bus/nightly.mjs`(아침 한 장·자정 마감)의 "차례" 글도 같은 값을 안 보는데, 그 파일은 "파일을 안 읽는다"
  가 제 규칙이라 leftOf 를 몰래 안 고치고 인자만 늘려 뒀다(afe0dab). 아침 한 장에 제리 중단 중인 총괄
  카드가 있으면 그때 인자를 물려주는 작은 커밋이 하나 더 필요하다 — 지금은 그런 카드가 없어서 안 급하다.

## 판정 말

통과. afe0dab 는 세라 걸 되돌린 게 아니라 같은 값을 다른 세 자리에도 물려준 것 — 세라 판단 그대로 두고
전파만 채웠다.
