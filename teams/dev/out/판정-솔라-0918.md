# 솔라 서버 묶음 판정 — 테라, 2026-09-18 (나리: 개발 판정 묶음은 솔라↔테라가 보고 나리 최종)

본 것: 3fcb74b(야/아 부름) · 7423a3f(이름 이어 깨우기·비서실 나리 자동 차례·시험 오염) · 5e531b7(upload silent) · e59ae01(/api/say 뿌리) · 8684e22·ef6146e·b30779d·53d3903(타임라인 자료·goal) · 12c4c8d·57d4599(푸시 문·대리 카드).
실측: `teams/dev/out/_addressee-check.mjs` 열셋 · `server/index.mjs` 585~641행 · `server/session.mjs` 600~612행 · `server/conductor.mjs` 946~951행 · `teams/sera/log.jsonl` 05:29 무렵(세라·나리 각 3답).

## 떨어뜨릴 이유 셋

① **e59ae01 — 적고 나서 거절한다.** `index.mjs` 636행 `emit(대표 말)` → 637행 `session.send` → 638행 `refused` 면 409. `session.mjs` 601행: 회차가 닫히는 중이면 refused. 그 순간 대표 말은 대화록에 이미 있고(말풍선 뜸) 세션은 못 받았고 화면엔 '못 보냈어요' 도 뜬다 — 대표가 다시 보내면 같은 말 둘. 전엔(훅이 적을 때) 거절되면 안 적혔다.
   고침 한 줄: 636·637 순서를 바꿔 `send` 가 refused 가 아닐 때만 `emit` — quiet 라 훅은 어느 순서든 안 적으니 나머지는 그대로. 실측은 못 했다(닫히는 중 상태를 손으로 못 만듦) — 줄 번호 읽기다.

② **7423a3f — 쉼표 뒤 둘째 이름은 구두점 없이는 안 깬다.** 실측 `_addressee-check.mjs`: "세라, 나리 둘 다 요약해" → `["secretary"]` (나리 빠짐). "세라, 나리, 둘 다"·"세라야 나리야, …"·"나리야 세라야 둘 다" 는 둘 다 됨. 규칙(`bus.mjs` 291~299행)이 첫 이름·둘째 이름에 같은 잣대를 쓰는데, 오인식("나리이야기") 걱정은 첫 이름에만 있다 — 이미 한 사람이 불린 뒤의 이름은 빈칸으로 끝나도 부름으로 받아도 된다. 덤: 야/아 없이 "나리!"·"나리?" 는 안 부름(m2 구두점에 `!?` 없음) — 대표 글 꼴로 있을 법하다.

③ **e59ae01 — 주석이 코드와 반대다.** `index.mjs` 585~588행 "여기서 말풍선을 만들지 않는다 — 훅이 남긴다. 여기서 또 남기면 두 번 뜬다" 가 636행 바로 위에서 그대로 산다. 다음 사람이 이 주석을 믿고 636행을 걷으면 대표 말이 늦게 뜨는 게 아니라 안 뜬다. 같은 계열: `/api/upload` 비-silent 길(699행)은 아직 옛 방식(`q ? quiet : say`, 훅이 적음) — 화면은 이제 silent 만 쓰니 안 밟히지만 두 길이 다른 건 적어 둬야 한다(`session.mjs` 598행 "말풍선은 훅이 남긴다" 도 이제 반만 참).

## 떨어뜨리지 않는 것 (확인만)

- 비서실에서 대표가 말하면 세라·나리 둘 다 답한다(05:29 실측 3+3) — 7423a3f ②, 대표 09-18 11:19 원문대로 의도한 것. 규칙 12행(대표께 답은 나리 하나, 173)과 나란히 읽으면 "비서실은 예외" 로 규칙.md 에 한 줄 있어야 다음 사람이 사고로 안 본다 — 나리 판단.
- 5e531b7 silent: 파일만 저장, 응답 `silent:true`, 화면 d14ce8b·e142110 이 그걸 받아 한 말풍선 — 오늘 실물 r37-imgline-412-a.png.
- e59ae01 이 막힘 상태(`phase === 'blocked'`)에서 `resumeRound` 와 겹치는지: resumeRound 는 note 만 남기고 대표 말은 안 적는다 — 두 번 안 뜸.
- b30779d goal 60자 면제 · ef6146e goal.teams 여섯 · 53d3903 마지막 칸 상태: 타임라인 새 판이 이 자료로 찍힌 실물이 오늘 r37-timeline-1280-n.png 까지.

## 판정 말

①은 코드 순서 한 줄이라 솔라가 바꾸고 check 돌리면 닫힌다. ②는 규칙 한 갈래(둘째 이름부터 빈칸 허용 + `!?`) — 솔라 몫. ③은 주석 셋 줄 고치기. 셋 다 오늘 안. 통과 여부는 나리가 낸다 — 저는 떨어뜨릴 이유만 적었다.
