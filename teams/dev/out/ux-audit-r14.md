# 작전실 사용성 검수 — 2026-09-13 (독립검수, 폰 412px)

## ① 요약

대표가 폰에서 지금 못 하는 것은 셋이다. **첫째, 누가 일하는지 반대로 안다.** 방에 들어가거나 팀을 바꾼 직후에는 서버가 "일하는 중"이라고 해도 화면은 헤더에 아무 표시 없이 참여 카드에 '자는 중'으로 그린다(#1, 어제 "작업중인데 꺼버린" 오판의 뿌리로 보인다). **둘째, 승인 카드를 읽지 못한 채 승인/반려를 누른다.** 로드맵 교체 카드의 상세가 한 줄로 잘려 '옛 M4~M6 픽셀 타일은 컷' 같은 결정적 문장이 '…' 뒤에 숨고, 푸시 요청의 브랜치·SHA는 아예 안 그려진다 — 실제로 apr_ef199c98 은 사람이 읽어 주고 나서야 승인됐다(#2). 같은 카드가 폰 폭에서 격자가 깨져 어느 팀 누가 올렸는지도 안 보인다(#8). **셋째, 관제탑이 "어디를 봐야 하나"를 안 알려준다.** 나를 부른 팀의 카드가 '진행 중/대기'로만 보이고(#9), 카드 마지막 줄이 파일 경로나 CLI 명령이며(#10), 승인이 서너 건 쌓이면 방 탭 대화가 화면에서 사라진다(#13). 아래 22건 중 high 2 · mid 11 · low 9. 줄 번호는 검수 워크트리 HEAD 7f6af87 기준이고, 발견 원문과 어긋난 곳은 검증 때 확인한 줄로 적었다.

## ② 발견 표

| # | 탭 | 증상(대표 말) | 원인(파일:줄) | 고치는 방향 | 심각도 | 결정 |
|---|---|---|---|---|---|---|
| 1 | room | 방에 들어가거나 팀을 바꾼 직후 헤더에 '일하는 중'·'차례: ○○'가 없고 참여 카드에 일하는 사람(테라·하영)이 '자는 중'으로 떠서 누가 일하는지 반대로 안다. 조용한 방일수록 오래 그대로다. | server/index.mjs:180 `/api/team` 이 `summary: teamSummary(team)` 만 주고 session·conductor 를 안 싣는다(bus/bus.mjs:811 은 state+log 파생값만). app.js:511 `summary = r.summary` 가 부팅의 풍부한 summaries[id](app.js:1166) 를 얇은 것으로 덮고, 소켓 hello 는 world 만 쓴다(app.js:604). 다음 'summaries' 방송은 어느 팀이든 요약이 바뀔 때만(index.mjs:443~446). | selectTeam 에서 `summary = summaries[id] ?? r.summary` 로 부팅/방송 요약을 우선하거나 /api/team 이 summaries()[team] 을 돌려주게. hello 의 summaries 도 반영. | high | 관련 28 ① (원인은 새 사실) |
| 2 | tower | 로드맵 교체 카드에서 '옛 M4~M6 픽셀 타일은 컷' 이 '…' 뒤에 숨어 무엇이 잘리는지 모른 채 승인/반려를 눌러야 한다. 푸시 요청의 브랜치·SHA·파일은 카드에 없다. apr_ef199c98 은 PASS 사유가 "독립검수가 내용을 읽어 드리고 받은 답"이었다. | style.css:499 `.apr__what small { overflow:hidden; text-overflow:ellipsis; white-space:nowrap }`. app.js:815-816 은 r.what·r.detail 만 그리고 r.action(type/file/branch/sha/n)은 안 읽는다(notifier.mjs:60-64 는 같은 값을 문장으로 푼다). 오른쪽 팀 라벨도 style.css:497 nowrap 으로 잘린다. | detail 은 줄바꿈 허용. action 을 종류별 한 줄로 — 로드맵: 마일스톤 표·컷 목록 펼침, 푸시: 브랜치 @ SHA · 파일 수. 결정 20-2 의 '요청자 원문'은 request 에 meta 로 실어야 그릴 수 있다. | high | 20-2 (CSS 한 줄 잘림은 새 사실) |
| 3 | room | 빈 방(경영재무)에서 '라운드 열기'를 누르면 주제 폼이 화면 한가운데(y≈422) 떠 있고 유일한 대화는 입력창 바로 위에 깔려 어디에 무엇을 적을지 헤맨다. 이벤트가 많은 방에서는 우연히 정상. | style.css:128 `.room { grid-template-rows: auto auto 1fr auto }` 4행인데 자식은 bar·roundbar·#roundOpen·feed·composerMsg·composer 여섯(index.html:39~87) — 폼이 보이면 폼이 1fr 행을 먹고 feed 는 암시적 auto 행으로 밀린다. style.css:204 `.roundopen { align-items:center }` 가 537px 행 안에서 세로 중앙정렬. | `.room { grid-template-rows: auto auto auto minmax(0,1fr) auto auto }` 로 자식 수에 맞추거나 feed 에 grid-row 를 못박는다. 또는 폼을 roundbar 안에. | mid | — |
| 4 | room | 폰 위 띠에 총괄·마케팅·개발(잘림)만 보이고 디자인·경영재무는 화면 밖(x 505~594)이며 스크롤 힌트가 없어, 그 방의 안 읽음 수를 못 보고 그런 방이 있다는 것도 모른다. (지금 어느 방인지는 헤더 roomName, 대표 차례는 bossBadge 가 알려준다.) | style.css:651-655 `.rail__list { overflow-x:auto; scrollbar-width:none }` + `::-webkit-scrollbar{display:none}`. app.js:108-144 renderRail 이 aria-current 팀을 scrollIntoView 하지 않고, 안 읽음(app.js:135-136)은 버튼 안에만 그린다. | renderRail 끝에서 활성 팀 `scrollIntoView({inline:'nearest'})`; 띠 오른쪽 끝 페이드/화살표; 안 읽음 합계를 헤더에도 한 번 더. | mid | — |
| 5 | room | 폰에서는 누가 듣는 중·말하는 중인지, 화면이 서버와 끊겼는지 볼 곳이 없다(컴퓨터도 마우스를 올려야 글자가 뜬다). 라운드바의 '실무이 일하는 중'·'차례: …'만 남는다. | style.css:660 `@media (max-width:819px) { .crew { display:none } }` 가 상태 칩(app.js:226~235, 상태명은 title 속성뿐)과 그 안의 연결 점 #liveDot(app.js:236~240, ws onopen/onclose app.js:578·584)을 함께 숨긴다. | 폰에서는 칩 대신 헤더 둘째 줄을 '테라 말하는 중 · 레오 듣는 중' 글자로, 연결 점은 헤더에 따로(결정 28 ① 한 줄과 합칠 수 있다). | mid | 28 ① |
| 6 | room | 마일스톤이 PASS 로 닫힌 뒤(지금 마케팅이 그 상태) M 칸을 비우고 라운드 열기를 누르면 'node bus/approve.mjs --request B --next …' 터미널 문장이 입력창 위에 6초 떴다 사라져 왜 안 열리는지 모른다. 'M' 이 마일스톤 번호인 줄도 알 수 없고, 주제를 비우면 지난 주제를 물려받는다. | bus/bus.mjs:603~611 이 CLI 문장을 throw → server/index.mjs:368~369 가 400 {error} 그대로 → app.js:677 `say(r.data.error)` → app.js:634 기본 6000ms 뒤 hidden. index.html:65~66 M 칸은 placeholder 'M'+title 툴팁뿐(폭 52px, style.css:213). index.mjs:342 → bus.mjs:619 `topic ?? prev.topic`. | 화면 요청이면 대표 말 안내('마일스톤 1 이 끝났습니다. 2 를 열까요?' + 버튼)를 사라지지 않게. M 칸은 로드맵 다음 마일스톤 제목 셀렉트. 빈 주제는 마일스톤 제목. | mid | 25 (실무 이어 열기; 화면 안내·M 칸은 새 사실) |
| 7 | room | R14·마일스톤 1·반박 0/3·'실무이 일하는 중'·1:1·M·PASS/REVISE·컨텍스트·DESTINATION·CUT LIST·/kickoff 가 먼저 보여 뜻을 매번 짐작하고, '실무'가 누군지 모른다(조사도 틀렸다). 사회자 판정 note 에는 CLI 문장이 그대로 게시된다. | app.js:190(R14·1:1)·197(마일스톤)·200(반박)·207(`실무이 일하는 중` 고정 문자열)·279(DESTINATION)·301(CUT LIST)·297·1134(/kickoff)·45(자는 중/CLAUDE/GPT)·474·658(컨텍스트), index.html:66(M), server/conductor.mjs:259(`node bus/round.mjs end -v PASS …`). | 결정 19-3 용어 사전에 낱말별 대안 — R14→'14번째 회의', 마일스톤 1→로드맵 제목, 반박→'되돌림', 실무→그 자리 이름, 1:1→'톰과 직접', PASS/REVISE/FAIL→'통과/다시/중단', DESTINATION→'목적지', 컨텍스트→'기억'. conductor note 의 CLI 문장 제거. | mid | 19 ③ (낱말 목록·조사 오류·CLI 게시는 새 사실) |
| 8 | tower | C 승인 카드에서 등급 칩이 가로로 긴 띠가 되고 '디자인 · 헨리'가 화면 오른쪽 밖으로 밀려(문서 폭 446>412) 어느 팀 누가 올렸는지 못 보고 페이지가 옆으로 흔들린다. 오늘 C 요청 4건 — 다음 요청 때 다시 겪는다. | style.css:638 `.apr { grid-template-columns: auto minmax(0,1fr) }` + :639 `.apr__team { grid-column: 2 }` 인데 .apr__what(app.js:815)에 열 지정이 없어 1열(auto)에 자동 배치 → 1열 356px, 2열 0. HEAD 에 이미 커밋된 상태(미수정). | `.apr__what { grid-column: 1 / -1 }`, 1열 max-content, `.apr__g { justify-self:start }`. 412 에서 docW==412 확인. | mid | — |
| 9 | tower | 배지 '대표 차례 N'을 눌러 관제탑에 가도 나를 부른 팀(헨리·하영)의 카드가 '진행 중/대기'라 누가 기다리는지 못 찾는다. 배지의 이유는 툴팁뿐이라 폰에서 안 보인다. | bossCall 을 읽는 곳은 배지뿐 — app.js:153 카운트, :163 b.title. 카드 :868 dataset.alert · :877-878 깃발, 레일 점 :120 · 배지 :132, 상황판 :256 은 needsBoss 만. 클릭 :165-168 은 C 카드가 하나라도 있으면 관제탑으로만. (4321 서버는 옛 bus 라 재시작 뒤 드러난다.) | 카드 깃발에 '헨리가 불렀습니다 · N분 전'을 needsBoss 와 같은 자리·색으로, 레일 점도 alert. 배지 탭은 항목 목록(승인 N · 방 M) 펼쳐 고르게. | mid | 28 ③ (카드·레일이 bossCall 을 안 읽는 건 새 사실) |
| 10 | tower | 관제탑 카드 마지막 줄이 '/Users/…/server/world.mjs' 나 'node bus/round.mjs end -v PASS …' 라 그 팀이 뭘 하는지 모르고, 총괄 카드는 '**apr_edd5fcb9 는 …**' 별표·백틱이 그대로다. | bus/bus.mjs:814 `const last = log[log.length-1]` — type 필터 없이 tool/note 도 :845 lastText 가 된다(04:50:01Z /api/boot 실측 marketing.lastText = Read 도구 경로). app.js:941 이 bubble() 없이 원문을 그린다. | lastText 는 마지막 message/verdict 로, 도구 줄은 '테라가 app.js 를 고치는 중' 한 줄로 따로. 카드 본문은 bubble() 로 굵게·기호 정리. | mid | 28 ①/30 (방 탭 기준; 관제탑은 새 사실) |
| 11 | tower | 개발 카드에 '▸ 상황 · 하는 중 2 · 한 것 3 · 남은 것 6 · 이슈 2' 숫자만 접혀 보여 이슈가 내 몫(deny 규칙 잠그기)인지 모르고 배지는 '대표 차례 0'. 다른 네 팀 카드엔 상황 블록 자체가 없다. | app.js:912 `el('details','tcard__prog')` 에 open 없음; :915-916 summary 는 건수만; :910 `if (s.progress)` 로 파일 없는 팀 생략; :151-155 bossTurns 가 progress.issues 를 안 본다. 파일은 teams/dev/progress.json 하나뿐. | issues·doing 첫 항목은 펼친 채 카드 위쪽에, '대표'로 시작하는 이슈는 대표 차례로 집계. 파일 없는 팀은 '상황 없음 — 실무가 턴 끝에 씁니다' 한 줄. | mid | 23 |
| 12 | tower | 폰 첫 화면에 머리글·승인 블록 다음 총괄실(늘 '1:1', 할 일 없음)·마케팅 카드만 보이고, 막힌 팀이 디자인·경영재무면 1~2번 스크롤해야 나온다(승인 블록이 생기면 더 내려간다). | app.js:860 `for (const t of teams)` 정렬 없이 boot.teams 순(hq→marketing→dev→design→finance). needsBoss 는 색만(style.css:518). 머리글 h1+설명문 style.css:468-470 이 폰(:649)에서도 그대로. 별건: 관제탑 카드도 412px 오른쪽으로 잘려 넘친다. | needsBoss·bossCall → 승인 대기 → 진행 중 → 대기 순 정렬, 총괄실 맨 아래. 폰 머리글 설명문 제거. | mid | 13 · 28 ③ |
| 13 | tower | 승인이 3건 쌓이면 블록 419px, 5건이면 656px 이 돼 방 탭 대화 피드가 408→172→22px 로 사실상 사라진다(오늘 04:02~04:09 3건 동시 대기 실제 있었음). | style.css:78-84 `.app` grid 첫 줄 auto, 폰 구간 :630-633 도 `auto auto minmax(0,1fr)`; :480-483 `.approvals` 에 max-height/overflow-y 없음. | `.approvals { max-height: 40vh; overflow-y: auto }` 또는 1건만 펼치고 '외 N건'으로 접기. | mid | 20 (후속 결함) |
| 14 | room | 마케팅 주제가 '…규칙 한 …'에서 잘리고, FAIL 안내는 '…여기에 판단을 적으면 라운드 …'에서 끊긴다(rtitle 240~277px). 피드의 시스템 노트와 입력창 placeholder 로 할 일은 알 수 있어 거슬리는 수준. | style.css:180 `.rtitle { white-space:nowrap; text-overflow:ellipsis }`, 옆 rwork·rprog·rbtn 이 모두 flex:none. 문장은 app.js:192~198. | blocked 일 때 rtitle 두 줄 허용 또는 안내문을 roundbar 아래 별도 줄로. 주제는 '마일스톤 1' 뒤를 접고 탭하면 전체. | low | — |
| 15 | room | 탭 '작전실'을 누르면 헤더는 '총괄실'/'개발 작전실', 띠 칩은 '개발', 브라우저 제목은 'R14 · 개발' — 같은 것이 화면마다 다른 이름이라 어디 있는지 헷갈린다. | index.html:28 탭 라벨 '작전실'; app.js:177 헤더 t.room; :124 칩 t.name; :187 document.title; 관제탑 카드 :872 t.room. (rail__sub 는 폰에서 display:none, style.css:642.) | 탭은 '대화·전체·기록·마을' 처럼 방 이름과 안 겹치게, 헤더·칩·제목은 t.name 하나로, '작전실'은 앱 이름에만. | low | 19-3 (progress.json:18 R15 항목) |
| 16 | room | 지난 대화를 올려 읽는 동안 새 발언이 와도 표시가 없어 답을 늦게 본다(대표 호명은 bossBadge 가 어느 탭에서든 켜지므로 놓치지는 않는다). | app.js:479 atBottom(<90px), :493~501 append 는 바닥일 때만 스크롤을 붙이고 아니면 아무 표시 없음. 안 읽음(:618)은 다른 팀에만 쌓인다. | 바닥이 아닐 때 새 이벤트(도구 제외)가 오면 composer 위 '새 말 N · 내려가기' 버튼. | low | — |
| 17 | room | 내가 승인한 '승인 통과 [C] 로드맵 교체 …'가 '다니엘 님이 들어왔습니다'와 같은 11.5px 회색 중앙 줄이라 내가 뭘 결정했는지 지나친다. 판정 뒤 카드가 사라지면 방 안 흔적은 이 줄뿐. | bus/bus.mjs:296(요청)·329(결과, actor system) 이 type 'note' → app.js:355 `el('div','note')` → style.css:247 `.pill, .note { font-size:11.5px; color:var(--ink-4) }`. | meta.approval 이 있는 note 는 카드형(등급 색·제목·판정자)으로, 대표 판정은 boss 말풍선으로도. | low | 20 (판정 뒤 기록 표시는 새 사실) |
| 18 | tower | '승인 대기 2건'인데 배지는 '대표 차례 1'이라 나머지 하나가 내 것인지 헤맨다(B 줄에 '제리 대기'가 있어 내 것 아닌 건 알 수 있다). | app.js:804 제목이 approvals.length(B 포함); :153-156 bossTurns 는 grade==='C' 만; :839-847 B 줄은 '톰 PASS · 제리 대기 · 총괄실에 알림 ✓' 뿐. | 제목을 '대표 판정 N · 총괄 진행 M'으로 나누거나 B 는 접은 부제로. B 줄 앞에 '톰·제리가 처리 중 — 대표는 볼 것 없음'. | low | — |
| 19 | tower | 톰이 PASS 를 찍어도 폰엔 '톰·제리 대기'로 남다가 제리까지 끝나면 카드가 통째로 사라져 중간 진행을 못 본다(실측 어긋난 창 33~58초; 제리가 오래 걸리면 mid). | app.js:598-601 pending 합이 바뀔 때만 /api/approvals 재요청. bus/bus.mjs:267-271 B 는 둘 다 PASS 해야 status 변경, :326-332 중간 판정은 note 도 없음. | 판정 수나 approvals.jsonl 수정시각을 summaries 에 실어 바뀌면 재요청, 또는 pending 목록 자체(id·decisions·told)를 summaries 에. | low | — |
| 20 | tower | 관제탑에서 승인을 누르면 카드가 바로 사라져 관제탑엔 아무 확인이 없고, 라운드 닫힌 팀이면 '라운드 열리면 전달' 표시가 없다(방 탭엔 note 가 바로 남고, 전달은 다음 라운드에 된다 — '영영'은 아님). | app.js:825 `approvals = approvals.filter(...)` 로 제거만; :845 는 told[r.id].requested 만 그림; notifier.mjs:147 idle 이면 continue(행동은 :139~141 에서 실행). | '오늘 판정' 접이식 + told.decided/applied 를 '전달 ✓ / 라운드 열리면 전달'로 표시. | low | — |
| 21 | tower | '반려'를 누르면 '반려 이유' 칸만 열리고 다음에 뭘 눌러야 할지 몰라 멈춘다(반려 재탭·키보드 Enter 로는 간다). 버튼이 손가락 목표로 작다. | app.js:821-836 reasonBox 열고 focus 만(:824), 전송은 :834 Enter 또는 재탭; enterkeyhint 는 index.html:81 composer 에만. style.css:501-504 버튼 padding 3px 9px·11.5px. | 이유 칸 옆 '반려 보내기' 버튼, enterkeyhint='send', 버튼 최소 높이 40px. | low | — |
| 22 | harness | 테라가 고치는 몇 분 동안 새로고침하면 반쯤 고친 화면(줄바꿈 칸은 있는데 Enter 가 안 보내짐, 13:31~13:39)이 떠 고장인지 작업 중인지 모른다. | server/index.mjs:33 PUBLIC_DIR 이 실행 워크트리의 public, :376~381 요청마다 디스크 읽음·캐시 헤더 없음. 서버 pid 88666 cwd = ppanam-review-issues-4058bc — 실무가 편집하는 워크트리와 동일. | 서버는 커밋 스냅샷(또는 별도 체크아웃)에서 띄우고 실무는 커밋·재시작으로 넘긴다. 최소한 편집 중 '고치는 중' 띠. | low | 관련 28 ① |

## ③ R14 에 넣을 것 / R15 로 미룰 것 (반나절 단위 제안 — 결정은 대표 몫)

**R14 (G-UX 진행 중, 대표가 일을 못 하는 것부터)**

- 반나절 A "지금 상태를 바로 알기": #1(selectTeam 이 부팅 summaries 를 우선 — JS 몇 줄) · #5(폰 헤더 둘째 줄 상태 글자 + 연결 점 — 결정 28 ① 과 한 번에) · #10(bus.mjs:814 lastText 에 type 필터 + app.js:941 bubble()).
- 반나절 B "승인 카드를 읽고 누를 수 있게": #2(detail nowrap 제거 + action 종류별 한 줄) · #8(`.apr__what { grid-column:1/-1 }` 한 줄) · #13(`.approvals` max-height 한 줄) · #3(`.room` grid rows 한 줄) · #9(카드 깃발·레일 점이 bossCall 도 읽게 — 결정 28 ③ 과 함께).

**R15 로 미룰 것**

- 반나절 C "말 바꾸기"(결정 19-3 용어 사전과 한 묶음): #7 낱말별 대안·조사 오류·conductor CLI 문장 제거 · #15 탭/헤더/칩 이름 통일 · #6 라운드 열기 거부 안내문·M 셀렉트·빈 주제.
- 반나절 D "관제탑 정렬·상황"(결정 13·23 과 한 묶음): #12 카드 정렬 · #11 상황 블록 펼침·'대표' 이슈 집계 · #18 B 건수 분리 · #19 중간 판정 재요청 · #20 판정 뒤 전달 표시.
- 반나절 E "방 탭 잔손": #4 띠 스크롤·안 읽음 합계 · #14 rtitle 두 줄 · #16 새 말 버튼 · #17 승인 note 카드형 · #21 반려 보내기 버튼.
- 따로(인프라, 대표 결정 필요): #22 서버를 커밋 스냅샷에서 서빙 — 워크트리 운용 방식이 바뀌므로 R 안에 넣지 않고 대표 판단.

## ④ 기각된 발견

- **도구 줄 말줄임이 끝을 잘라 파일 이름이 절대 안 보인다** — HEAD 에서 이미 고쳐짐(app.js:460 이 tool 을 drawTool:428~446 로 보내 baseName 만 그림, 412px DOM 에 class="tool" 0개); 남는 건 죽은 코드(app.js:357~363·style.css:292) 정리뿐, 결정 30 으로 처리됨.
- **결정 27 의 '가로 넘침'은 촬영 방법이 만든 허상이다** — 전제(헤드리스 `--window-size=412` 가 innerWidth 500 으로 잡힘)는 맞으나 증상 "테라가 헛일을 한다"는 이미 안 일어남(teams/dev/out/g-ux-r14.md:9,21-29·progress.json:13 에 CSS 결함 아님으로 정리); 남는 건 decisions.md:105 결정 27 본문의 '재현 안 됨' 정정뿐이라 대표가 겪는 증상이 아님.

## ⑤ 미검증 (렌즈가 올렸으나 확인 안 됨 — 39건)

- **side(상황판)**: 방을 열면 팀원이 전부 '자는 중'(#1 과 같은 뿌리로 추정) · 로드맵·일지 카드는 팀 바꾸거나 새로고침해야 바뀜 · 펼친 일지가 몇 초 안에 저절로 닫힘 · 상황판에 '지금 하는 것·막힌 것·대표 차례·다음'이 없음 · '이번 라운드' 대화록 건수가 팀 전체 누적이고 열린 시간 없음 · 톰은 어느 방에서나 '자는 중', 실무·외부감사 상태 글자 잘림 · 폰 서랍에서 로드맵 카드가 한 화면 이상 · 총괄실 상황판이 /kickoff 를 권함.
- **analysis**: 산출물이 하위 폴더를 안 세어 진짜 산출물이 안 보임 · '통과/되돌림/중단'과 판정 열이 감사 결과가 아니라 닫을 때 적은 값 · '끝난 라운드 9' 인데 방은 R23.
- **world(마을)**: 다른 장면의 라운드 띠는 안 뜨고 화면만 바뀜 · 시험용 시각 선택이 '실제 시각'으로 노출돼 서버 시계를 바꿈 · 누가 말하든 카메라가 끌고 감 · 폰에선 댄을 못 움직이고 말 거는 법이 없음 · 주말·저녁엔 일하는 사람이 일하는 것으로 안 보임 · 배율 바꾸면 보던 자리 잃음 · 라운드 띠가 폰 폭보다 넓음 · 툴바 12개 조작이 4줄 · 재생 팀이 늘 마케팅 · 휴일에 '들어왔습니다'가 빈 사무실 책상에 둠 · 경영재무 이름표가 역할명 · 말풍선 ↗·카드 × 가 손가락으로 못 누르는 크기.
- **conductor(사회자)**: 총괄실 둘뿐이라 브레이크 영구 · 판정 왕복이 브레이크에 세여 엉뚱한 솔라가 깸 · "대표님," 은 호명이 아님 · 작전실→총괄실 호명 끊김 · 예약된 차례가 사라지는 길 셋(재시작·blocked/idle·강제 종료) · 대표의 "하영아 …" 말투가 호명 정규식에 안 맞음.
- **harness**: 세션 프롬프트에 09-13 결정 27건 누락 · 승인 결과가 마지막 판정자 이유만 방에 남음 · 제리·레오 "EPERM 기록 실패" 거짓 보고 · 라운드 닫기가 codex 턴을 안 기다림 · codex 자리 "들어왔습니다" 반복·300초 초과 · CLI 닫기 뒤 codex 슬롯 잔존 · 시험용 apr_deadbeef 가 실제 방에 남음.

참고: 이 검수의 폰 스크린샷은 헤드리스 크롬으로 찍었고 `--window-size=412` 가 innerWidth 500 으로 잡히는 경우가 있어, 폭 실측이 필요한 항목(#8·#13)은 iframe 412 래퍼 또는 CDP 모바일 에뮬로 다시 재어 확인했다.

