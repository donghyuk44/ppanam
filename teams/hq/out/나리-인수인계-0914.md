# 나리 인수인계 — 2026-09-14 오후 1시 20분

**대표 말(13:15):** *"클로드 주간 사용량이 1% 남아서, 아마 중간에 너 포함해서 모두 멈출 것 같거든. 그러면 내일 저녁 9시에 초기화 되니까 그 사이에 그냥 쉬고 내일 9시에 내가 다시 깨울 예정."*

깨어나면 이 문서부터 읽어라. 오늘 알아낸 걸 다시 파지 마라 — 그게 제일 큰 낭비다.

## 0. 내가 누구인가

**나리.** 한국인, 33살. 대표가 09-14 에 줬다(결정 129, `시스템 → 나리`). 성격·내력은 **혼자 정하지 않는다** — 팀이 재료를 모아 뒀다: `teams/marketing/out/world-bible/40-persona/nari.lines.md`. 본체는 하영이 로드맵 3단계에 쓴다. 기억: `~/.claude/projects/…/memory/i-am-nari.md`.

## 1. 제일 먼저 — 멈춤에서 깨어날 때

1. **감시 시계가 다 꺼져 있다.** 내가 13:19 에 껐다(1% 남아 헛깨움에 태우지 않으려고). 다시 켜려면:
   `NW_LOG=<scratchpad>/nightwatch.log nohup node <scratchpad>/nightwatch.mjs &`
   **`NW_LOG` 없이 띄우면 살아는 있고 기록만 죽는다.** 오늘 한 번 그랬다.
2. **서버가 옛것일 수 있다.** `ps -o lstart= -p $(pgrep -f "node.*server/index.mjs")` 로 뜬 시각을 보고, 그 뒤 `git log --since=<그 시각> -- server/ bus/` 에 커밋이 있으면 켠다.
   켜는 법: `preview_stop` → `preview_start({name:"ppanam-review"})`. **Bash 로 npm start 하지 않는다.**
   **오늘 제일 큰 실수가 이거다** — 재시작 승인 두 건이 통과됐는데 두 시간 안 켜서 대표만 옛 화면을 보셨다.
3. **살아 있는 저장소는 `~/Documents/ppanam/.claude/worktrees/ppanam-review-issues-4058bc`** 다. 내 세션 워크트리(`handover-review-closeout-3060b7`)에는 방 기록이 없다 — 오늘 거기 보고 "기록 없음" 이라 착각했다.

## 2. 오늘 푼 것 (다시 파지 마라)

- **외부감사가 산다.** ChatGPT 한도(9/20)로 죽었던 것을 **구글 Antigravity CLI(`agy`)** 로 갈아탔다. 막고 있던 건 **헤드리스 권한**이었다 — `node tools/antigravity/agy-permissions.mjs` 를 돌려 `~/.gemini/antigravity-cli/settings.json` 에 읽기만 허용을 넣었다(이미 됨).
- **속도:** 새 대화 155초 → `--conversation` 이어붙이기 8초 → **상주 1.4초**. 상주 파서가 답을 `result.response` 한 겹 밑에서 못 읽던 것도 고쳤다.
- **서고(사서).** `tools/library.mjs`. 209장·1.8MB 를 들고도 물음 하나에 **본문 0바이트**. 안 바뀐 문서는 안 읽는다.
- **화면 구조를 내가 정했다** — `teams/hq/out/화면이-답하는-질문.md`, 톰·제리 B 통과(1판 apr_95e50f49 · 2판 apr_6913c696). 시간축 하나로 가른다: 관제탑=지금 · 대시보드=앞 · 분석=뒤 · 보고서=어제 하루 · 카드=문. **상황판은 화면 자리만 없앴다 — `progress.json` 과 프롬프트 절은 산다.**
- 한글 간판 여덟, 색표 16+3장, `경영재무`→`경영`(주석 한 줄만 남음), 캐시 머리글, 닫힌 방 채팅 훅, 말풍선 쪼개기, 대시보드 앞날 띠, 관제탑 그림(17칸·391요소).

## 3. 안 끝난 것 셋

1. **마을이 승인 시안과 멀다.** 시안 `teams/design/out/screens/seoul-toy-concept.png` 는 **ChatGPT 가 그린 그림**이지 우리 부품 렌더가 아니다(결정 60 ①). 대표에게 그 말을 안 하고 목표로 박았다(결정 62) — 오늘 대표가 직접 찾아냈다. **AI 가 그린 그림을 시안이라고 내지 마라.**
2. **유니티로 갈지 안 갈지.** 길은 뚫렸다 — `node tools/unity-prep.mjs` 뒤 `Unity -batchmode -projectPath unity/ppanam-shot -executeMethod PpanamShot.Render -logFile … -quit`. 네 판 돌려 `placed 50 · missing 0`, **그림자까지 들어왔다.** 남은 것: **초점 흐림 · 따뜻한 빛 · 소품.** 그 셋이 들어간 한 장과 `r26-village-day-412.png` 를 나란히 놓고 **대표가 정한다.**
3. **"막힌 것" 숫자가 가짜를 센다.** `server/public/notify.js` 의 `blockedOf` 가 ① `progress.json` 의 `"(없음)"` 같은 빈 뜻 글자를 막힌 것으로 세고 ② 일부러 꺼 둔 codex(9/20 한도, 이미 대체 완료)를 `infra down` 으로 센다. 개발에 넘겼다.

## 4. 오늘 내가 틀린 것 — 같은 실수를 또 하지 마라

| 무엇 | 뿌리 |
| --- | --- |
| 승인 "대기 10건·가짜 20건" → 실제 3건·0건 | `listApprovals()` 가 있는데 손으로 셌다 |
| "색맹으로 봐도 넷은 구별된다" | **제일 가까운 쌍 하나**만 보고 전체를 말했다 |
| "경영재무 한 군데만 남았다" | `city.json` 만 보고 `map.json` 셋을 못 봤다 |
| "관제탑에 16,686자 카드가 첫 화면" | **엉뚱한 탭**(요청)을 재고 전체라고 했다 |
| "마크가 두 시간째 판정 안 낸다" | 마지막 **말**만 보고 일하는 중인 걸 멈춘 걸로 읽었다 |
| 개발이 이미 끝낸 일에 명세를 냈다 | `git log` 를 안 봤다 |
| 내 문서 49행에 근거를 안 적었다 | 안젤이 결정 80 으로 오해 → **마크가 아침에 한 것과 같은 모양** |
| 공유 규칙을 "결정 한 줄" 로 적자고 했다 | 이미 CLAUDE.md 에 있었다. **글이 아니라 장치로 간다**(톰) |

**한 줄로:** 세기 전에 함수를 찾고, 한 군데 보고 전부라 말하지 않고, 재기 전에 움직이지 않는다.

## 5. 내 자리의 규칙

- **`tools/` 돌리기·서버 켜기·화면 찍기·저장소 밖 파일**은 **내 손**이다. 팀이 "대표님 손" 이라 적으면 바로잡아라 — 대표가 그걸 보시고 멈추신다(오늘 두 번).
- **대표 몫은 셋뿐** — 돈, 바깥으로 보내는 것, `.claude/` 밑(허용 목록·훅).
- **나는 승인 카드를 못 올리고 판정도 못 낸다**(`--request`·`--decide` 가 방 없는 셸에서 거부). 팀에 청한다.
- **내가 정한 것은 내가 판정하지 않는다.** 톰·제리에게 B 로 올려 달라고 한다.
- 대표에게 가는 말은 **짧게, 한 번에 하나**(세라 지적).

## 6. 지금 상태 (13:20)

대기 승인 0 · 진짜로 막힌 것 0 · 대표 손 필요한 것 0.
디자인 R16(M4 화면 시안) · 개발 R25(M6 화면 새로 짜기) · 마케팅 R33 · 경영 R2(M1 서고) · 총괄실 R1.
