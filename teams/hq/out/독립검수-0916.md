> 기록 문서 — 2026-09-16 에 쓴 것. 그 뒤 뒤집힌 결정이 있을 수 있다. **지금 맞는 것은 `teams/hq/out/현황.md`(살아 있는 한 장)와 `teams/dev/decisions.md`(번호 장부 — `teams/hq/out/proxy-decisions.md` 는 그 부록)** 다. 이 문서의 문장이 그 둘과 다르면 이 문서가 옛것이다.

# 밤에 만든 장치는 대체로 도는데, 오늘 아침 끝난 일이 거의 없습니다

*독립검수 1판 · 2026-09-16 09:20~09:30 KST · 읽기 전용 · 저장소 `.claude/worktrees/ppanam-review-issues-4058bc`*
*로그의 ts 는 세계표준시라 +9시간 해서 읽었습니다. 아래 시각은 전부 우리 시각입니다.*

## 대표가 알아야 할 것 다섯 줄

1. 오늘 아침 세 시간 동안 닫힌 회차 1개, 통과한 단계 0개입니다.
2. 마케팅은 06:56 에 감사 둘 다 통과했는데 두 시간 반째 안 닫혔습니다.
3. 돈 계산이 3.8배 부풀려 있습니다 — 실제 75달러, 장부 합 282달러.
4. 만든 것이 아직 원격에 없습니다 — 254개, 사흘째 그대로입니다.
5. 노라가 71분 말이 없었는데 장치는 못 찾고 나리가 불러 깨웠습니다.

---

# A. 아침 점검표 대비 적용 현황

기준: `teams/hq/out/점검-0916.md`(06:38) 4절 "지금부터" 와 3절 해결안, 그 뒤 문서 열둘.
판정: **됨**(코드·상태 파일로 확인) · **반쪽**(일부만) · **말만**(문서에만) · **되돌아감**.

## A-1. 토큰·캐시 (점검 3-1)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| 토큰 장부 `state/usage.jsonl` | **됨** | `server/usage.mjs:17 recordUsage` → `server/session.mjs:475` 에서 실제 호출. 70줄, 첫 줄 08:04, 끝 09:22. 자리·회차 필드 있음 |
| 장부 금액 정확성 | **되돌아감(버그)** | `costUsd` 는 세션 누적값인데 `todayUsage`(usage.mjs:52)가 **줄마다 더한다**. design:guide 11줄이 2.08→6.09→…→19.75 로 단조 증가. 합산 $281.72 vs 실제(세션별 최종값 합) **$74.95** — 3.76배 |
| 대시보드 "오늘 쓴 것" 한 줄 | **말만** | `todayUsage` 호출처 0. `server/public/app.js` 에 "오늘 쓴 것"·usage 문자열 0건 |
| 회차 끝에 세션 안 버리기(T2) | **됨** | `server/session.mjs:713 if (milestonePassed) reset(team)` — 커밋 c439733(08:10). CLAUDE.md 표도 같이 고침(11ca28a) |
| 책장 배선(결정 116 ①) | **반쪽** | 코드는 됨(`session.mjs:191 shelfOf`, 275~296 에서 인격 다음 칸). 그런데 **`shelf.md` 가 `teams/dev/` 하나뿐**. 점검이 "접두가 없다"고 지목한 경영팀엔 없음 |
| 큰 파일 전문 Read 금지 | **반쪽** | dev/shelf.md 에만 글로 적힘. 코드로 막는 것 없음 |
| 외부감사 인격 전문 재전송 중단(T3) | **감사 대기** | work.json T3 = "감사 대기". `bus/outside.mjs:300 assemblePrompt` 그대로 |
| 훅 matcher 에 Bash·Grep | **말만** | `.claude/settings.json` PostToolUse matcher = `Write\|Edit\|NotebookEdit\|WebFetch\|Read`. Bash·Grep·Agent 없음 |
| `_check` 유령 세션 | **반쪽** | `state/sessions.json` 에서 빠짐 ✓. 그런데 `state/conductor.json._queue` 에 `_check`(round 0, 09:05 저장)·`sera` 항목 **아직 남음** |
| 아침 8회 서버 재기동 → 0 | **반쪽** | 오늘 아침 재시작 결재 요청 **1건**(apr_b9213844, 07:01). 밤 15건에서 크게 줄었음 |

## A-2. 감사 규칙 (점검 3-9)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| 판정 지시문 "떨어뜨릴 이유 셋" | **됨** | `bus/bus.mjs:116-127 verdictInstruction` — conductor·outside 같은 글 |
| 정형문 PASS 를 REVISE 로 되돌림 | **됨, 실제로 돎** | `bus.mjs:134-138 passShapeError`, `1810` 에서 적용. 오늘 **2회 발동**(마케팅 06:48·06:55, `verdictShape` 이벤트) |
| 같은 규칙의 헛발 | **부작용 있음** | 06:55 안젤의 "PASS 기록됐어요. 순서대로 다니엘 차례로" 한 줄이 판정으로 잡혀 REVISE 로 뒤집힘 — 판정이 아닌 말이 판정 칸에 들어감 |
| 총괄실은 모양 검사 안 함 | **의도된 구멍** | `bus.mjs:1810 !isOffice(team)`. 제리 결재 PASS 7건(오늘)은 전부 "…대조를 마쳤습니다" 정형문 그대로 통과 |
| 퀄리티 잣대 `teams/<팀>/bar.md` | **반쪽** | 다섯 장이 **있음** — 단 `teams/marketing/out/bar/*.bar.md`(07:29~07:30, 하영 초안). 팀 폴더로 안 옮겨졌고 코드에 `bar` 배선 0 |
| 감사 무게 나누기(작은 B) | **됨** | `approve.mjs --small`, 커밋 75790b7 |
| 나리 무작위 재검 하루 셋 | **말만** | 기록 0 |

## A-3. 멈춤·자동 재개 (점검 3-7)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| ㉠ 회차 닫힘 뒤 자동 이어열기 | **됨** | 커밋 9840773(07:15), `bus.endRound`. 경영 R5 가 06:59 에 자동으로 열림(`라운드 4 이 닫히며 넘어온 차례 1건` note) |
| ㉡㉢㉣ 멈춤 감시 `checkStalls` | **됨, 도는 중** | `server/conductor.mjs:534`, `server/index.mjs:797` 틱 배선. 오늘 순찰 표 **6회**(08:24·08:44·08:45·08:46·09:04·09:24) |
| 그런데 깨우는 대상 | **구멍** | `checkStalls` ㉢ 은 `session.ownerOf(team)` **한 자리만** 부른다. 둘째 자리(노라·안젤·솔라·클레멘타인)가 조용하면 아무도 안 부른다 — 오늘 노라 71분이 그것 |
| 외부감사 10분 자동 판정 `autoVerdicts` | **한 번도 안 돎** | `conductor.mjs:495` 배선 됨. 로그에 `"verdictFlow":"auto"` **0건** |

## A-4. 대표 화면 (점검 3-4·3-5·3-6, 사용성 문서)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| 대표 화면 글 하네스 말 0줄 | **됨 — 검수관이 직접 돌림** | `node tools/boss-words-check.mjs --all` → **"대표 화면 글 43줄 중 하네스 말 0줄 (0%)"** (06:38 점검 땐 48줄 중 45줄 94%) |
| 첫 화면 = 대시보드 | **됨** | `server/public/app.js:2628 setView(hashView ?? 'tower')` |
| 탭 다섯 표준어 | **됨** | index.html 45~49 채팅·대시보드·타임라인·리포트·설정. 분석은 리포트 서브탭(hidden) |
| 마을 숨김 | **됨(빈틈 하나)** | index.html 53 `hidden`. 단 `app.js:1104` 이 `#팀/world` 로 한 번 들어가면 단추가 다시 뜸 — 안젤 U7 이 같은 지적 |
| 보고서 PC 1280 버그 | **됨** | 74ab37a `style.css` `.report` 추가. 안젤 2판이 사진(`usability-report-1280.png`)으로 재확인 |
| 인격 목록 중복 | **됨** | d64d543 — 자리 단위 → 사람 단위 |
| 작업판 블록 `/api/work` | **됨(오늘 09:2x 부터)** | `server/index.mjs:440` 라우트, 검수관 실측 `curl localhost:4321/api/work` → **200, streams 있음**. 08:26 커밋 당시 404 라던 것이 풀림 |
| `state/work.json` | **있음** | 40건. 구조는 깨끗 — 순환 0, 고아 의존 0 |
| 카드 체계(card.js) | **반쪽** | `card.js`·`card.css`·`/api/card/<팀>` 200 ✓. 화면에 꽂는 C7 은 **대기**(work.json) — `app.js` 에 card.js import 없음 |
| 빌드 신선도 (src vs public) | **거의 맞음** | `server/public/app.js`(08:26) vs `server/src/app.js`(09:24) 차이 **2줄**(방 이름 기본값). 09-15 처럼 크게 벌어진 상태는 아님 |
| 세계관 서브탭 | **말만** | 설정 화면(loadSettings, app.js:1158)은 있으나 "세계관" 서브탭은 주석 한 줄(app.js:1170)뿐 |

## A-5. 위임·권한 (점검 4절, 권한-현황-0916)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| `state/delegation.json` | **있음** | `to: system`, `until 2026-10-01`, 결정 141. 07:24 생성 |
| `approve.mjs --as system` | **됨** | `bus/approve.mjs:79·113·216` — 환경 없는 셸에서만 |
| system 이 실제로 낸 판정 | **5건, 전부 07:43 한 순간** | `state/approvals.jsonl`. chief 판정 마지막 06:40. **07:43 이후 판정 0건**(1시간 40분) |
| 톰이 대신 옮겨 적은 건 | **0건** | 07:43 이후 approvals 에 아무 줄도 없음. 결재 큐 자체가 멈춤 |
| 훅의 `<analysis>`·영어 필터 | **됨** | `.claude/hooks/to-bus.mjs:174-175` 두 줄 실재 |
| `Bash(node tools/*)` 허용 | **됨, 다만 자리마다 다름** | settings.json allow 26줄에 있음. 안젤은 08:07 에 `screen-shot.mjs` 로 직접 사진을 찍었고, 같은 아침 헨리는 `diff-0916.md` 에 "이 세션은 screen-shot.mjs 가 막혀 새로 못 찍었다", 유진은 07:04 에 "제 세션은 node tools/ 가 막혀" |
| agy(외부감사) deny `unsandboxed(*)` 제거 | **됨** | 커밋 1561e8b. 다만 효과는 아래 A-6 |

## A-6. 외부감사 (점검 3-10)

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| 도구 사건을 방 tool 이벤트로 | **말만** | `server/gemini.mjs:41` 은 여전히 `text_delta` 계열만 읽음. `step_update`·`tool_name` 파싱 0. 오늘도 외부감사 넷의 도구 이벤트 **0** |
| 판정 차례 모델 3.1-pro / 말 차례 3.8-flash | **말만** | 다섯 cast.json 전부 `geminiModel: gemini-3.6-flash`. `bus.mjs:912 GEMINI_MODELS` 도 옛 목록 그대로 |
| 폴백 `cast.json` fallback | **말만(2일째)** | 다섯 팀 cast.json 에 `fallback` 키 **0개** |
| codex(원래 다니엘) | **막힘** | `state/outside-cooldown.json` — 2026-09-20 15:38 까지 사용량 한도. 다섯 자리 전부 gemini 대체 |

## A-7. 인격·함정·부분 성공

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| 경영 인격 파일 셋 | **됨** | d49d883 — `teams/finance/guide.md`·`review.md`·`outside.md` |
| 세라 인격 파일 | **됨** | 3792cb4 — `teams/sera/secretary.md` 86줄 |
| 인격 40줄 | **전부 안 됨** | 열다섯 장 모두 40줄 초과. 최소 72줄(finance), 최대 152줄(marketing/guide) |
| 나리 인격 파일 | **없음** | `teams/hq/system.md` 없음. 세라가 09:28 에 "배경 절이 없는 사람은 나리 하나" 라고 대표께 직접 확인 |
| `traps.md` | **반쪽** | `teams/marketing/traps.md` 하나(07:41). 나머지 네 팀 0. **프롬프트 배선 0** — `session.mjs`·`bus/*` 에 traps 문자열 없음 |
| 부분 성공(PARTIAL) | **말만** | `bus/bus.mjs:28 VERDICTS = PASS/REVISE/FAIL` 그대로 |

## A-8. 서브에이전트 상한·모델

| 항목 | 판정 | 근거 |
| --- | --- | --- |
| "한 자리 최대 둘" | **말만 — 코드 0** | 방에 공지로만 나감(경영 07:45 등), `proxy-decisions.md` 07:45 에 기록. 서버·버스·설정 어디에도 세는 곳 없음 |
| Agent 툴 금지 | **글로만** | `session.mjs:227` 프롬프트 한 줄. 그런데 커밋 bde6c26(07:54)에 "테라 손 하나(서브에이전트)가 씀" — 규칙과 실제가 어긋남 |
| 서브에이전트 사용 측정 | **잴 수 없음** | 훅 matcher 에 Agent/Task 가 없어 로그에 안 남음. "Agent/Task 0건" 은 **안 썼다는 증거가 아님** |
| 모델 opus·sonnet 만 | **지금은 맞음, 강제는 반쪽** | `state/teams.json` 여섯 팀 전부 opus, cast 의 `llm` 은 ops·review 넷만 sonnet. fable 0건. 다만 `CLAUDE_MODELS`(bus.mjs:909) 검사는 `updateCastAgent` 경로에만 걸려, **파일 직접 수정은 안 걸린다** — 실제로 07:41 에 디자인이 fable 로 바뀌었다가 07:45 에 되돌려졌고 git 에 자국이 없다(`git log -S fable` 0건) |

---

# B. 팀별 실행력·역할 이행

기간: **09-16 06:00~09:20**(밤은 09-15 21:00~06:00, 비교용).
"발언/도구/판정" 은 `teams/<팀>/log.jsonl` 의 message·tool·verdict 이벤트를 센 것.

## B-1. 자리별 표

| 자리 | 팀 | 발언 | 도구 | 판정 | 30분+ 공백 | 밤 대비 | 역할 이행 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 테라 guide | 개발 | 29 | **299 (31%)** | 0 | **0회** | 발언 82→29, 도구 660→299 | **냈다** — 커밋 다수, 공백 0 |
| 솔라 ops | 개발 | 25 | 192 (20%) | 0 | 2회 87분 | 도구 95→192 (두 배) | **냈다** — T1·T4·O1·W1·O4 커밋 |
| 헨리 guide | 디자인 | 25 | 154 (16%) | 0 | 1회 41분 | 도구 175→154 | **냈다** — `screens/diff-0916.md` 실물 |
| 클레멘타인 ops | 디자인 | 25 | 123 (13%) | 0 | 1회 60분 | 도구 31→123 (4배) | **냈다** — 커밋 제목 언급 13건, 최다 |
| 하영 guide | 마케팅 | 33 | 85 (9%) | 0 | 1회 33분 | 도구 197→85 | **냈다** — bar 다섯·boss-lines·traps |
| 안젤 review | 마케팅 | 22 | 38 (4%) | **5** | 3회 127분 | 판정 5(밤도 5) | **냈다 — 둘 다** 사용성 2판 + 판정 5 |
| 유진 guide | 경영 | 13 | 31 (3%) | 0 | 2회 71분 | 발언 66→13 | **냈다** — daily-template·감사-F2 |
| 노라 review | 경영 | 10 | 13 (1%) | 0 | **3회 176분** | 발언 70→10 | **늦게 냈다** — 09:18 F2, 09:26 감사-F1 |
| 톰 chief | 총괄 | 44 | 28 (3%) | 0 | 1회 71분 | 발언 157→44 | **냈다** — 따라가기 4판·의견수렴 |
| 세라 secretary | 총괄·비서실 | 9+11 | 7 | 0 | 2회 152분 | — | **반쪽** — 아침 초안 07:03, 06:30 정시 보고는 없음 |
| 나리 system | 여섯 방 | 106 | 0 | **5**(07:43) | 방마다 40~100분 | — | **반쪽** — 아래 B-4 |
| 레오 outside | 개발 | 2 | **0** | **1** | 2회 175분 | 판정 8→1 | 판정 1건, 모양 규칙 지킴 |
| 다니엘 outside | 마케팅 | 1 | **0** | **1** | 2회 200분 | 판정 3→1 | 판정 1건 |
| 마크 outside | 디자인 | 2 | **0** | **0** | 2회 199분 | 판정 3→0 | **오늘 판정 0** |
| 빅터 outside | 경영 | **0** | **0** | **0** | 종일 | 판정 3→0 | **오늘 방에 한 마디도 없음** |
| 제리 outside | 총괄 | 14 | **0** | 0(결재 7) | 2회 159분 | 발언 70→14 | 결재 대조 7건, 전부 정형문 |

**대표가 지적하신 것에 대한 답**

- **테라 쏠림**: 밤 50% → 아침 **31%** 로 떨어짐. 개발 팀 전체는 51%. 여전히 가장 큰 자리지만 밤보다 나아졌고, 솔라·클레멘타인이 각각 두 배·네 배로 올라와 분산됐다.
- **솔라·노라·안젤·클레멘타인이 자기 몫을 냈나**: 솔라 ○(커밋), 안젤 ○(사용성 2판 + 판정 5), 클레멘타인 ○(커밋 최다), **노라는 71분 지각 뒤 냈다**(09:18 `아침한장-잣대.md`, 09:26 `감사-F1-노라.md`).
- **상호 감사가 기록으로 있나**: **경영에서는 진짜로 돈다.** 09:21 유진→노라 F2 감사에서 "`teams/hq/out/reports/` 엔 보고서 0장인데 '다섯 절 09:00 보고서' 라 적혔다" 를 잡았고, 09:26 노라→유진 F1 감사에서 "테라 줄이 R30 인데 rounds.jsonl 마지막은 R31" 을 잡았다. 둘 다 고쳐졌다. 다른 팀은 아침에 상호 감사 판정 기록 0.
- **외부감사가 판정을 냈나 말만인가**: 오늘 아침 외부감사 판정 **2건**(레오 1·다니엘 1). 레오 07:04 건은 "떨어뜨릴 이유 1: 1280px 리포트 그리드…— 반박: 74ab37a 와 b0635-… 사진" 으로 **새 규칙을 지켰다**. 마크·빅터는 0건. 빅터는 방에 한 마디도 없다.
- **톰이 운영 총괄을 냈나**: **냈다.** `따라가기.md` 09:15 4판까지, `의견수렴-0916.md` 완성. 다만 자기 표의 숫자가 틀렸다(아래 C-2).
- **세라가 보고를 냈나**: 아침 한 장 초안 `teams/hq/out/sera/2026-09-16-아침-초안.md`(07:03) 를 냈다. 06:30·18:30 정시는 07:14 에야 정해져 **오늘 06:30 정시 보고는 없었다**. 비서실에서 대표 질문 여섯에 직접 답했다(07:02~09:28).

## B-2. 회차 — 아침에 끝난 것이 거의 없다

| 팀 | 회차 | 단계 | 시작 | 09:20 현재 | 오늘 닫힌 회차 |
| --- | --- | --- | --- | --- | --- |
| 개발 | 32 | 10 | 07:20 | 열림 3시간 | **1개**(R31, 07:15 PASS) |
| 디자인 | 18 | 5 | 06:59 | 열림 3시간 21분 | 0 |
| 마케팅 | 36 | 5 | **02:12** | 열림 **7시간 8분** | 0 |
| 경영 | 5 | 4 | 06:59 | 열림 3시간 21분 | 0 |
| 총괄 | 2 | 1 | 06:08 | 열림 3시간 12분 | 0 (rounds.jsonl 아직 없음) |
| 비서실 | — | — | — | 회차 없음 | — |

**통과한 마일스톤 0개.** `teams/*/rounds.jsonl` 의 마지막 줄은 각각 개발 R31(07:15)·디자인 R17(04:23)·마케팅 R35(02:09)·경영 R4(05:31).

**가장 아픈 것 — 마케팅 R36**: 06:55 판정 시작 → **06:56 "안젤·다니엘 모두 통과. 이제 하영이 이 회의를 통과로 닫을 수 있습니다"**. 그런데 09:20 까지 안 닫혔다(2시간 24분). 그 사이 멈춤 감시가 하영을 두 번 불렀다(08:44·09:04). 하영은 죽지 않았고 그 시간에 다른 산출물을 냈다 — **닫는 일이 아무의 몫도 아니다.**

## B-3. 노라 71분 침묵 — 원인 추적

로그로 잰 것(`teams/finance/log.jsonl`):

| 시각 | 무슨 일 |
| --- | --- |
| 08:04 | 나리가 직책 바꿈 알림 → 노라 "알겠어요 — F2 는 제 이름으로" |
| 08:24 | `checkStalls` ㉢ 발동 — "방이 20분째 조용합니다" → **유진에게** 물음 |
| 08:25 | 유진: "노라, 쓰기가 아직 막혀 있으면 사고 신고니 나리한테 말해 줘" |
| 08:45 | `checkStalls` 두 번째 — **또 유진에게** |
| 08:45 | 유진: "노라가 08:04 뒤 40분째 말이 없고 F2 파일도 안 생겼어요" |
| 09:04 | **나리가 노라를 직접 부름** |
| 09:14 | 나리 재촉 — "2분 안에 답 없으면 서버를 다시 켜요" |
| 09:15 | 노라: "살아 있어요 — F2 지금 씁니다" |
| 09:18 | 노라 F2 제출 |

**원인: 차례를 못 받은 것도 세션이 죽은 것도 아니다.** `state/sessions.json` 에 `finance:review` id 살아 있고, `state/infra.json` 은 09:21 에 "살아 있음 8 · 좀비 0". `checkStalls` ㉣(꺼진 자리)는 세션이 살아 있으면 안 걸리고, ㉢(방 침묵)은 `session.ownerOf(team)` **한 자리만** 부른다(`conductor.mjs:571 enqueue(team, owner, 'status')`). 그래서 둘째 자리가 조용한 것은 장치가 원리적으로 못 잡는다.

**재발 가능한가 — 예.** 아침에 재발했고, 깨운 건 또 나리다. 코드가 그대로면 내일도 같다.

## B-4. 나리 — 같은 잣대로

| 냈나 | 근거 |
| --- | --- |
| 냈다 — 점검표 한 장 | `점검-0916.md` 32KB, 오늘 아침 모든 팀 지시의 뿌리 |
| 냈다 — 대리 결정 판정 5건 | 07:43, `approvals.jsonl` by:system |
| 냈다 — 방마다 지시 106회 | 여섯 방 system 발언 합 |
| 냈다 — 문서 15장 | `teams/hq/out/` 오늘 수정 20파일 |
| **못 냈다 — 자기 인격 파일** | `teams/hq/system.md` 없음. 09:28 세라가 대표께 직접 보고 |
| **못 냈다 — 07:43 이후 판정 0** | 결재 큐가 1시간 40분째 비어 있음 |
| **되돌아간 것 — fable** | 07:41 에 디자인 모델을 fable 로 바꿨다가 07:45 에 되돌림. 대표 결정이 "한도" 였는데 대리 판단이 반대로 갔다 |
| **반쪽 — 문서가 코드를 앞지름** | 오늘 나리가 만든 문서 15장 중 코드 배선이 끝난 것은 셋(토큰 장부·멈춤 감시·회차 재개) |

---

# C. 운영 실태·작업 방식 공격 검토

## C-1. 지금 이 순간 (09:20~09:30 실측)

| 항목 | 값 | 근거 |
| --- | --- | --- |
| 서버 | 살아 있음, 포트 4321 | `curl /api/work` 200 |
| 방 여섯 | 다섯이 회차 running, 비서실은 회차 없음 | `teams/*/round.json` |
| 살아 있는 자리 | 8 · 좀비 0 (sessions.json 항목은 11) | `state/infra.json` 09:21 |
| 유령 큐 | `_check`·`sera` 가 `conductor.json._queue` 에 남음 | `state/conductor.json` |
| 외부감사 실행 중 | 0 | `state/outside-running/` 빈 폴더 |
| codex | 09-20 15:38 까지 한도 초과 | `state/outside-cooldown.json` |
| 멈춤 기록 | 09-14~09-15 한 건만 | `state/pauses.json` |
| 오늘 쓴 돈 | **실제 약 $74.95** (08:04~09:22, 1시간 18분) | usage.jsonl 세션별 최종값 합 |
| 가장 비싼 자리 | design:guide **$19.75** · dev:guide $11.41 · design:ops $9.64 | 같은 파일 |

## C-2. 작업판 `state/work.json`

- 40건 · 감사 대기 13 · 통과 13 · 진행 7 · 대기 6 · 안 함 1
- **순환 0 · 고아 의존 0** — 구조는 깨끗하다.
- **앞이 안 끝났는데 진행/통과 2건:**
  - `H2(통과) ← H1(감사 대기)` — 세라 인격 파일을 제리 대조 전에 붙였다(커밋 3792cb4, 07:45). 감사 전 통과의 실물이다.
  - `O3(진행) ← O2(진행)`
- **톰의 따라가기 표(09:15 4판)가 정본과 안 맞는다:** 톰은 "통과 12·감사 대기 12·진행 6·대기 4" 라 적고 그 아래 13·13·6·6 개를 나열했다. 정본은 13·13·7·6. 어긋난 항목 셋 — `L2`(톰 통과, 실제 진행) · `G4`(톰 감사 대기, 실제 통과) · `R1`(톰 대기, 실제 감사 대기). 표를 지키는 사람의 표가 틀렸다.
- **병목 표시:** `bottlenecks` 여섯 중 "재시작" 은 오늘 아침 카드 1건뿐이라 이미 병목이 아니다. "나리 판정" 은 맞다 — 감사 대기 13건이 서 있다.
- **우선순위 vs 실제 순서 — 안 맞는다.** `전체-작업표-0916.md` 는 1~4 토큰 · 5~6 권한 · 7~11 UI 순인데, 실제 커밋은 UI(보고서 폭 06:06, 첫 화면 06:18, 마을 숨김 07:23) → 권한(06:28) → **토큰 1위가 07:49**(6e56d92). 1순위가 세 번째로 들어왔다. 그리고 아침 커밋 58개 중 초상·칩 관련이 13개(22%) — 우선순위 표에 없는 일이다(대표 08:2x 지시로 들어온 것).

## C-3. 화면

`server/public/app.js` 를 읽어 확인:
- 첫 화면은 `tower`(대시보드) — 2628행.
- 대시보드 첫 블록이 작업 보드(1583행 "⓪ 작업 보드 — 맨 위 한 블록"), `/api/work` 200 이므로 지금 뜬다.
- **판정 도장 원문(PASS/REVISE)이 화면 코드에 아직 남아 있다** — 낱말 매핑(`VERDICT_WORD`)은 `card.js` 에 있으나 `card.js` 가 `app.js` 에 안 꽂혔다(C7 대기). 개발 로드맵 10단계 제목이 "대표 화면 사람 말 — 결정 140 · **판정 도장** · 정리" 인 이유.
- 카드 부품 `card.js`·`card.css`·`/api/card/<팀>` 은 다 있는데 세 곳에 꽂는 일(C7)이 **테라 대기** — 부품만 있고 화면엔 없다.

## C-4. 아침 한 장 자동 생성 — 돌긴 도는데 읽을 수가 없다

`teams/hq/out/daily/2026-09-16-자동.md`(08:24, runMorning 첫 산출):

```
## 대표님 대신 정한 것
- 나리 · 아직 쉬운 말로 안 적음   ← 여섯 줄 전부 이것
## 된 것
- 개발 · 아직 쉬운 말로 안 적음
```

15줄 중 **7줄이 "아직 쉬운 말로 안 적음"**. 사람 말 검사는 거르기만 하고 채우지 않아, 자동 보고서의 절반이 빈 줄이다. 장치는 됐고 물건은 반쪽이다.

## C-5. 사용성 검수 — 안젤은 냈다

`teams/marketing/out/usability-0916-R36.md`(08:07). 사진을 **직접 찍었다**(`teams/marketing/out/shots/usability-*.png` 7장, `node tools/screen-shot.mjs`). 1판 아홉 줄을 고쳐짐 4 · 아직 1 · 그대로 1 · 미확인 3 으로 재판정하고 새 항목 U10 을 냈다. 오늘 아침 산출물 중 "물건을 열어 본" 감사의 모범이다.

## C-6. 저장소 위생 — 나빠졌다

| 항목 | 06:00 점검표 | 지금 | 방향 |
| --- | --- | --- | --- |
| 미푸시 커밋 | 196 | **254** (origin/claude/ppanam-review-issues-4058bc 대비) | **나빠짐** |
| 미커밋 파일 | 60 | **162** (수정 22 · 안 담긴 것 140) | **나빠짐** |
| 원격 | `git branch` 에 upstream 설정 자체가 없음 | 그대로 | — |

점검표 4절 첫 줄이 "푸시+커밋 — 첫 B 카드" 였는데, 오늘 아침 58커밋이 더 쌓였고 **푸시는 한 번도 없었다**. `.gitignore` 7행을 푼 것(6ba6d99)이 out/ 산출물을 커밋 가능하게 만들어 미커밋 수가 오히려 늘었다 — 그 자체는 맞는 방향이지만 커밋이 안 따라갔다.

**같은 작업 트리 섞임**: 따라가기.md 08:4x 덧에 기록됨 — "클레멘타인 커밋 d61339a 에 솔라 T3 가 섞여 들어감 — 같은 작업 트리라 index 가 섞인 것". 점검표가 밤에 2회라 한 것이 아침에 한 번 더.

---

## C-7. 구조적 문제 다섯 (실측으로 확인된 것만)

### ① 회차를 "닫는 일" 이 아무의 몫도 아니다
- **근거**: 마케팅 R36 은 06:56 에 두 감사 모두 통과했는데 09:20 까지 `running`(2시간 24분). 아침 세 시간 동안 닫힌 회차 1개·통과한 단계 0개. 다섯 방이 3~7시간째 열려 있다.
- **왜 지금 장치로 재발하나**: `checkStalls` 는 "20분 사건 없음"만 본다. **"판정이 다 났는데 안 닫혔다" 를 보는 눈이 없다.** 오늘도 하영을 두 번 불렀지만 문구가 "지금 뭐 하나" 라 하영은 다른 일을 답하고 끝났다.
- **고칠 것 한 줄**: 판정 흐름이 완료(`판정 완료` note)된 뒤 N분 안에 `round_end` 가 없으면 서버가 실무에게 "닫으세요" 한 줄을 주고, 두 번째엔 순찰 표 빨간 줄로 올린다.

### ② 멈춤 감시가 첫째 자리만 부른다 — 둘째 자리는 사각지대
- **근거**: `conductor.mjs:571·578` 이 `session.ownerOf(team)` 하나만 `enqueue`. 노라 71분(08:04~09:15) 동안 순찰이 두 번 돌았으나 **둘 다 유진을 불렀다**. 깨운 건 나리(09:04·09:14).
- **왜 재발하나**: 둘째 자리(솔라·클레멘타인·안젤·노라)는 오늘 아침 공백 합이 450분이다. 코드가 그들을 부를 길이 없다.
- **고칠 것 한 줄**: ㉢ 을 자리별로 — 그 회차에 몫이 걸린 자리(`work.json` 의 seat)가 20분 조용하면 **그 자리를** 부른다.

### ③ 장부·표의 숫자가 스스로 틀린다 — 잰다는 말만 맞다
- **근거 셋**: (ㄱ) `todayUsage` 가 세션 누적값 `costUsd` 를 줄마다 더해 **3.76배**($281.72 vs 실제 $74.95). (ㄴ) 톰의 따라가기 4판이 자기 나열과도 안 맞고 정본 `work.json` 과 셋이 어긋남. (ㄷ) 자동 아침 한 장 15줄 중 7줄이 "아직 쉬운 말로 안 적음".
- **왜 재발하나**: 새 장부·새 표를 만들 때 **그 숫자를 검산하는 검사가 통과 조건에 없다.** `round.mjs check` 114줄은 코드 동작을 재지 값의 정합성을 안 잰다.
- **고칠 것 한 줄**: 장부·표를 내는 일의 통과 조건에 "다른 파일로 같은 값을 한 번 더 세서 맞나" 한 줄을 넣는다(경영이 오늘 F1·F2 상호 감사로 실제로 한 그것).

### ④ 문서는 열다섯 장 늘고 코드는 셋 붙었다
- **근거**: 오늘 아침 `teams/hq/out/` 수정 20파일. 그중 코드까지 간 것 — 토큰 장부·멈춤 감시·회차 자동 재개 셋. 남은 것: `bar.md` 다섯 장(배선 0) · `traps.md` 한 장(배선 0) · 책장(네 팀 파일 없음) · 폴백(다섯 팀 빈칸, 이틀째) · PARTIAL(미구현) · 인격 40줄(열다섯 장 전부 초과) · 서브에이전트 상한(코드 0).
- **왜 재발하나**: 문서를 내는 것이 "냈다"로 세어지고, **배선이 끝나야 닫힌다는 조건이 없다.** `bar.md` 는 심지어 감사가 판정 첫 줄에 쓰도록 되어 있는데 감사 프롬프트가 그 파일을 모른다.
- **고칠 것 한 줄**: 규칙 문서는 "그 규칙을 읽는 코드 한 줄" 이 같은 회차에 들어와야 통과로 센다.

### ⑤ 강제되는 규칙과 말로만 있는 규칙이 섞여 있고, 구분이 화면에 없다
- **근거**: 강제됨 — 정형문 PASS 되돌리기(오늘 2회 발동)·`--as system` 환경 검사·`selfPassError`. 말뿐 — 서브에이전트 한 자리 둘(세는 코드 0)·Agent 툴 금지(프롬프트 한 줄인데 07:54 커밋에 "서브에이전트가 씀")·모델 opus·sonnet(파일 직접 수정은 검사 통과, 실제로 07:41 fable 이 들어왔다 나감, git 자국 없음)·큰 파일 전문 Read 금지(글로만).
- **왜 재발하나**: `CLAUDE_MODELS` 검사는 `updateCastAgent`(bus.mjs:987) 경로에만 있고 세션이 `cast.json` 을 직접 고치면 안 걸린다. 훅 matcher 에 Bash·Agent 가 없어 **어긴 것을 셀 수도 없다.**
- **고칠 것 한 줄**: 세션이 뜰 때 `cast.json`·`teams.json` 의 모델을 허용 목록으로 한 번 검사해 어긋나면 안 띄우고, 훅 matcher 에 `Bash|Agent` 를 더해 어긴 것이 기록에 남게 한다.

---

## 잘 돌아간 것 — 공격만 하면 그림이 틀립니다

- `node tools/boss-words-check.mjs --all` 실측 **43줄 중 0줄(0%)**. 06:38 점검 땐 94% 였다. 하루 만에 뒤집힌 유일한 큰 숫자다.
- 정형문 PASS 되돌리기가 **실제로 두 번 작동**했고, 레오의 07:04 판정은 새 규칙(떨어뜨릴 이유 → 반박 → 근거 커밋·사진)을 그대로 따랐다.
- 경영팀의 상호 감사가 **서로의 사실 오류를 두 건 잡아 고쳤다**(없는 보고서를 있다고 쓴 것 · R30/R31 오기). 오늘 아침 유일하게 "물건을 연" 감사다.
- 안젤의 사용성 2판은 자기가 찍은 사진 7장을 근거로 1판 아홉 줄을 재판정했다.
- 회차 자동 이어열기가 경영 R5 를 06:59 에 사람 손 없이 열었다.
- 서버 재시작이 밤 15건에서 아침 1건으로 줄었다.

---

## 검수관이 못 본 것

| 못 본 것 | 왜 |
| --- | --- |
| 화면을 눈으로 본 것 | 읽기 전용·서버 조작 금지라 `app.js` 코드와 `/api/*` 응답으로만 확인. 실제 렌더링·폰 412 는 안 봄 |
| 외부감사 세션 속 | `state/outside-sessions.json` 의 gemini 세션 안에서 도구가 실제로 돌았는지 — 방 기록에 안 남아(A-6) 확인 불가 |
| 서브에이전트 실사용 | 훅 matcher 에 Agent/Task 가 없어 로그에 자국이 없음. "안 썼다" 도 "썼다" 도 못 셈 |
| 09:20 이후 | 09:24~09:28 에도 파일이 계속 쓰이는 중이라 마지막 20분은 일부만 반영 |
| 밤 시간대 정밀 대조 | 점검-0916 의 밤 숫자(Read 445회·38.6MB 등)는 재계산하지 않고 인용만 함 |
| `state/requests/` 29건 내용 | 요청 블록의 개별 진행 상태는 표본만 봄 |
| 실제 토큰 청구액 | `costUsd` 는 Claude 가 돌려준 값이라 요금 명세와 대조는 못 함. "실제 $74.95" 도 세션별 최종값 합이라 **추정**이다 |
| 대표 화면 30초 시험 | 사람이 해야 하는 것 — 안젤 2판이 대신했으나 검수관이 재현하지 못함 |
