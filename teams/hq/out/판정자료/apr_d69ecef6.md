# 판정 자료 — apr_d69ecef6 (대리 결정 B — 마케팅 C 카드 apr_82ec927f "계획표 5판 — 7단계 제목 글자만" 을 대표 대신 통과시킬까) — 톰, 09-18 14:2x

판정은 나리(141 상시 위임 — 로드맵 변경은 C). 총괄실 카드라 제리 대조(183)를 불렀다 — 답이 오면 7절에 글자 그대로. 이 장은 물건을 연 것 · 자 결과 · 잣대 · 떨어뜨릴 이유 셋 초안(결정 142).

## 1. 물건을 연 것

| 무엇 | 본 것 |
|---|---|
| 제안 파일 | `teams/marketing/out/roadmap-5.proposed.json` 20,421 B(14:20). 카드 action 파일 이름과 같다. |
| 정본과 차이(스크립트로 잼) | destination 같다 ○. 컷리스트 일곱 글자까지 같다 ○. 단계 수 일곱 같다 ○. M1~M6 글자까지 같다 ○. 다른 건 M7 넷 — title · owner.하영 · owner.안젤 · deliverable, 전부 "붙다" 꼴을 보통 말로 바꾼 것(붙는 대로 → 새로 될 때마다 · 붙일 때마다 → 새로 될 때마다 · 붙인다 → 잇는다 · 안 붙은 탭은 "안 붙음" → 아직 안 된 탭은 "아직" · 붙이는 건 → 넣는 건 · 붙은 탭마다 → 새로 된 탭마다). 새 절 하나 `_proposal_7b`(하영 까닭 한 줄). |
| M7 안의 "붙" | 정본 M7 열둘 → 제안 M7 0. M4 하나·M5 일곱은 그대로(지난 단계, pass) — 카드 "7단계 제목과 조건 글 안" 그대로다. |
| 뜻이 바뀐 데 | 통과 조건 ④의 표시 글자 하나 — "안 붙음" 이 "아직" 으로. 낱말 바꿈이지 조건 자체는 같다(안 된 탭은 이 단계를 막지 않는다). |
| 근거 | 결정 216(09-18 10:5x 대표 원문 — 비유로 안 쓰는 낱말 스물여섯, 붙다 포함) ○. 까닭: 타임라인 화면 마케팅 줄에 7단계 제목이 그대로 떠서 사전 밖 말 0 이 안 된다 — 안젤 반려. 사진 `teams/marketing/out/r40-timeline-1280-tall.png` 는 내가 안 열었다(그림 판독은 나리). |
| 단계 상태 | 마케팅 M7 status now(라운드 40 열림) — 지금 하는 단계의 제목을 바꾸는 것. 순서·조건은 그대로라 라운드를 다시 열 일은 없다. |
| 대리 조건 | 결정 141 상시 위임 — 돈·바깥 없음 ○. |

## 2. 자 결과 — `node tools/boss-words-check.mjs`

- ✗ `결재 C apr_82ec927f (66자, 60 넘음)` — 대표 화면 "정할 것" 줄이 60자를 여섯 자 넘는다.
- ✗ `결재 B apr_d69ecef6 (74자, 60 넘음)` — 같은 글.
- 카드 글 안의 "붙는 대로" · "붙다 비유" 는 바꾸는 낱말을 따옴표로 인용한 것 — 문자 그대로 쓰임이라 216 위반은 아니다(내 읽기).

## 3. 잣대

- 마케팅 잣대 — 사전 밖 말 0 이 이 팀의 자. 이 카드가 그 자를 로드맵 제목에까지 적용하는 것 ○. 컷리스트 안 넘음 ○(컷 글자까지 같다). 마일스톤 — 7단계 안에서 글자만, 단계·순서 그대로 ○.
- 정본 하나(229) — 제안 파일 하나, 정본 하나. 마케팅 방엔 옛 `roadmap.proposed.json`(09-13 20:30, 5,186 B)이 남아 있는데 이 카드 파일이 아니다 — 지우는 건 관리 창 몫.

## 4. 떨어뜨릴 이유 셋 초안 (판단 아님)

1. 통과 조건 ④의 표시 글자 "안 붙음" → "아직" — 이미 쓴 검수표 2판(out/screen-30s-test.md)에 "안 붙음" 이 있으면 표와 로드맵 글자가 한 번 어긋난다. 하영이 같이 고치는지 카드엔 없다.
2. M4·M5 의 "붙" 여덟은 그대로다 — 지난 단계라 화면엔 안 뜨지만, 로드맵 파일 전체로 자를 대면 0 이 아니다. 이번엔 7단계만이라고 카드가 스스로 범위를 정했으니 뜻은 맞다.
3. 카드 줄 66자 — 대표 화면 C 줄이 자를 넘는다.

## 5. 이 뒤

둘 다 PASS 면 서버가 정본 `teams/marketing/roadmap.json` M7 넷을 바꾸고 방에 "대리 — 나리 위임". 톰은 결정 기록에 대리 결정 한 줄, 보드 마케팅 7단계 줄 이름은 그대로(what 에 "붙" 없음 — 확인은 통과 뒤 한 번).

## 7. 제리 대조 (gemini 자리, 09-18 14:2x — 글자 그대로)

- apr_d69ecef6 대조: 마케팅 계획표 5판 7단계 제목 및 Deliverable 문구의 '붙다' 비유 표현을 결정 216(보통 말 원칙)에 맞춰 '새로 될 때마다'·'아직 안 된 탭'으로 글자만 고친 것이 `teams/marketing/roadmap.json`과 `out/roadmap-5.proposed.json` 차이 대조로 확인됩니다. 단계 순서·조건·의미의 변경이 없으며 대표가 하지 않은 문구 추가도 없습니다.

톰 덧: codex 는 09-20 15:38 까지 한도라 gemini 자리로 온 답이다.
