# 권한 현황 — 자리별 되는 것·막힌 것 (2026-09-16 07:3x, 나리 실측 1판)

대표 원문(07:2x): "권한 상태 파악 조정 되게 급해 보이거든."

## 1. 권한이 정해지는 자리 넷

| 자리 | 파일 | 누가 고치나 | 지금 상태 |
| --- | --- | --- | --- |
| 팀 세션(클로드)의 명령 허용 | `.claude/settings.json` allow 목록 | 대표만(분류기가 나리를 막음, 09-16 06:3x 실측) | `node bus/*` 여덟 · `node tools/:*` · `node --check` · `node --test` · `cd` · `mkdir` · `cp` · `curl localhost` |
| 자리별 도구 금지 | `teams/<팀>/cast.json` disallow | 나리(대리) | 안젤·노라의 쓰기 금지를 07:2x 풀었음. 남은 금지 0 |
| 외부감사(gemini) 명령 | `~/.gemini/antigravity-cli/settings.json` | 나리(저장소 밖)·저장소 스크립트는 테라 | 06:2x deny `unsandboxed(*)` 뺌 → status·check 실행 확인 |
| 방 기록 훅 | `.claude/hooks/to-bus.mjs` · settings.json matcher | 대표만 | 압축 속말 한 줄 들어감(07:0x). Bash·Grep 은 훅이 안 남겨 "무엇이 막혔나"가 기록에 없음 |

## 2. 실제로 막힌 것 — 방 기록에서 센 것

| 누가 | 무엇 | 언제 | 원인(실측 또는 추정) |
| --- | --- | --- | --- |
| 안젤·하영 | `node tools/boss-words-check.mjs` | 06:44 | 허용 줄은 있음. 추정: 명령 앞에 `cd` 나 절대 경로 — 솔라가 맨 명령으로 시험 중 |
| 솔라 | `rm` 임시 파일 | 07:15 | `rm` 은 허용 목록에 없음(맞음 — 지우는 건 나리) |
| 노라 | `perl` 실측 | 05:24 | `perl` 허용 없음(맞음 — 자는 `node tools/` 로) |
| 세션들 | 스크린샷·유니티·빌드 | 밤새 | 나리 손으로 통일(결정) — 병목이나 안전 |
| 노라·안젤 | 파일 쓰기 | 밤새 | cast disallow — 07:2x 풀었음 |
| 레오·마크·다니엘·빅터 | 모든 명령 | 09-14~09-16 | 나리의 agy deny — 06:2x 풀었음 |

## 3. 고칠 것 — 순서

1. 솔라: 팀 세션이 `node tools/…` 를 맨 명령으로 돌려 되는지, 안 되면 거부문 그대로 — 원인 확정(32회차).
2. 훅이 Bash 도 남기게 matcher 한 줄(대표 손) — 이게 있어야 "무엇이 막혔나"를 기록으로 본다. 대표 손 명령은 다른 것과 묶어 한 번에.
3. 자리마다 "할 수 있는 명령" 을 인격 파일 40줄 안에 세 줄로 — 세션이 매번 부딪히지 않게.
4. 이 표를 현황 화면 "설정집" 탭에 그대로(테라, 34회차) — 대표가 권한 상태를 화면에서 본다.
