> 관리 창 나래의 계획 파일을 그대로 저장소에 옮긴 것(2026-09-18 11:0x). 대표가 "진행" 한 계획. 세라 검수용 — 잣대 다섯. 아래 글의 경로·번호는 실행용이라 그대로 두었다.

# 나래 개명 + 회사 법칙 순서 — 계획 (2026-09-18, 관리 창 나래)

## 대표께 드리는 요약 (쉬운 말)

**끝낸 일** — 인수인계 두 장과 기억 파일을 읽었습니다. 여섯 방 기록 끝을 봤습니다. 그저께 밤 아홉 시 뒤로 아무도 일하지 않았고 서버는 꺼져 있습니다. 대표님 말씀 전체를 저장소 실물과 하나씩 대봤습니다(읽기만, 고친 것 없음).

**다른 사람이 하는 중인 일** — (11:0x 갱신) 테라·솔라: 나래 이름표·프로필 카드·기록 화자(순서는 나리가 정함) · 하영·안젤: 나래 인격 열 줄, 한국어 낱말 규정집 · 세라: 이 계획과 참고물 검토 검수. (계획을 쓸 때는 서버가 꺼져 있어 아무도 일하지 않았습니다.)

**제가 할 일 — 이 순서로 (대표님 "진행" 뒤)**
1. 서버를 켭니다. 대표님 오늘 말씀을 글자 그대로 총괄실에 올립니다. 톰이 번호를 붙입니다.
2. 제 이름을 나래로 바꿉니다 — 사전에 한 줄(하영), 화면 파란 이름표(테라), 규칙·권한 한 장(톰), 제 기억 파일(나래). 방에서 노란 나리와 파란 나래가 다르게 보이면 끝입니다.
   같은 시간에 스킬 넷(슈퍼파워·카파시·웨이파인더·스펙킷)을 제가 직접 설치합니다. 컴퓨터가 막으면 대표님이 누르실 명령 한 줄을 드립니다.
3. 대표님 새 말씀이 일 앞에 끼어들지 않게 — 먼저 "기다리는 말씀" 한 장에 앉히고, "지금"이라 하신 것만 바로 합니다.
4. 헌법 한 장 — 세계관·목차·규칙·책장이 어디 있는지 한 장에 모읍니다. 지금 넷 다 있는데 흩어져 있습니다.
5. 팀원이 켜질 때 읽는 것을 셋으로 줄이고, 더 필요하면 사서(찾기 명령)에게 묻게 합니다. 같은 문서를 되풀이해 읽는 수를 세어 화면에 보입니다.
6. 일마다 책임자·담당·시작·끝·결과물 파일을 남깁니다. 결과물이 없으면 일이 닫히지 않습니다.
7. 대시보드 첫 판 — 대표님이 고르신 셋만.
8. 그 뒤 — 주제 스레드, 채팅 기능 여덟, 예쁜 PC·폰 화면, CS.

**왜 "빙빙 도는 감"인가 (실측)** — 대표님 집착이 아니라 파일에 없는 것이 있어서입니다. 일 76개에 시작·끝 시각이 하나도 없고, 결과물 검사 줄이 2개뿐이고, 한 사람이 열린 일을 열다섯·열아홉 개 들고 있고, 팀원이 켜질 때 순서 목록을 안 받습니다. 그래서 솔라는 한 회차에 같은 파일을 49번 읽었고, 그 회차 하나에 테라·솔라가 300달러 넘게 썼습니다. 화면은 이 사실을 못 세니 "구색만" 보입니다.

---

## Context (나래용 — 아래는 실행 문서, 대표 글에는 번호·경로 안 씀)

대표 원문(09-18 09:4x, 관리 창): ① 나리(시스템) → 나래 개명이 첫 일 ② 문제 — 사용량만 늘고 빙빙 도는 감 ③ 목표 — 슬랙+노션+카카오톡 플랫폼(PC 레딧/슬랙/노션, 모바일 카카오톡보다 편하게, 대시보드 GA보다 쉽게) ④ 대시보드 지표 여덟 ⑤ 프로젝트별 책임자·담당 매트릭스(가장 적합한 사람이 팀장) ⑥ 드라이브·폴더 세 층(헌법·경영·개발·마케팅·디자인·CS + 카테고리) ⑦ 스킬 넷(Superpowers·Karpathy·Wayfinder·Spec-kit) ⑧ "어떤 순서로?" ⑨ 세션 시작 규칙(최소 md + 어제 + 나는 누구 = 이어서, 필요한 건 사서에게) ⑩ 불만 둘(룰 안 따름 · 중간에 멈추고 시스템 안 따름). 대표가 공유한 `~/Documents/triptude/`(08월 프로젝트)에 spec-kit 스킬 열 개(`.claude/skills/speckit-*`)·헌법 예시(`.specify/memory/constitution.md`)·거버넌스 다섯 장·`harness.config.json`(requiredReads)이 있다 — "없는 건 여기서 가져오면 됨".

실측은 읽기 전용 워크플로 여섯(탐색 넷 → 설계 → 적대 검수, 1.15M 토큰)으로 했다. 결과 원문: `/Users/donghyukham/.claude/projects/-Users-donghyukham-Documents-ppanam--claude-worktrees-jovial-nightingale-5c7428/bcc06a1a-e5d8-429d-a9d0-990afbd124a5/subagents/workflows/wf_87f178d6-e34/journal.jsonl`. 살아 있는 트리 `~/Documents/ppanam/.claude/worktrees/ppanam-review-issues-4058bc`(HEAD ce86879, 서버·로그·state 여기). git 은 이 트리에서만, 내 파일만 `--` 경로, 커밋 직전 `git status`, 푸시는 원격 셋.

### 실측 요약 (숫자는 전부 파일에서 센 것)
- 세계는 09-16 22:10 KST 에 멈췄다(멈춤 감시가 9시 종료 뒤에도 13번 울림 — 같은 단계 자동 재개 `bus/bus.mjs:1811-1818` + `server/conductor.mjs:622 checkStalls` 에 종료 상태가 없음). 서버 프로세스 없음. 살아 있는 트리에 09-16 자리 세션이 남긴 미커밋 변경 10파일(state·png·md) — 남의 것, 손대지 않음.
- 나리 두 손: 화자 id 둘 다 `system`, 관리 창 말은 `bus/say.mjs --as system` → `meta.hand:'cli'`(파랑), 서버 나리는 훅 `hand:'server'`(노랑). 화면은 색만 가르고 이름은 둘 다 cast `name` '나리'(`server/src/app.js:673-684`). 관리 창 세션 자체는 훅이 안 남긴다(`.claude/hooks/to-bus.mjs:77-78`, PPANAM_TEAM 없음).
- 대표 항목 ↔ 저장소: 세계관 있음(`teams/marketing/out/world-bible/` 57파일) · 업무규칙 있음(`teams/hq/out/규칙.md` 46줄, `CLAUDE.md`) · 색인 있음(`teams/finance/out/library/toc.md` 298줄, manifest 519파일) · 책장 있음(`teams/<팀>/shelf.md` 여섯) · 사서 있음(`tools/library.mjs find` — 책장 두 줄이 "막혀 있다(P2)"라 적었으나 `.claude/settings.json` 에 `Bash(node tools/*)` 허용이 이미 있음, 자리 세션에서 실제로 도는지만 미확인) · 경영=finance(화면 이름 "경영") · 헌법 폴더 없음 · CS 없음(개발 컷 리스트 "CS 팀 신설 — 공백이 보일 때") · 인박스 없음 · '#' 스레드 없음(요청 블록 `state/requests/req_*.jsonl` 62건이 팀 간 스레드) · 채팅 기능 여덟(apr_cee2d254, 대표 C 대기)에 스레드 없음.
- 세션 시작: `server/session.mjs:299-324 assemblePrompt` = 인격 + 책장 + 결정 해석 + 일지 5문단 + 회차 브리프(상한 12,000자). `order.md`·`goal.md`·`규칙.md` 언급 0. 리셋 뒤 테라 첫 턴 Read 2, 솔라 첫 일 턴 Read 12(그중 `bus/bus.mjs` 5회).
- 반복 읽기(09-16, `teams/*/log.jsonl` tool 사건): 솔라 `bus/round.mjs` 72회·`bus/bus.mjs` 68회, 테라 `server/src/app.js` 83회; 한 회차 안 솔라 `bus.mjs` 49회. Read 사건에 offset/limit·session_id 가 안 남아 부분 읽기와 못 가른다. Skill 호출은 훅 matcher 에 `Skill` 이 없어 0으로 보일 뿐(transcript 에 48건).
- 토큰: `state/usage.jsonl` 자리·회차·세션 단위(카드 id 없음). 개발 R33 솔라 $120.49·테라 $192.66. 화면은 `SHOW_USAGE=false`(대표 "갑자기 달러??").
- 작업 보드 `state/work.json` 76건: 시각 필드 0(`doneAt` 코드는 있고 실물 0), `doneCheck` 2건, '막힘' 0건(bottleneck 글자만), order.md ↔ work id 연결 0. 열린 항목: 테라 19·솔라 15. '진행' 은 자리마다 1.
- 스킬 넷: Superpowers·Karpathy·Wayfinder 는 데스크톱 앱 번들(`anthropic-skills`)이라 이 창에서만 보이고 자리 세션(`server/session.mjs:345 spawn('claude', …)`, `--plugin-dir` 없음, transcript 9개 anthropic-skills 0)에는 없다. Karpathy 네 원칙은 `CLAUDE.md:42-45` 네 줄과 같다(이미 적용). Spec-kit CLI 0.16.5 있음(`specify init --help` 예시 줄: `specify init --here --integration claude`), init 은 `.claude/skills/speckit-*` 열 개 + `.specify/` 를 만든다 = `.claude/` 밑(대표만). triptude 에 같은 판이 이미 깔려 있다.
- 폴더 이름 바꾸기 비용: 팀 id = 폴더 이름(`bus/bus.mjs:856-867 paths`), 하드코딩 `'hq'` 141곳·`'dev'` 70곳·`'finance'` 9곳 + 색인 키 511 + work.json·log 의 team 칸.

---

## 집중 체크 담당 (대표 11:19, 비서실 — 기존 일에 +α)
- 나리(서버): 마케팅·경영이 잘하고 있는지 집중 체크(결과물 품질·대표 뜻 반영).
- 세라: 디자인·개발이 잘하고 있는지 집중 체크(결과물 품질·대표 뜻 반영, 시안=실물, 외부 서비스 창과 나란히 견준 기록).
- 나래: 총괄실 관리 — 원문 올리기·번호 확인·순서 목록에 들어갔나·서버·사진·푸시·브리핑·분배. "나래가 멈추면 전부 멈춘다" → 세라·나리에게 짐을 나눈다.
- 브리핑 문서 넷(11:20 비서실·총괄실에 경로로): 나래-계획-0918 · 그래프-보기-제안-0918 · 참고물-검토-0918 · 대표-원문-0918.

## 순서 (조건은 "무엇이 되면" — 시각 없음)

| # | 무엇 | 시작 조건 | 담당 | 끝 증거(파일) | 스킬(빌리는 모양) |
|---|---|---|---|---|---|
| 0 | 서버 켜기 · 감시 둘 다시 켜기(대표 말 / "나리," 부름) | 대표 "진행" | 나래 | `preview_start({name:"ppanam"})` 응답 200 · `pgrep -f server/index.mjs` 새 pid | — |
| 1 | 대표 원문을 총괄실에 글자 그대로(문단 일곱, 말풍선 일곱) + 같은 글을 `teams/hq/out/대표-원문-0918.md` 한 장 · 톰이 결정 번호(208~) · 제리 원문 대 배분 감사 | 0 끝 | 나래(올리기) · 톰(번호) · 제리(감사) | `teams/hq/log.jsonl` 에 actor boss(via 관리 창) 일곱 줄 · `teams/dev/decisions.md` 새 번호 | wayfinder 지도 다섯 절(목적지·메모·정한 것·안 정한 것·안 할 것) — 새 파일 대신 `goal.md`(목적지) · `decisions.md`(정한 것) · 3번 기다리는 말씀(안 정한 것) · `roadmap.json cutList`(안 할 것) 에 대응 |
| 2 | **나래 개명** (아래 "개명 파일 목록") | 결정 번호가 서면 | 하영(사전) → 테라(cast 여섯·app.js·build) · 솔라(say.mjs 한 줄 — 솔라 '진행 중'은 나리(서버)가 아침에 정한 아침 한 장 여섯이라 그 뒤 '다음' 첫 줄, 순서는 나리가 정함 — 세라 지적) · 톰(규칙·권한·goal) · 나래(기억 파일·인수인계 머리 줄·세션 제목·사진·푸시) · 레오 감사(코드) · 제리 감사(문서) | 총괄실 사진 412·750·1280 에 노란 '나리'·파란 '나래' 나란히 · 자리 파일 여섯에 나래 키가 다 있음(실제 키 이름은 테라가 넣은 것 — 실물은 `cliName`, 커밋 전 — 으로 `grep -c cliName teams/*/cast.json` = 6) · `cmp server/src/app.js server/public/app.js` 같음 · 기억 파일 갱신 | karpathy 3(손댄 곳만) · verification-before-completion(사진 본 뒤 "됐다") · 커밋 전 code-review(자리 세션에 있음) |
| 2-1 | **스킬 넷 설치 — 나래가 직접**(대표 09-18 10:3x "직접 가능한데?"). 테라·솔라가 개명 코드를 만드는 동안 나래가 자기 창에서: (a) 스펙킷 — 살아 있는 트리에서 `specify init --here --integration claude`(`.claude/skills/speckit-*` 열 개 + `.specify/`; triptude 에 같은 0.16.5 판 실물 있음, 안 되면 거기서 복사) (b) 웨이파인더 — `claude plugin marketplace add mattpocock/skills` → `claude plugin install mattpocock-skills@mattpocock`(이 창 wayfinder SKILL.md 에 적힌 설치 줄 그대로) (c) 슈퍼파워 — `https://github.com/obra/superpowers` README 를 먼저 열어 설치 줄을 인용한 뒤 그대로(안 본 명령은 안 친다) (d) 카파시 — `https://github.com/karpathy` 는 사람 페이지라 스킬 저장소가 아님. 규칙 네 줄은 이미 `CLAUDE.md:42-45`; 팀원 세션용은 이 창 번들 `karpathy-guidelines/SKILL.md` 를 저장소 `.claude/skills/` 에 복사. 사용자 플러그인은 자리 세션에도 보인다(spritecook 9개가 지금 그 길로 보임). `.claude/`·`~/.claude/` 는 분류기가 막을 수 있음 — 막히면 우회 없이 그 명령을 대표께 Run 버튼으로 | 결정 번호가 서면 | 나래 · 레오(저장소 `.claude/` 커밋 감사) | `ls .claude/skills/` 에 speckit 열 + karpathy · `claude plugin list` 에 둘 · 자리 하나 리셋 뒤 transcript `skill_listing` 에 새 이름(지금 30개 → 늘어남) · 커밋 `-- .claude/skills .specify` | — |
| 2-2 | **한국어 낱말 규정집 + 비유로 못 쓰는 낱말 스물여섯**(대표 10:5x, 결정 216 — 세라 지적으로 추가). 하영: 웹에서 한국 회사가 보통 쓰는 낱말(노션·지라·슬랙·카카오워크 한국어 화면) 근거로 규정집 한 장 + 사전 0절에 스물여섯 표(문자 그대로 뜻일 때만 허용, 예문 하나씩) → 테라·솔라: 자(bosswords 검사)에 그 표 → 안젤: 방 말·보고 글에서 실제로 걸리는 줄 세기. 나래는 제 글부터 뺀다(참고물 검토 한 장 고침) | 결정 216 섰음(지금) | 하영 → 테라·솔라 → 안젤 · 제리 감사 | `opsroom-words.md` 0절 표 스물여섯 줄 · 규정집 파일 한 장 · 자 코드에 표 · 안젤 표본 한 장 | — |
| 3 | **기다리는 말씀 한 장** — 대표 새 말씀은 먼저 여기 앉고, 나리(서버)가 순서에 끼움. 대표가 "지금"이라 한 것만 바로. 낱말은 하영이 사전에 먼저(후보: 수신함·받은 편지함·인박스) | 대표 결정 ④ "진행"(09-18 10:3x 받음) + 사전 줄 | 하영(낱말) → 톰(파일 첫 판, 규칙.md 한 줄) → 나리 서버(끼우기) | `teams/hq/<사전 낱말>.md` 존재 · `규칙.md` 한 줄 · 대표 말풍선이 오면 그 자리에서 톰이 한 줄 옮긴 기록 | wayfinder "Not yet specified" |
| 4 | **헌법 한 장** `teams/hq/헌법.md` — 원칙(CLAUDE.md 지킬 것 여덟을 그대로 가리킴) + 문 넷(세계관 `world-bible/10-world.md` · 목차·색인 `library/toc.md` + 새 `색인.md`(대표 폴더 여섯·카테고리 순으로, 없는 칸은 "아직 없음") · 업무규칙 `규칙.md` · 책장 `shelf.md` 여섯 + 사서 `tools/library.mjs find`) + 개정 규칙 한 줄(바뀌면 결정 번호). 09-16 진단 고치는 법 다섯을 규칙.md 에 번호 줄로. 인격 열여섯 장의 규칙 줄 ↔ 규칙.md 어긋남 대조(유진·노라 잣대) | 3 끝 | 톰(헌법·규칙 3판) · 하영(색인 낱말) · 유진(색인 manifest 다시 빌드 · 인격↔규칙 대조) · 세라 검수 · 나래(md→html 변환, `/out/hq/헌법.html` 대표 폰) | `teams/hq/헌법.md` · `teams/hq/out/색인.md` · `규칙.md` 3판 · `library/manifest.json` builtAt 갱신 · 대조표 한 장 | spec-kit constitution 모양(원칙 + 문지기 조건 + 개정 규칙) — triptude `.specify/memory/constitution.md` 는 19KB 라 모양만, 우리는 한 장 |
| 5 | **세션 시작 규칙이 실제로 세션에 박히게** — (a) `server/session.mjs briefOf` 에 그 자리 `order.md` '진행 중' 줄 + 헌법 경로 한 줄(12,000자 안, 길이 로그로 한 번 잼) (b) 자리 세션에서 `tools/library.mjs find` 가 도는지 실측 → 책장 낡은 두 줄 고침 (c) 종료 상태 — 대표가 "오늘은 여기까지" 하면 회차 자동 재개·멈춤 감시가 멈추는 상태 파일(시계 아님) (d) 훅에 Read `offset/limit`·`session_id` 남기기 + `.claude/settings.json` PostToolUse matcher 에 `Skill` — 솔라가 패치 스크립트 한 개, 나래가 돌림(2-1 과 같은 잣대: 분류기가 막으면 대표께 Run 버튼) (e) 회차 조건에 스킬 박기: 커밋 전 code-review, 시안 검토 web-design-guidelines, 계획 쓸 때 writing-plans(2-1 뒤에 보이면) | 4 끝 | 솔라(a·b·c·d 스크립트) · 레오 감사 · 나래(스크립트 실행, 재시작) · 대표(막히면 Run) | 리셋 뒤 첫 턴 tool 사건 Read ≤ 3 (`teams/<팀>/log.jsonl`) · tool 사건에 `meta.offset` 보임 · Skill 사건 ≥ 1 · 종료 상태 뒤 `stallPatrol` note 0 | karpathy 4(검사 가능한 목표) · triptude `harness.config.json requiredReads`(읽어야 할 파일을 해시로 남김) 모양 — 우리는 로그 tool 사건이 그 증거 |
| 6 | **일마다 책임자·시작·끝·결과물** — `order.md` 다섯 칸(일·결과물·책임자·담당·상태, goal.md 3절 "네 칸" 변경 = 대표 결정 ④) + 줄에 work id · `state/work.json` `startedAt`('진행'으로 바뀔 때)·`doneAt`(`bus/bus.mjs:1939 advanceWorkOnPass` 실제로 찍히게)·`outputs[]`·`owner`(책임자) · `doneCheck` 없는 줄은 못 닫음(톰이 먼저 76건 경로 채운 뒤 강제) · 한 사람 '진행 중' 1줄(goal.md:20 그대로) | 대표 결정 ④ + 5 끝 | 톰(order.md 틀·doneCheck 채우기·책임자 후보) · 팀장 다섯(자기 order.md) · 솔라(work.json 필드·닫힘 검사) · 나리 서버(책임자 확정·카드) · 빅터·레오·마크 감사 | `order.md` 여섯이 다섯 칸 + work id · `grep -c doneAt state/work.json` > 0 (통과 한 건 뒤) · '진행' 항목 doneCheck 100% · 열린 항목 한 사람 최대: 테라 19·솔라 15 → 순서 목록 '다음' 2 + '예정' 나머지 | superpowers writing-plans(맥락 0 인 사람이 읽어도 되는 카드) · spec-kit tasks 모양 |
| 7 | **대시보드 데이터** — `state/usage.jsonl` 줄에 `workId` · `log.jsonl` 사건에 `workId` · `bottleneck` 을 사람·카드 id 로 · 차례 사건(giveTurn) 로그에 · `/api/analysis repeats` 를 자리·회차·파일로 · `app.js` 가 `/api/timeline` 부름(`teams/dev/order.md:14` 테라 '다음' 줄 그대로) | 6 끝 | 솔라(서버·버스) · 테라(화면 연결) · 유진(지표 정의 = 아침 한 장과 같은지) · 레오 감사 | usage 새 줄에 workId · `/api/analysis?all=1` repeats 가 파일·회차별 · `app.js` grep `/api/timeline` ≥ 1 | — |
| 8 | **대시보드 화면 첫 판 = 그래프 보기 첫 판**(대표 11:1x 정의 "업무 관리를 마인드맵·지식그래프·바둑판 돌처럼 유기적으로 연결해 어디가 꼬였나·전체가 어떻게 연결됐나 본다", 제안 `teams/hq/out/그래프-보기-제안-0918.md` 2판, 대표 답 "계획대로 뒤에") — 대표 결정 ①의 셋(사람·막힘·반복 읽기)만 + 적대검수 K1~K10 남은 것 · 잣대 "GA보다 쉽게" = 큰 숫자 셋까지(헨리 제안 30행)·숫자마다 비교·12살 30초(노라) · **QA 는 외부 서비스(슬랙·노션·카카오톡·GA) 실제 창을 크롬에 띄워 놓고 나란히 비교, 담당자가 고치고 다시 잰다(대표 11:13 "자기 업무 결과물에 대한 책임")** | 7 끝 + 대표 결정 ① | 헨리 시안 → 테라 → 클레멘타인 CSS → 안젤 사용성 → 마크 → 나리 서버 판정 → 나래 사진 셋 → 세라 대표께 | `teams/dev/out/shots/` 412·750·1280 · K1~K10 통과 · 적대검수 재판정 | web-design-guidelines(시안) · code-review(커밋 전) · dataviz(그래프) |
| 9 | 주제 스레드 화면(요청 블록을 카드 id 로 묶어 보이기, '#' 새 채널 안 만듦 — 방은 팀당 하나) · 채팅 기능 여덟(apr_cee2d254, 대표 C) | 8 끝 | 테라·헨리·하영('스레드' 슬랙 표준어 확인)·안젤 | 요청 탭 사진 셋 · req open 줄에 workId | — |
| 10 | PC 디자인(레딧/슬랙/노션)·모바일(카카오톡보다 편하게)·세라 앱 · 대표께 여쭐 것 넷(폰 탭 아래로·첫 화면 채팅·어두운 화면·큰 숫자) 는 8 시안 때 단추 | 9 끝 | 헨리·클레멘타인·테라·안젤·마크 | 사진 셋 · roadmap 11·12단계 통과 | web-design-guidelines · 참고물은 제안으로 |
| 11 | 폴더·CS — 실제 폴더 이름은 안 바꿈(하드코딩 220곳+), 색인(4)이 대표 여섯 묶음으로 보임. CS 는 안젤 자리(사용성 검수) + 기다리는 말씀으로 시작, 새 방은 8 뒤 공백이 보이면 여쭘(개발 컷 리스트 줄 그대로) | 대표 결정 ② | 톰·안젤·유진 | `state/teams.json` 변화 없음 · 색인에 CS 칸 "안젤 자리" | — |

### 09-16 진단 넷 ↔ 고치는 단계
순서 목록 없음 → 4·5·6 / 새 말씀이 앞에 끼어듦 → 3 / 한 사람 여러 일 → 6·8 / 판정 없이 닫힘 → 4(명문)·6(doneCheck 강제)·5(c)(종료 상태).

### 대표 불만 둘 ↔ 고치는 단계
"룰 안 따름(최소 md·어제·나는 누구·사서)" → 5(a)(b) (어제 = 일지 5문단·나는 누구 = 인격 은 이미 자동, 빠진 건 순서 줄과 사서 실측) / "중간에 멈추고 시스템 안 따름(팀·개인 규칙이 통합 안 됨)" → 4(인격 16장 ↔ 규칙.md 대조)·5(c).

### 대표 폴더·카테고리 ↔ 있음/없음 (색인 4 에 이 그대로)
- 헌법: 세계관 있음 · 목차·색인 일부(세계관 README 없음, `00-audit.md` 가 대신) · 업무규칙 있음 · 책장 있음.
- 경영(finance): 팀별 역할(`cast.json` title) 있음 · 작업 보고(아침 한 장·progress.json) 있음 · 작업 계획(roadmap·order.md) 있음.
- 개발(dev): 프론트(`server/src/app.js`)·서버(`server/`)·알고리즘(`bus/`) 있음 · 백엔드·DB·웹앱·앱·웹서비스 없음(배포·Supabase 는 컷).
- 마케팅: 브랜드 슬로건(goal.md 1절) 있음 · 카피(사전·plain-words) 일부 · 홍보 계획·출시 계획·SNS·PR·기사·오픈챗 없음.
- 디자인: UI/UX(시안·screens) 있음 · 색상 시스템(style.css :root, 흰 바탕 정본) 있음 · 브랜딩·아이콘·로고 없음 · 아키텍처 없음.
- CS: 사용성 QA(안젤)·디자인 QA(마크) 일부 · 플랫폼 QA·고객 문의 없음.

### 스킬 넷 — 배운 것 → 우리에 맞춘 것 → 여쭙는 것
- Karpathy: 가정 꺼내기·단순하게·손댄 곳만·검사 가능한 목표 → 이미 `CLAUDE.md:42-45`. 덧붙일 것 = "됐다" = doneCheck 파일(6). 여쭙기 없음.
- Superpowers: brainstorm→plan→execute→verify, 증거 먼저 → 우리 `/kickoff`·`roadmap.json`+`order.md`·`/round`·doneCheck+`/verdict` 와 같은 뼈대. 지금은 자리 세션에 안 보임 → 2-1 에서 사용자 플러그인으로 설치(대표 "직접 가능한데?"). 회차에 박는 자리: 계획 쓸 때 writing-plans, "됐다" 전 verification-before-completion, 커밋 전 requesting-code-review. `subagent-driven-development` 는 브리프 "Agent 툴로 동료 부르지 마라"(`session.mjs:253`)와 충돌 — 안 씀.
- Wayfinder: 큰 일을 "안 정한 것" 지도로, 하나씩 정하되 실행은 안 함 → 3(기다리는 말씀) + goal.md + decisions.md + cutList. 원본 플러그인은 2-1 에서 설치(설치 줄은 이 창 SKILL.md 인용). 원문 인용: "Keep a single running map document … Destination; Notes; Decisions so far; Not yet specified; Out of scope"(이 창 wayfinder SKILL.md 3항).
- Spec-kit: constitution→specify→clarify→plan→tasks→implement→converge → 4(헌법 = `.specify/memory/constitution.md` 자리에 우리 헌법 한 장을 두거나 가리킴)·`/kickoff`·6(order.md+work.json). 다른 자리가 판정하는 `/verdict` 는 spec-kit 에 없다(우리가 더 셈). 설치는 2-1(나래 직접, 막히면 대표). 근거: `specify init --help` 예시 줄 "specify init --here --integration claude", triptude `.claude/skills/speckit-*` 열 개 실물.

---

## 나래 개명 — 파일 목록 (2단계, 이 순서로)

원칙(09-18 10:58 대표 "나래와 나리는 분리된거야. 두명의 각자 다른 사람이야" 뒤 고침): 나래는 나리와 **다른 사람**이다 — 프로필 카드 따로(대표 10:57), 인격 열 줄 따로(하영·안젤, 재료 없는 칸은 비움), 사전 직책 "시스템 관리자"(하영 10:56). 기록의 화자를 새 사람으로 가를지(새 화자 id vs 지금처럼 `system`+손 표시 `cli`)는 개발이 정해 제안하고 레오가 감사한다 — 새 화자면 자리 파일 여섯·`say.mjs` SAYABLE·호명·결재·서버가 자리를 새로 띄우는 문제까지 걸친다. `cast.system.name`(나리)은 어느 쪽이든 안 바꾼다(호명·프롬프트·멘션이 이 값). 나래는 카드 판정(`approve --decide`)을 안 한다.

| # | 파일:줄 | 지금 | 어떻게 | 누가 |
|---|---|---|---|---|
| 1 | `teams/marketing/out/opsroom-words.md:113-118` 직책 표 | 나리 = 사장 | 한 줄 더: 나래 — 관리 창(재시작·사진·설정·설치·올리기·지우기), 파란 이름. `:240` 자동 안내는 그대로 | 하영 |
| 2 | `~/.claude/projects/-Users-donghyukham-Documents-ppanam/memory/i-am-nari.md` "두 손" 절 · `two-hands-of-nari.md` · `MEMORY.md` 줄 | 나리(시스템) | 관리 창 = 나래. 새 세션이 첫 파일로 읽는 곳 — 개명 첫 항목 | 나래 |
| 3 | `teams/hq/cast.json:39-47` system + `teams/{sera,dev,design,marketing,finance}/cast.json` system | name/initial/color/title/does/model/titleWas | 새 키 `"cli": {"name":"나래","initial":"래"}` 여섯 파일 다(`who()` `app.js:53` 가 그 방 cast 를 먼저 읽음) | 테라 |
| 4 | `server/src/app.js:47` · `:664-684` · `:668` · `:672` 주석 · (선택) `:145` | `lastActor` · `a.name/a.initial/${a.name} 카드` · `cont = lastActor===e.actor && !called` | `lastHand` 추가 · `shown = (e.actor==='system' && e.meta?.hand==='cli' && a.cli) ? {...a,...a.cli} : a` 로 세 자리 · `cont` 에 `lastHand === (e.meta?.hand ?? null)` (나리 뒤 나래가 이름 없이 붙는 것 막음 — 화면 cont 논리로 세면 46쌍) · `@나래` 칩(선택) · `npm run build:public` | 테라 |
| 5 | `server/public/style.css:410` 주석 · `docs/event-schema.md:83`(cast 필드)·`:175`(hand) | "관리 창 나리" | '나래' + `cast.system.cli` 계약 | 테라 |
| 6 | `bus/say.mjs:96-100` | `--as` 무엇이든 `hand:'cli'` | `process.env.PPANAM_ACTOR==='system' ? 'server' : 'cli'` — 코드상 서버 나리가 Bash 에서 치면 파랑으로 보일 수 있음(로그엔 0건, 09-16 04:51Z 시도는 방 검사에 거부됨) | 솔라 |
| 7 | `teams/hq/system.md:13,20` | "나리(시스템)가 켠다" | '나래' — 서버 나리 프롬프트 인격(`session.mjs:137-144`) | 톰(제리 감사) |
| 8 | `teams/hq/out/규칙.md:40` · `권한.md:13` · `goal.md:29` · `따라가기.md:67,98` · `배정순서-1015.md` · `state/work.json:884` | 나리(시스템)/관리 창 나리 | '나래'. `:892` 대표 원문 인용은 그대로 | 톰 |
| 9 | `teams/dev/decisions.md` | 마지막 207 | 새 번호 한 줄 "관리 창 나리(시스템) → 나래. 화자 id·호명·판정은 그대로, 파란 이름만" | 톰 |
| 10 | `teams/hq/out/나리시스템-인수인계-0916-밤.md` · `나리-인수인계-0916-저녁.md` · `스킬-검토-0916.md` | 본문 | 머리 한 줄 "이 글의 '나리(시스템)' 은 09-18 부터 '나래'". 본문·남의 기록(`현황.md`·`proxy-decisions.md`·`인수인계-0916-나리.md`(서버 나리 것))은 안 고침 | 나래 |
| 11 | 이 세션 제목 | — | `set_session_title` "나래 — 관리 창" | 나래 |

안 건드리는 것: cast `name` · 훅 `hand:'server'`(`.claude/hooks/to-bus.mjs:202,218`) · `server/conductor.mjs:324` · `app.js:2151` 서버 나리 카드 노랑 · 판정 이름(`bus/bus.mjs:716`·`bus/nightly.mjs:18`·`server/nightly.mjs:207`·`notify.js:22`·`card.js:38`·`notifier.mjs:78`) · `bus/approve.mjs:235-238` · 09-16 02:30Z 이전 hand 없는 902건 · 초상.

확인(증거): `grep -c cliName teams/*/cast.json` 여섯 다 1(키 이름은 테라 커밋 기준) → `cmp src public` 같음 → 서버 재시작 불필요(`server/index.mjs:158 summaries()` 가 매 요청 `readCast`) → 총괄실에 나래 한 줄(`node bus/say.mjs --team hq --as system "톰, …"`) 파란 '나래'·머리글자 '래' → 톰이 "나리," 로 서버 나리 부름 → 노란 '나리' → 바로 뒤 나래 한 줄에 이름 줄 있음 → 비서실·개발 방에도 한 줄씩 → `node tools/screen-shot.mjs teams/dev/out/shots/r36-narae-hq-{412,750,1280}.png hq/room W H` → `teams/hq/log.jsonl` 마지막 줄 `meta.hand:'cli'` → 헨리 "같다" → 레오(코드)·제리(문서) 판정 → 나리 서버 대리 결정 → 세라 대표께 한 줄.

---

## 대표 결정 (버튼 넷 — 답은 글자 그대로 총괄실에 먼저, 1단계 말풍선에 같이)
① 대시보드 첫 판 셋 ② 폴더(색인으로 보이기 / 실제 이름 바꾸기 / 구글 드라이브) ③ 스킬 설치 ④ 기다리는 말씀 + 책임자 칸(goal.md 네 칸 → 다섯 칸). 답은 맨 아래 "대표 답" 절.

## 안 하는 것
폴더 이름 바꾸기 · 나래 새 actor id·판정·호명(카드는 대표 10:57 지시로 만든다) · 옛 기록 소급 · '#' 새 채널 · 노션 동기화·앞일 날짜·손으로 옮기는 칸·마을·유니티(타임라인 계획 6절 컷) · 달러 표시 · 대시보드 여덟 한 번에 · 시각으로 정한 규칙("하루 두 번"·"21시 문") · 대표 글에 번호·경로·회차 · 채팅 여덟을 순서 앞으로.

## 위험
여섯 cast 중 하나 빠짐(검사 grep cliName = 6) · say.mjs 안 고치면 서버 나리 Bash 말이 파랑 · cont 안 고치면 나리 뒤 나래가 이름 없이 붙음 · 결재 카드·멘션 읽음은 두 손을 못 가름(나래는 판정 안 함으로 막음) · 기다리는 말씀이 또 하나의 "적어 두는 곳"이 됨(대시보드 '정할 것' 띠에 수를 보임, 8) · doneCheck 강제 전 감사 대기 19건 못 닫힘(톰이 먼저 채움) · 프롬프트 12,000자 상한 · `.claude/` 밑은 대표 손(명령 건넴, 우회 안 함) · 사서 실측 전엔 "막혀 있다" 두 줄이 낡은 것인지 모름 · 한 사람 여러 일 — 이 계획도 테라·솔라·톰에 몰림 → order.md 로 한 줄씩, 개명(2)이 끝나기 전에 3~6 을 안 연다.

## 검증 (계획 전체)
- 2: 사진 셋 + 로그 마지막 줄 + grep/cmp (위 확인 절).
- 3: 대표 말풍선 뒤 톰 한 줄이 파일에 서는 것 — 실물 한 번.
- 4: `/out/hq/헌법.html` 을 대표 폰(412)으로 열어 사진, 색인의 "아직 없음" 칸이 위 표와 같음.
- 5: 자리 하나 리셋 → 첫 턴 tool 사건 Read ≤ 3 · `meta.offset` 보임 · 종료 상태 뒤 stallPatrol 0 · `tools/library.mjs find` 자리 세션 실행 로그 1건.
- 6: 통과 한 건 뒤 `doneAt` 실물 · order.md 여섯 다섯 칸 · `/api/timeline` tasks 에 owner.
- 7~8: `/api/analysis repeats` 실물 · 사진 셋 · 적대검수 재판정 문서.

## 대표 답 (09-18 10:3x, 관리 창 버튼 — 글자 그대로)
- 대시보드 첫 판 셋: "사람·막힘·반복 읽기 (Recommended)" → 8단계는 ④ 사람마다 지금 무엇·살아 있음 · ⑤ 일마다 담당·막힘 · ③ 같은 문서 반복 읽기 수 셋.
- 폴더 여섯: "목차 한 장으로 여섯 묶음 (Recommended)" → 4단계 `색인.md`, 실제 폴더 이름 그대로(11단계 확정).
- 스킬 넷: "https://github.com/obra/superpowers / https://github.com/karpathy Wayfinder (Matt Pocock: https://github.com/mattpocock/skills) Speckit (https://github.com/github/spec-kit) 직접 가능한데 ?" → 2-1 단계: 나래가 직접 설치, 분류기가 막으면 명령을 대표께.
- 기다리는 말씀 + 책임자 칸: "진행 (Recommended)" → 3·6단계 열림(goal.md 네 칸 → 다섯 칸 승인).

## 대표께 드리는 첫 말 (실행 시작 때, 세 줄 안)
돌아왔습니다. 인수인계 읽었습니다. 서버부터 켜고 말씀을 총괄실에 올린 뒤, 나래로 이름을 바꿉니다.
