> 기록 문서 — 2026-09-16 에 쓴 것. 그 뒤 뒤집힌 결정이 있을 수 있다. **지금 맞는 것은 `teams/hq/out/현황.md`(살아 있는 한 장)와 `teams/dev/decisions.md`(번호 장부 — `teams/hq/out/proxy-decisions.md` 는 그 부록)** 다. 이 문서의 문장이 그 둘과 다르면 이 문서가 옛것이다.

# 코드 점검 한 장 — 서버 세 파일 (나리, 2026-09-16 13:4x, 대표 지시 13:3x "설계랑 코드 점검 검수 스킬사용해서 한번 돌려봐")

도구: 하네스 코드 리뷰(code-review, medium) — `server/conductor.mjs` · `bus/bus.mjs` · `server/session.mjs` 세 파일 통째(본책 대비 3,665줄 새 코드라 diff 가 아니라 전체). `server/index.mjs`(/api/say)·`state/teams.json`·`state/work.json` 과 교차. 아래 일곱은 도구가 확인한 것 — 마지막 하나만 "그럴 법함".

무거운 순. 고치는 손은 전부 솔라(서버). 판정은 레오(외부)·테라(내부).

| # | 어디 | 무엇이 잘못되나 | 대표 눈엔 | 
| --- | --- | --- | --- |
| 1 | `server/conductor.mjs:871` | 대표님이 총괄실 밖 방에서 "나리, …"·"세라, …" 라 부르면 아무 데도 안 간다 — /api/say 가 그 방 주인 자리로 보내는데 그 자리 model 이 null 이고, 사회자는 k===0 이면 callHomeElsewhere 를 건너뛴다 | 디자인 방에서 나리를 불러도 답이 없음 |
| 2 | `server/conductor.mjs:600` | 멈춤 감시 ㉡이 roadmap 차례를 방이 쉴 때만 넣는데, dispatch→stash 가 쉬는 방에선 called·carried 아닌 차례를 다 버린다 — 주인은 못 받고 "버립니다" 노트만 남음 | 다음 단계 제안이 안 옴 |
| 3 | `server/session.mjs:507` | 방이 닫히는 중(closing)에 차례가 끝나면 turnEndListeners 를 안 불러 사회자 inflight 가 고아로 남아 저장됨 — 재시작하면 같은 차례가 두 번 가고 ㉣ "다시 띄웁니다" 가 또 감 | 재시작 뒤 같은 말이 두 번 |
| 4 | `server/conductor.mjs:511` | expireFlows 가 10분이면 판정 흐름을 끊는데 감사역이 아직 일하는지 안 본다 — 긴 클로드 감사 뒤 나온 PASS 카드가 안 세고, endRefusal 이 PASS 닫기를 막음 | 판정이 왔는데 회차가 안 닫힘 |
| 5 | `bus/bus.mjs:459` | requestApproval 이 emit() 반환값을 바로 쓰는데 비서실처럼 only:['message'] 방에선 null 이라 TypeError — 승인 요청이 기록 안 됨 | 비서실에서 카드를 올리면 사라짐 |
| 6 | `server/conductor.mjs:212` | unheard() 가 다른 회사 자리(codex)에게 한 대표 말을 주인에게서 건너뛰는데, /api/say 도 주인에게 안 줬으니 어느 클로드 세션도 못 듣는다 | 대표님이 다니엘·레오에게 한 말이 팀에 안 들림 |
| 7 (그럴 법함) | `server/session.mjs:622` | alive() 가 exitCode 로 보는데 그건 'close' 전에 먼저 서서, 그 틈에 enqueue 가 오면 세션이 바꿔치기되고 옛 close 핸들러가 일찍 나가 deathPlan 재시도·settle 을 건너뜀 | 죽은 세션 차례가 조용히 사라짐 |

## 순서 (나리 안 — 세라 검수 전)

1·6 은 같은 뿌리(대표 말이 안 가는 것) — 먼저. 5 는 한 줄(null 검사) — 같이. 3·4·7 은 세션·흐름 쪽이라 D2 검사기 옆에 묶어서. 2 는 ㉡ 멈춤 감시(O1) 밑으로.

## 안 본 것

화면(`server/public/app.js`)·실행자(`executor.mjs`)·훅은 이번 범위 밖. 설계(문서) 점검은 도구가 코드만 보므로 따로 — 대표님 말씀 "설계랑" 은 O5 설계안·N1 감독 설계를 세라·제리가 글로 보는 것으로.

## 2판 — 합본 열둘 (나리(시스템), 13:5x)

둘째 도구 돌림: 같은 code-review 를 high 로, 오늘 06:00 기준(71314b5) 이후 diff 2,035줄(서버·버스·훅 일곱 파일). 후보 26 → 확인 21 · 그럴 법함 4 · 반박 2. 위 일곱과 겹친 것은 합쳤다(1판 2 = 아래 6, 1판 4 는 아래 5 묶음). 무거운 순. 손은 솔라, 판정 레오·테라. 지금 하는 C4·C14 뒤에 이 순서대로 하나씩.

| # | 어디 | 무엇이 잘못되나 | 대표 눈엔 | 판정 |
| --- | --- | --- | --- | --- |
| 1 | `bus/bus.mjs:1822` verdictTargetActor | 판정 대상을 자유 글 첫 이름으로 찍는다 — 감사역 이름이 앞에 오면 그 자리 work 항목이 통과로 넘어가고 뒷자리가 깨진다 | 안 한 일이 "통과"로 뜸 | 확인 |
| 2 | `server/conductor.mjs:871`·`:212` (1판 1·6) | 총괄실 밖에서 "나리,"·"세라," — /api/say 가 주인 자리로 보내는데 model null 이라 아무 데도 안 감. 다니엘·레오에게 한 대표 말도 클로드 세션이 못 들음 | 디자인 방에서 불러도 답 없음 | 확인 |
| 3 | `server/notifier.mjs:106` | 늦게 결재된 옛 착수 B 가 setMilestoneStatus(now) 를 조건 없이 불러 지나간 단계를 now 로, 현재 단계를 wait 로 되돌림 | 회차가 엉뚱한 단계에 섬 | 확인 |
| 4 | `bus/bus.mjs:137` passShapeError | '반박'을 이유와 같은 줄에서만 센다 — 다음 줄에 쓰거나 '반론'이라 쓰면 PASS 가 REVISE 로, 두 번째면 흐름 abort | 통과인데 반려로 기록 | 확인 |
| 5 | `server/conductor.mjs:511`·`:678`, `server/session.mjs:507`·`:622` (1판 3·4·7 묶음) | expireFlows 가 감사역이 일하는 중에도 10분이면 끊고, 끊을 때 inflight 를 안 지워 ㉣ 이 빈 대상 판정 요청을 다시 낸다(훅은 'guide' 로 오기록). closing 중 turnEnd 안 불러 재시작 뒤 같은 차례 둘. alive() 가 exitCode 로 봐서 close 전 틈에 세션 바꿔치기 | 판정 왔는데 안 닫힘 · 같은 말 두 번 | 확인(alive 는 그럴 법함) |
| 6 | `server/conductor.mjs:600` (1판 2) | ㉡ 이 idle 방에 roadmap 차례를 넣지만 dispatch→stash→pickCarry 가 버림 | 다음 단계 제안이 안 옴 | 확인 |
| 7 | `bus/bus.mjs:459` requestApproval (1판 5) | emit() 반환이 비서실(only:['message'])에선 null → TypeError, 카드 기록 안 됨 | 비서실 카드가 사라짐 | 확인 |
| 8 | `server/usage.mjs:58` withCostDeltas | --resume 뒤 누적이 옛 최고값을 넘을 때까지 delta 0 | "오늘 쓴 것"이 작게 나옴 | 확인 |
| 9 | `bus/bus.mjs:1657` | 계획표 다 끝난 뒤 '대기' 회차를 PASS 로 닫으면 setMilestoneStatus 가 true 를 돌려 마일스톤 통과 이벤트·청소·대기 회차 열기가 또 돔 | "N 통과"가 두 번 | 확인 |
| 10 | `server/conductor.mjs:574` doneCheckOf | 경로 경계 검사 없이 statSync, 실패는 catch 로 "조용히 기다림" — 오타 하나면 닫기 알림이 영영 안 옴. 저장소 밖 파일도 완료 조건 가능 | 회차 닫기 재촉이 안 옴 | 확인 |
| 11 | `server/session.mjs:743` reset | 브리프(라운드 번호·진행)는 spawn 때만 실려 한 단계 안 여러 회차 동안 옛 번호를 붙듦 | 지난 할 일을 붙들고 답함 | 확인 |
| 12 | `bus/bus.mjs:1732` advanceWorkOnPass | work.json 읽고-고쳐-쓰기가 tmp+rename 없이 — 두 방 PASS 훅이 겹치면 통과가 덮어써짐 | 통과가 '진행'으로 남음 | 그럴 법함 |

### 뭉치기 (솔라 손 기준)
- 뭉치 ㄱ 대표 말·판정 대상: 2 → 1 → 7. 셋 다 "말이 엉뚱한 데 가거나 안 감".
- 뭉치 ㄴ 회차·단계 상태: 3 → 9 → 6 → 11. 넷 다 setMilestoneStatus·roadmap 차례.
- 뭉치 ㄷ 흐름·세션 수명: 5 → 4 → 10 → 12 → 8.

### 설계 쪽(코드 밖, 세라·제리 글로)
- 'system' 특별 처리가 아홉 군데(peopleOf·crossPost·addressees·훅…) — 자리 하나 추가할 때마다 아홉 곳. N3 설계에 "system 도 cast 의 자리 하나" 를 넣을지.
- usage.mjs 가 parseJSONL·dayKeySeoul 을 다시 구현 — bus 것 쓰기(청소, 급하지 않음).
- checkStalls 가 250ms 마다 로그 전체를 읽고 todayUsage 가 팀마다 usage.jsonl 전체를 읽음 — 지금은 파일이 작아 안 보임. 하루 지나면 보임.
