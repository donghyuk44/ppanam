# 합치기 전 정리 — 추적 안 되는 파일 81개 (나리, 2026-09-16 11:4x, 세라 검수 ⑤)

`git status --porcelain | grep '^??'` 81줄(폴더는 한 줄로 접힘. 세라가 센 11:3x 엔 88 — 그 사이 솔라·테라가 7개 커밋). 넣음 42 · 넣지 않음 27 · 버림 12 = 81, 패턴으로 셈. 결정 164(대표 11:3x "메인병합도 나리가 판단하에 처리해라 저녁 6시까지는 전체 승인") 아래 나리 대리. 세라 통과 뒤 실행.

규칙 — .gitignore 에 이미 있는 것 그대로: 산출물(`teams/*/out/`)은 넣고, 사진 폴더와 100KB 넘는 그림은 뺀다(`tools/out-ignore.mjs`, 11:4x 다시 돌려 151개). 새로 세우는 규칙 하나: **`teams/*/in/`(대표가 주신 원본)은 넣지 않는다** — 12MB PDF·그림, 이 PC 에 두고 산출물이 경로로 가리킬 땐 "이 PC 에만" 한 마디를 붙인다(세라 조건). ignore 에 더 넣는 살아 있는 상태: `state/infra.json` · `state/nightly.json`.

## 넣음 (커밋) — 42

| 무엇 | 왜 |
| --- | --- |
| `state/budget.json` · `delegation.json` · `outside-cooldown.json` · `state/idle-chat/` · `state/requests/` | 서버가 읽는 상태 — 없으면 새 PC 에서 안 돈다 (sessions·approvals 는 이미 ignore). `infra.json`(server/infra.mjs 91행이 6분마다 덮어씀)·`nightly.json`(server/nightly.mjs 24행)은 서버가 쓰는 살아 있는 파일이라 **ignore 로** — 세라 검수 하나 |
| `AGENTS.md` · `.codex/agents/` | 외부감사(codex) 인격·지시 — 다니엘·제리가 이걸로 뜬다 |
| `teams/hq/shelf.md` · `teams/design/shelf.md` · `teams/marketing/shelf.md` | 책장(결정 116) |
| `teams/design/out/triptude-map.md` · `timeline.svg` · `png/cards.png` · `png/workboard.png` · `fix-0916/` · `review/report2-1판-헨리.md` · `gpt/` · `gpt64/` · `nightly/` | 디자인 산출물 (100KB 넘는 그림은 ignore 블록이 뺀다) |
| `teams/marketing/out/boss-lines-2.md` · `branding-check.md` · `screen-words-l1-pass.md` · `usability-0916-R36.md` · `R37.md` · `world-bible/40-persona/117-check.md` · `nightly/` · `shots/`(592K, 큰 것은 블록이 뺌) | 마케팅 산출물 |
| `teams/finance/out/library/` · `nightly/` · `teams/dev/out/nightly/` · `teams/sera/out/` | 산출물 |
| `teams/dev/out/_an-block.mjs` · `_app-check.mjs` · `_app-next.mjs` · `_lineup-check.mjs` · `_usage-smoke.mjs` · `_work-board-check.mjs` · `teams/design/out/_req-count.mjs` · `_svg-check.mjs` | 검사 도구 — 커밋 글이 이 이름으로 근거를 댄다(`_n1-check`·`_prune-check` 와 같은 꼴). 8개 |
| `tools/portrait-sheet.mjs` | 초상 시트 도구 — tmp 아님 |

## 넣지 않음, 디스크에 둠 (ignore 줄 추가) — 27

| 무엇 | 왜 |
| --- | --- |
| `teams/design/in/2026-09-16-triptude-city-compare.pdf`(10MB) · `teams/design/in/triptude/*.jpg` · `teams/sera/in/`(664K) | 대표 원본. `.gitignore` 에 `teams/*/in/` 한 줄 |
| `unity/ppanam-shot/ProjectSettings/*.asset`(21) · `Assets/*.meta`(3) · `Packages/packages-lock.json` — 25 | 유니티가 만든 것, 순서 9 "안 함"(대표 다시 부르실 때). `.gitignore` 에 `unity/ppanam-shot/ProjectSettings/` · `unity/**/*.meta` · `unity/**/packages-lock.json`. `ProjectVersion.txt` 는 이미 추적 중이라 그대로 |

## 버림 (지움) — 14 (12 + 세라 둘)

| 무엇 | 왜 |
| --- | --- |
| `.claude/hooks/to-bus.mjs.bak` · `.claude/settings.json.bak` · `sera-app/main.js.bak` · `state/outside-sessions.json.bak-test` · `teams/hq/round.json.bak` | 손으로 만든 백업, 원본이 git 에 있다. 5 |
| `.claude/hooks/to-bus.mjs.bak-0916` · `.claude/settings.json.bak-0916` | 오늘 훅·설정 패치 백업(162). 재시작 11:26 뒤 정상 확인했으니 지운다. 2 |
| `tmp-morning-test.mjs` · `tmp-svg-text.mjs` · `tools/tmp-boss-lines-count.mjs` · `tools/tmp-check-finance-color.mjs` | tmp 접두 — 한 번 쓴 것. 4 (`tools/tmp-boss-lines-count.mjs` 는 하영 자(boss-words-check)와 다른 파일인지 하영 확인 뒤) |
| `tools/_work-board-check.mjs` | `teams/dev/out/_work-board-check.mjs` 와 diff — 다른 건 2행 import 경로 하나(`./work-board.mjs` ↔ `../../../tools/work-board.mjs`), 나머지 31줄 같음. 검사 도구는 `teams/dev/out/_*.mjs` 에 두는 게 관례(`_n1-check`·`_prune-check`)라 **dev/out 쪽이 정본**, tools/ 쪽(10:32, 11초 뒤 복사본)을 지운다. 1 |
| `tools/patch-hook-hand-0916.mjs`(추적됨, e7675f2) | 훅 패치 한 번 쓴 것 — 162 로 끝남. `git rm`. 1 |
| `teams/sera/in/2026-09-16T00-44-47-438Z-6e1769.png` | 대표 개인 메모가 찍힌 카톡 화면 — 모양 참고 끝. ignore 와 별개로 디스크에서 지움. 1 |

## 순서

① 세라 통과 → ② `.gitignore` 줄 셋 추가 + 버림 12 지움 → ③ `git add -A` 뒤 `git status` 로 "넣음" 목록과 한 줄씩 대조 → ④ 커밋 한 번(글: 이 파일 경로) → ⑤ 테라 푸시 카드(163) → ⑥ 세라 잣대 ①~④ → ⑦ 합치기(164, 18시 전).
