# 경영팀 인수인계 — 2026-09-14 (유진·노라 → 09-15 밤 9시의 우리)

30초짜리다. 기억은 안 넘어오고 파일만 넘어온다. 상황판(`node bus/progress.mjs --team finance --show`)이 이 문서보다 새 것이면 그쪽을 믿어라.

## 1. 지금 어디
- 라운드 2 · **M1 "서고와 사서"** — 노라 PASS(09-14 03:44Z). **빅터(gemini) 판정 대기** — 빅터가 PASS 하면 서버가 닫는다. 빅터 답이 없으면 결정 85(10분 대리)로 톰·제리에게 청한다.
- 낸 것: `tools/library.mjs`(사서 스크립트 3판) · `teams/finance/out/library/`(toc.md 9,458바이트 · manifest.json · index/ · 실측/2026-09-14-3판.txt) · `teams/finance/out/사서-물음-다섯.md`(물음 다섯 + 팀 확인 + find 대조표 + 사서 규칙 10줄) · `teams/finance/roadmap.json`(M1 하나, 컷리스트 넷).
- 결정 125(둘 다 일한다): 유진 = 사서, 노라 = **보고서 틀**(대표 새 일: 레퍼런스 → 비교 → 샘플 → 검수, 도표·그래프·12살 워딩·매뉴얼). 감사 맞바꿈(노라→사서, 유진→보고서 틀), 빅터는 둘 다. 비용 장부는 그 뒤.

## 2. 바로 다음
1. (유진) 빅터 판정 확인 → 닫히면 `git add tools/library.mjs teams/finance/roadmap.json` 커밋(out/ 은 gitignore). `bus/library.mjs`(옛 사본, 나리가 옆으로 옮겼거나 남아 있음)는 지운다.
2. (노라) 보고서 틀을 M2 로 B 요청: `node bus/approve.mjs --request B "…" --next` 는 로드맵에 M2 가 있어야 하니 **먼저 `teams/finance/roadmap.json` milestones 에 M2 를 적고**(네 걸음을 하나로 갈지 넷으로 쪼갤지 노라가 정함) `--roadmap` 또는 `--next` 로 올린다. "언제 것인지·그 뒤 바뀐 것" 칸을 틀에 넣을지도 같이.
3. (유진) 자리들이 사서를 실제로 쓰게 하기 — 두 가지: ① 세션이 `node tools/library.mjs` 를 돌릴 수 있게(지금은 허용 목록 밖 — `.claude/settings.json` 은 대표만 고친다, `Bash(node tools/:*)` 가 있는데도 막혔으니 왜인지부터) ② 프롬프트에 목차 한 장(9KB)을 끼우는 자리 — 개발팀 일이라 요청 블록(`approve.mjs --request B --to dev:guide`)으로. 컷리스트: 서버 코드는 우리가 안 만진다.
4. (유진) 사서 4판 후보(급하지 않음): `find` 에서 자기 파일(`사서-*`)·일지를 뒤로 미루기 · 목차 여유(9,458/10,000) · 빈 파일 둘(`teams/design/out/colormap-uv.md` 0B → 헨리, `teams/hq/journal/outside.md` 0B → 제리)은 남의 몫.

## 3. 막힌 것
- **세션이 `node tools/library.mjs` 를 못 돌린다.** build·check·find 는 전부 나리(하네스)가 대신 돌렸다. 실측은 `sh teams/finance/out/사서-실측-명령.sh`(출력이 `out/library/실측/` 에 쌓임 — 서고 밖). 나리 손.
- `rm teams/finance/out/사서-3판-실측결과.txt` — 191바이트 안내 한 줄만 남은 파일, 지워도 됨(세션은 rm 못 함). 나리 손.

## 4. 내일의 나에게 — 오늘 헛디딘 자리
1. **답을 손으로 옮기지 마라.** 색인 파일을 손으로 읽어 답을 쓰면 갱신을 건너뛰어 낡은 답이 나간다(4번 decisions.md 17줄·2번 event-schema 새 절 넷). `find`/`show` 는 답하기 전에 갱신을 먼저 돈다 — 그 출력으로 답하라.
2. **접힌 줄(`*.json N장`)도 훑어라.** 목차만 눈으로 훑다 colormap-roles.json·manifest.json 을 놓쳤다. `find` 는 접힌 것도 색인을 다 연다.
3. **"묻는 사람이 쓴 것" 은 안 읽을 이유가 아니다.** 하영의 1절 말 사전을 "이미 하영 것" 이라 뺐다가 틀렸다 — 규칙 본체는 쓴 사람도 열어 놓고 쓴다. 안 읽을 것의 이유는 내용이어야 한다.
4. **절 이름이 먼저, 줄 번호는 뒤.** 줄은 그날 밀린다(하영 5줄·테라 위에 절 넣음). 남에게 전할 땐 절 이름으로.
5. **그림은 서고 밖이었다** — 3판부터 이름·크기만 목차에. 판정에 그림이 든다는 걸 헨리가 짚었다.
6. **실측 출력을 `out/` 에 두지 마라** — 문서로 잡혀 find 를 오염시킨다(3판에서 209번째 문서가 됐다). `out/library/` 아래만 서고 밖이다.
7. **`Bash(node tools/:*)` 가 허용 목록에 있어도 막혔다.** 이유 모름. 새 CLI 를 bus/ 에 두든 tools/ 에 두든 같다 — 돌리는 건 나리 손이라고 처음부터 잡고 가라.
8. **찾은 것 둘은 기록됐다:** 번호 없는 대표 결정(gemini 임시 외부감사 → 톰이 결정 124 로 적음) · 계약에 없던 자동 착수(테라 fe51dae). 다시 잡지 마라.

## 5. 손대면 안 되는 것
- `tools/library.mjs` 는 3판 그대로 두고 판정을 받는다(INDEX_VERSION 3). 규칙을 또 바꾸면 208장을 다시 읽고 4판 실측이 또 필요하다 — 판정 뒤에.
- `teams/finance/out/사서-물음-다섯.md` 의 팀 확인 절(헨리·테라·하영·톰 답)은 인용이다. 고치지 않는다.
- `teams/finance/roadmap.json` 컷리스트 넷: 서버에 끼우기(개발) · 본문 손대기 · 제목 손으로 쓰기 · 책장 밖 읽는 자리 재기(세라).
