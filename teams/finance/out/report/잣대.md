# 보고서 틀 — 잣대

글 노라(review) · 받아쓰기 유진(guide) — 방 기록 그대로, 토씨 안 바꿈. 출처는 각 절 머리의 기록 번호.
M2 ③ 통과 조건을 숫자로. 라운드 3, 2026-09-16(우리 시각). 절은 노라가 올리는 순서대로 쌓인다 — 아직 다 안 찼다.

---

## 잣대 재는 법 — 형식별 명령
(evt_9874648b30 · 2026-09-15T15:59:43Z)

유진 말대로 형식마다 명령을 못 박는다. 하네스 낱말 금지(apr_/req_/evt_ 0건) 재는 법:

- .md — 주석 문법이 없다. 파일 그대로 검사한다: `grep -n 'apr_\|req_\|evt_' <파일>.md` → 0줄이어야 통과.
- .html / .svg — `<!-- ... -->` 를 먼저 지우고 검사한다: `perl -0777 -pe 's/<!--.*?-->//gs' <파일> | grep -n 'apr_\|req_\|evt_'` → 0줄이어야 통과. (`-0777` 은 파일을 통째로 읽어서 여러 줄에 걸친 주석도 지운다 — tower.svg 2-4행처럼.)

tower.svg 로 확인: `<!--` 열고 닫는 줄이 14곳(grep '<!--\|-->' 결과), 블록 13개 중 req_ 가 들어간 건 2-4행 한 블록뿐이고 나머지 12블록엔 하네스 낱말 없음 — 지운 뒤 검사해도 결과는 그대로 0.
(첫 판 12곳/11곳은 노라가 거둠 — 다시 돌려 14/13/12 로, 유진 grep 과 일치. evt_752be5905a · 16:03Z)

## 문장 길이 상한 — 자동으로 못 재서 버림(시도 둘 다 기록)
(evt_864ef07379 · 2026-09-15T16:59:55Z)

두 번 시도했다. ① wc -L 로 파일 최대 줄 길이만 쟀더니 헤더·표·목록 줄까지 섞여(275·238) 산문 문장 길이가 아니었다 — 버림. ② node 스크립트로 html <p> 태그 안 본문만 뽑아 한국어 종결 어미(다./요./니다.) 기준으로 문장을 쪼개 재려 했는데 이 세션 승인 밖이라 못 돌렸다.
두 시도 다 "산문 문장만 골라 길이를 재는" 자동화된 방법을 못 만들었다 — 사람이 셀 수는 있어도 스크립트로 못 재면 M3 마다 같은 수가 안 나온다. 대표 규칙대로 이 잣대는 버린다.

## 숫자 종류 목록(초안)
(evt_c9d83b61f2 · 2026-09-15T17:03:50Z)

tower.svg 145행 규칙("숫자는 넷뿐 — 막힌 것 수·보실 것 수·단계 N/M·회차. 발언 수·대화 수·검토 결과 수는 없다")을 보고서에 맞게 옮긴다. 내 html 시안(202-206·184·224행)에 실제 쓴 숫자를 종류별로 나누면:
- 개수(건): "오늘 통과된 승인 8건"(204행) · "기다리는 승인 3건"(205행)
- 시간: "답 오는 시간 10초"(203행)
- 비율: "0/195장"(184행)
- 목록에 없어서 뺀 것: 발언 수·잡담 줄 수 같은 방 안 대화량 — 지금 시안엔 아예 안 나온다(224행 "마을 브랜딩 2판이 디자인으로 — 톰 통과 10초 뒤 제리 통과"의 10초도 시간 종류라 목록 안).

제안: 숫자 종류 다섯으로 못 박는다 — 개수(건)·비율(N/M)·시간(초·분)·용량(바이트/KB)·회차. 이 다섯 아니면 숫자를 안 쓴다.

(정정 evt_dd97aa8a20 · 17:07Z — 첫 판은 넷이었고 "보고서엔 회차가 안 나왔다" 고 적었으나 노라가 거둠: 시안 html 134·154·169·178·189행에 회차 다섯(개발 25회차 · 디자인 14회차 · 마케팅 31회차 · 경영 2회차 · 총괄실 1회차). 노라 말: "안 나왔다는 말은 grep 없이 넘겨짚은 거였다 — 시안이 실제로 쓰고 있으니 빼는 대신 회차를 다섯째 종류로 넣는다." 위 "224행" 은 223행.)

## 절 목록·순서(제안) — 거둠, 아래 '정정' 이 대신함
(evt_d8a6271863 · 2026-09-15T18:14:08Z — 톰·제리·유진이 같은 자리를 짚음: 보고서 정본은 tower 가 아니라 결정 80)

tower.svg 2행 주석 — 이미 톰·제리 B 통과받은 순서(나리 '화면이-답하는-질문.md'): ① 막힌 것 ② 누가 뭘 했나(오늘 된 것) ③ 대표님이 보실 것 ④ 팀. 일일보고서에 그대로 옮기되, 쓴 문서라 ③을 ②보다 앞에 둔다(대표님이 급한 것부터 보게) — 화면과 문서는 스크롤 유무가 달라서 순서를 하나 바꿨다:
① 막힌 것 ② 대표님이 보실 것 ③ 오늘 된 것 ④ 팀·다음. 넷 아니면 절을 안 만든다.

## 비교 기준 범위 확정 + 도표 규칙
(evt_b0f7b414a3 · 2026-09-15T18:19:48Z)

비교 기준 범위: 절 제목에 붙는 머리 숫자(막힌 것 N건·보실 것 N건 같은, 지금 상태가 좋은지 나쁜지 판단하는 자리)에만 건다. 본문 목록 안의 개수(4번·2건·6건/5건처럼 "오늘 뭘 몇 번 했다"를 세는 활동 이력)는 예외 — 그건 판단이 아니라 기록이라 비교할 기준값 자체가 없다. 그래서 시안의 여덟은 안 걸린다: 조사 1 의 "0 아니면 통과 아님"은 그때 대던 두 파일의 머리 숫자·측정 그리드에만 쓴 규칙이었지, 활동 로그 개수까지 넓힌 적 없다 — 범위를 지금 이렇게 못 박는다.

도표 규칙: 이번 시안엔 도표 없음 — 이유: .md 라 그림을 못 넣고, 팀 다섯 줄은 표 하나로도 12살이 30초 안에 읽는다(대표 잣대). 규칙: 팀 단계처럼 N/M 진행이 있으면 그림 대신 텍스트 막대(예 "■■■□□□ 3/6")로 대신한다 — md 파일엔 이미지 대신 이 표기, html·svg 파일엔 실제 막대(tower.svg 78·84·90행 같은 것)를 쓴다.

## 절 목록·순서, 정정
(evt_7a5a57792c · 2026-09-15T18:27:51Z)

tower.svg 만 보고 "넷 아니면 절을 안 만든다"를 세운 건 틀렸다 — 그건 화면 패널 수 규칙이지 보고서 절 수가 아니다. teams/dev/decisions.md 171행(결정 80)·177행(결정 85③)을 직접 열어 다시 짠다.

절 목록(순서 그대로, 결정 80 원문 순서를 따른다):
0. 대표님 대신 정한 것 — 그날 대리 결정이 있을 때만(결정 85③, "맨 위")
1. 어젯밤 사이 있었던 일 — 팀마다 한 줄(결정 80①)
2. 오늘 대표님 차례 — 없으면 "없습니다"(결정 80②)
3. 오늘 각 방이 할 일 — progress.json 의 "다음"(결정 80③)
4. 톰의 한마디(결정 80④, 글 있는 날만)
5. 어제 나온 그림(결정 80⑤)

"넷 아니면 안 만든다"는 버린다 — 결정 80 이 다섯(+조건부 0)을 이미 못박았다. teams/design/out/screens/ui/report.svg(헨리, M4, 2행 주석)가 이 다섯+85③을 화면 카드 넷에 이미 앉혀 놨고 "글의 틀(다섯 절 문장)은 경영팀 몫" 이라고 직접 적어 뒀다 — 이걸 조사에서 놓쳤다, tower 만 보고 하나 있는 줄 알았던 게 잘못이었다.

## 숫자 — 머리와 본문을 가름 · N/M 정의 (헨리 답 ②③ 반영)
(evt_fc37ffd2e0 · 2026-09-15T18:28:33Z — 헨리 확인 req_4204c2f9, 톰 확인으로 닫힘)

② 머리 숫자(넷: 정하실 것 수·멈춘 것 수·단계 N/M·회차)와 본문 숫자(다섯: 개수·비율·시간·용량·회차)를 한 줄로 섞지 않는다 — 잣대.md 에 "머리"·"본문" 두 절로 가른다.
③ N/M 정의 못 박음: N = 통과(pass)한 단계 수, M = 로드맵에 적힌 전체 단계 수. 지금 진행 중인 단계 번호(예: "6단계 진행 중"의 6)를 N 에 넣지 않는다 — 그건 "몇 번째를 하는 중"이지 "몇 개를 끝냈는지"가 아니라 서로 다른 값이다. 예: 개발 9단계 진행 중(pass 8·now 1·wait 1) → N/M = 8/10(9단계 자체는 아직 안 끝났으니 분자에 안 셈).
(정정 evt_dcc5b21cb7 · 18:36Z — 첫 판 "개발 6단계 · 5/9" 는 파일 안 열고 지어낸 예시라 노라가 거둠. teams/dev/roadmap.json 62행 n:9 now, 1~8 pass, 10 wait.)
