# 아침 한 장 — 잣대 두 줄과 쓰는 순서 (경영 4단계, F2)

글 노라. 감사 유진. 2026-09-16(우리 시각), 라운드 5.
재료: `teams/finance/out/daily-template.md`(F1, 유진) 1·2·4절 · `server/public/bosswords.js` · `teams/dev/decisions.md` 177행(결정 85③).
쓰기 금지가 풀린 뒤(나리 08:0x) 처음으로 제 손으로 쓰는 파일. daily-template.md 5절에 유진이 받아 적어 둔 두 줄은 이 파일로 옮긴 뒤 경로 한 줄만 남기기로 했다.

## 잣대 두 줄

- **자 0줄** — `server/public/bosswords.js` 14행 `bossOk`(비어 있지 않고 · 60자 안(6행 `MAX_LEN`) · `isBossWord`(11행, 8행 `JARGON` 정규식)) 하나로 세 칸 전부를 잰다. `node tools/boss-words-check.mjs` 는 이 함수를 그대로 부른다(14행 import) — 도구가 막히면 같은 정규식을 `\b` 없이 `grep -E` 로 대신 댄다. 그건 도구보다 그물이 넓어 **grep 0 이면 도구도 0** 이라는 것이지, 값이 같다는 뜻은 아니다(0 이 아닐 땐 수가 다를 수 있다 — 유진 지적, 우리 시각 07:05).
- **12살이 30초** — 자로 못 재는 값이라 사람이 읽는다. 처음 보는 눈으로 세 칸을 읽고 시계로 재며, 다 읽은 뒤 세 물음(어젯밤 누가 무엇을 냈나 · 무엇이 왜 막혔나 · 대표님이 오늘 할 동작은 무엇인가)에 막힘없이 답할 수 있어야 통과. 지금은 틀뿐이라 잴 실물이 없다 — 이 값은 "재는 법" 이고 "잰 값" 이 아니다. 첫 실물은 09-17 06:30, 그때 제가 직접 시간을 잰다(`teams/finance/out/daily-first-check.md` 기록 칸에 적는다).

## 쓰는 순서 — 세 칸 틀 (M4, daily-template.md 1·2절 그대로)

1. **대표님 대신 정한 것** — 그날 대리 결정이 있을 때만(결정 85③, decisions.md 177행). 없으면 절 자체를 안 낸다.
2. **된 것** — 팀당 한 줄. 재료는 `rounds.jsonl` 의 지난 06:30 이후 PASS(팀당 마지막 하나), 없으면 `progress.json` done 첫 줄. 낼 게 없는 팀은 "{팀} · 어제는 낸 게 없어요".
3. **막힌 것 N** — 다섯 팀 `progress.json` blocked[] 그대로, N=그 줄 수, 없으면 "막힌 것 0" 한 줄.
4. **대표님 손 N** — 대기 중 C 카드의 `--boss` 한 줄 + `progress.json` boss[], N=그 줄 수, 없으면 "대표님 손 0 — 오늘은 없어요".

순서 고정(대리 결정 → 된 것 → 막힌 것 → 대표님 손). 절 이름 넷은 `grep '^## '` 로 재는 잣대라 daily-template.md 1절 글자 그대로 — 여기서 다시 안 옮겨 쓴다, 틀리게 베낄 위험이라 원본만 본다.

이 절은 M3 `매뉴얼.md` 56~93행("## 쓰는 순서·빈 틀", 결정 80 다섯 절)을 갈음하지 않는다 — 그건 안쪽 자료로 남는다. 다만 그게 "09:00 보고서" 라는 살아 있는 물건 몫은 아니다 — `teams/hq/out/reports/` 엔 09-14 README 한 장뿐이고 서버(`server/index.mjs:382`)도 `teams/hq/out/daily/` 만 읽는다. 나리 통과 문구(apr_f0c65cd4)가 결정 80 ③④를 타임라인 탭·세라 카드로 보낸 뒤로, 지금 대표가 보는 건 세 칸 하나뿐이다.
