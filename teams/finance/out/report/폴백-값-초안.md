# 폴백 칸 다섯 — 값 초안 (경영, 나리 15:5x 지시 · 솔라가 그대로 넣는다)

2026-09-16 16:0x · R7 · 유진. 재료: 다섯 `cast.json` outside(`jq`) · `bus/bus.mjs` 1009 `fallbackOf`·996 `GEMINI_MODELS`·1023 `geminiModelOf`(16:0x 다시 연 값 — 처음엔 925·993·1020 이라 적었다: 15:2x 에 grep 한 숫자를 파일이 15:44 에 자란 뒤 다시 안 열고 옮겼다, 노라 지적) · `bus/outside.mjs` 392~396 · `state/infra.json` · 노라 A2(`폴백-부분성공.md`) · 이 방 오늘 503 다섯 번(`log.jsonl` grep).

## 지금 값 (f79af99, 15:37 — 다섯 다 같다)

| 자리 | model | geminiModel | fallback | suspended |
| --- | --- | --- | --- | --- |
| 제리·레오·마크·다니엘·빅터 | gemini | gemini-3.6-flash | gpt | — |

칸은 채워졌다. 남은 건 **값이 맞나** — 둘이 걸린다.

## 걸리는 것 둘

1. **`fallback: gpt` 는 09-20 15:38 까지 한도에 걸린 codex 다**(`state/infra.json`). 지금 폴백이 실제로 돌면 한도 걸린 엔진을 또 두드린다 — 노라 A2 25행 그대로. 그러니 배선은 "폴백이 gpt 면 `outsideCooldown()` 먼저, 쿨다운이면 대표께 올리고 건너뜀"(392~396행이 gpt 자리에 하는 것) 이어야 한다. 값은 gpt 가 맞다 — 09-20 뒤엔 그게 유일한 다른 회사 엔진이다.
2. **오늘 실제로 죽은 건 엔진이 아니라 모델이다** — `gemini-3.6-flash-medium` UNAVAILABLE 503, 이 방에서만 다섯 번(06:33·06:39 등), 빅터 판정이 두 번 밀렸다. 이건 `fallback`(엔진) 이 아니라 `geminiModel` 층의 일인데 그 층엔 대체가 없다(`geminiModelOf` 는 한 값, `GEMINI_MODELS` 는 3.6-flash·3.1-pro 둘뿐 — 점검 3-10 은 agy 에 3.8-flash·3.7-flash·3.1-pro 가 있다고 했다).

## 초안 — 자리마다

| 자리 | fallback(엔진) | geminiModel 대체(모델, 새 칸 `geminiFallback` 제안) | 왜 |
| --- | --- | --- | --- |
| 제리(총괄 대조) | gpt | gemini-3.1-pro | 결재 대조는 하루 40건 넘고 판정 글이 길다 — 503 때 pro 로 한 번 더, 09-20 뒤 codex |
| 레오·마크·다니엘·빅터(팀 외부감사) | gpt | gemini-3.8-flash(점검 3-10 목록에 있으면) 아니면 gemini-3.1-pro | 판정 차례는 pro, 말 차례는 flash 라는 개발 몫 셋(3-10)과 같은 방향 |
| 다섯 다 | — | 둘 다 503 이면 `none` 과 같은 길 — 대표께 올리고 그 판정 차례는 건너뜀(방에 한 줄) | 앵무새 반복 = 문 잠긴 신호(3-10 "남는 규칙") |

값 자체는 **fallback 은 다섯 다 gpt 그대로**, 더할 건 모델 대체 칸 하나와 쿨다운 확인 한 줄 — 둘 다 솔라 배선. 09-20 15:38 뒤 되돌림(`model: gpt`, `suspended` 없음)은 나리 보드 줄(A2 에서 올림) 그대로 사람 손.

## 잰 것

- `jq` 다섯 cast ✓ · `fallbackOf` 1009행 ✓ · `GEMINI_MODELS` 996행 둘 ✓ · `geminiModelOf` 1023 한 값 ✓(줄 번호는 16:0x 다시 연 것 — 본문 2절의 "993행" 도 같이 고침) · 503 다섯 번은 `log.jsonl` grep -c 5(그중 빅터 안내 둘 06:33·06:39) ✓. `agy models` 목록은 이 세션이 못 돌린다 — 3.8-flash 가 있는지는 점검 3-10 의 말이지 내가 본 게 아니다.
