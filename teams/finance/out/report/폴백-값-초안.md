# 폴백 값 초안 — 외부감사 자리 다섯, 어느 자리에 무엇을 대체로 두나 (경영, 나리 15:3x 지시)

2026-09-16 15:5x · R7 · 유진. 재료: 다섯 `cast.json` outside(`jq`), `bus/bus.mjs` 925 `fallbackOf`·993 `GEMINI_MODELS`·1020 `geminiModelOf`, `bus/outside.mjs` 389~410, `state/infra.json`, 노라 A2(`폴백-부분성공.md`), 오늘 이 방 기록의 503 다섯 줄. 솔라가 그대로 넣는 것은 3절 표 한 줄씩.

## 1. 지금 적혀 있는 것 (15:37 f79af99 뒤)

| 방 | 자리 | model | geminiModel | fallback | suspended |
| --- | --- | --- | --- | --- | --- |
| 총괄 | 제리 | gemini | gemini-3.6-flash | gpt | - |
| 개발 | 레오 | gemini | gemini-3.6-flash | gpt | - |
| 디자인 | 마크 | gemini | gemini-3.6-flash | gpt | - |
| 마케팅 | 다니엘 | gemini | gemini-3.6-flash | gpt | - |
| 경영 | 빅터 | gemini | gemini-3.6-flash | gpt | - |

칸은 다섯 다 찼다(결정 116 ② "빈칸으로 두지 않는다" ✓). 값이 맞느냐가 남는다.

## 2. 값이 걸리는 자리 둘 — 파일로 잰 것

1. **`fallback: gpt` 는 09-20 15:38 전엔 못 쓰는 값이다.** `state/infra.json` detail "계정 사용 한도 — 2026-09-20T15:38 까지". 그리고 지금 코드는 gemini 자리에서 폴백을 아예 안 부른다 — `outside.mjs` 392행 `dry || KIND !== 'gpt' ? null : outsideCooldown()`(노라 A2 1절). 그러니 오늘 이 값은 **적혀만 있고 안 도는 값**이고, 솔라가 gemini 실패 분기를 넓히는 순간 한도 걸린 codex 를 두드리는 값이 된다(A2 25행 쿨다운 한 줄이 먼저).
2. **오늘 실제로 죽은 건 gemini 쪽이다.** 이 방 기록에 `UNAVAILABLE (code 503): No capacity available for model gemini-3.6-flash-medium` 다섯 줄(06:33Z·06:39Z 등 — 빅터 판정이 두 번 밀렸다). 그런데 `fallback` 칸은 엔진('gpt'|'gemini'|'none')만 알고 **같은 회사 다른 모델**로 넘어가는 칸이 없다 — `geminiModelOf` 는 `geminiModel` 하나만 읽는다(1020행). 오늘 같은 날 도움이 되는 폴백은 엔진 교체가 아니라 **모델 교체**(3.6-flash → 3.1-pro)인데 그 칸이 없다.

## 3. 초안 — 자리마다 이렇게 (솔라가 넣는 값)

| 자리 | fallback(엔진) | geminiModel | geminiFallbackModel(새 칸, 제안) | 왜 |
| --- | --- | --- | --- | --- |
| 제리(총괄) | gpt | gemini-3.6-flash | gemini-3.1-pro | 결재 대조 자리 — 오늘 503 이 제리에서 카드 줄을 세웠다(15:3x c3d89143). 모델 폴백이 제일 급한 자리 |
| 빅터(경영) | gpt | gemini-3.6-flash | gemini-3.1-pro | 오늘 판정 두 번 밀림(06:33Z·06:39Z 503) |
| 레오(개발) | gpt | gemini-3.6-flash | gemini-3.1-pro | 코드 판정 — pro 가 맞는 자리(점검 3-10 "판정 차례 3.1-pro high") |
| 마크(디자인) | gpt | gemini-3.6-flash | gemini-3.1-pro | 같음 |
| 다니엘(마케팅) | gpt | gemini-3.6-flash | gemini-3.1-pro | 같음 |

- `fallback: gpt` 는 다섯 다 그대로 둔다 — 09-20 15:38 뒤 맞는 값. 그 전엔 **쿨다운 확인이 앞에 서야** 한다(A2 25행): 폴백이 gpt 고 `outsideCooldown()` 이면 부르지 않고 대표께 한 줄, 건너뜀.
- `geminiFallbackModel` 은 새 칸이라 **제안**이다 — `geminiModelOf` 가 503/UNAVAILABLE 한 번 뒤 이 값으로 한 번 더 부르는 것(코드는 솔라). 3.1-pro 는 `GEMINI_MODELS`(993행) 둘째 값이라 이미 허용 목록 안이다. 점검 3-10 의 `agy models` 목록(3.8-flash·3.7-flash·3.1-pro)은 `GEMINI_MODELS` 에 아직 없어 — 넣는 것도 솔라, 넣기 전엔 3.1-pro 하나.
- `suspended` 는 다섯 다 비워 둔다 — 지금 gemini 는 살아 있고(오늘 통과 카드 다수), 죽은 건 한 모델의 순간 용량이다. suspended 를 적으면 라운드가 외부 감사 없이 닫힌다(결정 118 ②) — 그건 폴백이 아니라 포기다.
- 09-20 15:38 되돌림: `model: gpt` 로 다섯을 되돌리는 건 예약이 없다(A2 11행) — 나리 보드 한 줄 그대로. 되돌리면 `fallback` 은 `gemini` 로 뒤집는다(fallbackOf 기본값이 그렇게 계산하니 칸을 비워도 같다 — 비우는 쪽이 낫다, 116 ② "빈칸 금지" 와 부딪히면 'gemini' 명시).

## 4. 남기는 것

- 새 칸 이름 `geminiFallbackModel` 은 제안 — 솔라가 `cast.json` 계약(docs/event-schema.md)에 맞춰 이름을 정한다.
- 이 초안은 값이지 배선이 아니다 — 넣어도 392행이 그대로면 아무것도 안 바뀐다(노라 A2 3절 첫 줄).
