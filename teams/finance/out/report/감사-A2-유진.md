# 감사 — A2 폴백-부분성공.md (노라) · 유진, 2026-09-16 10:3x, 라운드 6

물건: `teams/finance/out/report/폴백-부분성공.md` 26줄. 방식: 결정 138 — 떨어뜨릴 이유 셋을 먼저, 파일을 열어 댄 것만. 판정 낱말은 첫 줄에 안 쓴다.

## 맞은 것 (댄 것만)

다섯 cast.json `fallback` 0줄·outside 다섯 다 `model:"gemini"` ✓ · `bus.mjs` 925~929 `fallbackOf`(none→null, 적힌 값, 아니면 gpt↔gemini) ✓ · 쓰는 곳 `outside.mjs` 394 한 곳, 392행 `dry || KIND !== 'gpt' ? null : outsideCooldown()` 안 ✓(`server/infra.mjs` 58행은 주석뿐) · `state/infra.json` detail 문구 ✓ · decisions.md 362행 ✓ · `bus.mjs` 1389~1391·1520·1530·1548·1556 ✓ · `round.mjs` 963행 suspended 예 ✓ · 결정 137 = 402행 ✓ · dev 로드맵 n=10 now 에 "닫힘 = 자 0줄 + 나리 30초(412·1280) + 헨리 같다 + 레오 떨어뜨릴 이유 셋" ✓.

## 떨어뜨릴 이유 셋

| # | 어디 | 노라 글 | 파일에서 댄 것 | 무게 |
| --- | --- | --- | --- | --- |
| 1 | 19행 | "회차는 레오(외부감사) PASS 하나로 닫히지만" | 하나가 아니다. 판정 흐름은 내부감사 → (PASS 면) 외부감사 순서라(`.claude/commands/verdict.md` "사회자가 내부감사 → (PASS 면) 외부감사") 외부는 내부 PASS 뒤에만 불리고, `endRefusal`(1380~1386)은 마지막 카드가 PASS 인 것 + 사회자 "판정 완료" note 를 또 본다. 회차 닫힘 = **내부 PASS + 외부 PASS + 흐름 note 셋** — 오늘 경영 R5 가 노라 message 로 두 번 멈춘 자리가 그 note 다 | 중간 — 규칙 글의 사실 |
| 2 | 17~19행 | "결정 137 로 … 단계는 나리 검사·헨리 '같다' 가 따로 끝나기 전엔 로드맵에서 `now` 그대로 남는다" | 저절로 안 남는다. `bus.mjs` 1553~1556 은 지금도 `end -v PASS` 면 무조건 `pass` 를 찍는다 — 137 은 글이지 배선이 아니다(25행에서 노라도 "글로만 있다" 고 썼는데 19행은 되는 것처럼 적혔다). 오늘 R5 실측: 단계를 열어 두려면 실무가 **`-v PASS` 를 안 붙이고 닫아야** 했다(`round.mjs end --summary`, 판정 카드 둘 PASS 인데 rounds.jsonl verdict null). "지금 되는 것" 과 "137 이 시키는 것" 을 한 줄로 가르면 3절 셋째 줄의 배선 요청이 정확해진다 | 큰 것 — 솔라가 이 글을 보고 "이미 그렇게 돈다" 고 읽을 수 있다 |
| 3 | 23행 | "KIND 가 뭐든 실패하면 `fallbackOf` 값으로 한 번 더" | gemini 의 폴백 값은 'gpt' = codex 인데 codex 는 09-20 까지 한도(`state/infra.json`·`outsideCooldown`). 이 글대로 넓히면 gemini 가 실패할 때 **한도 걸린 codex 를 또 부르는** 길이 된다. 넓히되 "폴백이 gpt 면 `outsideCooldown()` 먼저 보고, 쿨다운이면 대표께 올리고 1 로 나간다(지금 392~396행이 gpt 자리에 하는 것 그대로)" 한 줄이 있어야 배선 글이 된다 | 중간 — 배선 요청의 구멍 |

작은 것 하나: 9행 "388행 `let KIND`" → **389행**(387 `async function ask`, 388 `const T0`, 389 `let KIND`).

## 노라 고침 (10:4x) — 유진이 파일 다시 열어 댐

넷 다 들어감: 19행 회차 닫힘 셋(카드 PASS·판정 완료 note·외부 PASS) ✓ · 21행 "저절로 되는 게 아니라 실무가 `-v PASS` 안 붙이고 닫아야" ✓ · 25행 폴백 넓힐 때 `outsideCooldown()` 먼저 ✓ · 389행 ✓. 남은 건 글자 하나 — 21행 첫머리 "회차보다 안 저절로 안 남는다" 가 꼬였다("회차와 달리 저절로 안 남는다" 일 것). 뜻은 안 다치니 판정엔 안 건다 — 노라 손이 닿을 때.

## 바라는 고침 (고쳐지기 전 적은 것)

1은 19행 한 문장, 2는 19행 "그대로 남는다" 를 "남겨야 한다 — 지금 코드는 `pass` 를 찍으므로 실무가 `-v PASS` 없이 닫는 수밖에 없다(R5 실측)" 로, 3은 23행에 쿨다운 한 줄. 넷 다 노라 손 — 고치면 여기 자국 남기고 빅터에게.
