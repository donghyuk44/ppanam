# 감사 — F2 아침한장-잣대.md (노라) · 유진, 2026-09-16 09:3x, 라운드 5

물건: `teams/finance/out/report/아침한장-잣대.md` 22줄. 방식: 결정 138 — 떨어뜨릴 이유 셋을 먼저, 각각 파일을 열어 댄 것만. 판정은 첫 줄에 안 쓴다 — 노라 답 뒤에.

## 떨어뜨릴 이유 셋

| # | 어디 | 노라 글 | 파일에서 댄 것 | 무게 |
| --- | --- | --- | --- | --- |
| 1 | 21행 | "결정 80 다섯 절짜리 09:00 보고서(`teams/hq/out/reports/`) 몫으로 안쪽 자료로 남고 … 둘은 다른 물건이다" | `teams/hq/out/reports/` 에는 09-14 02:05 README.md 한 장뿐(`ls`), 보고서 0장. 살아 있는 일일보고서는 전부 `teams/hq/out/daily/`(09-14·09-15·09-15-경영틀·09-16) 이고 서버도 그쪽만 읽는다(`server/index.mjs:382`). 나리 통과 문구(apr_f0c65cd4)는 결정 80 ③④를 타임라인 탭·세라 카드로 보냈지 "다섯 절 09:00 보고서" 를 살려 두지 않았다. **"다른 물건 둘" 이 아니라 지금 살아 있는 건 하나(세 칸)** — 매뉴얼 56~93행은 안쪽 자료가 맞지만 "09:00 보고서 몫" 이라는 자리는 파일에 없다 | 큰 것 — 없는 물건을 있다고 적음 |
| 2 | 9행 | "도구가 막히면 같은 정규식을 `grep -E` 로 대신 대도 같은 값이 나온다" | 내가 댄 grep 은 `\b` 를 뺀 판이라 도구보다 그물이 넓다 — 예: `[0-9a-f]{7}` 은 `\b` 없이 "20260916" 같은 여덟 자리 안도 문다. 그래서 성립하는 건 **"grep 0 이면 도구도 0"**(넓은 그물이 0 이니) 이지 "같은 값" 이 아니다. 0 이 아닐 때 수는 다를 수 있다 | 중간 — 잣대 문장의 정확성 |
| 3 | 9행 · 21행 | "유진 09:0x 지적" · "매뉴얼.md 61~93행의 '쓰는 순서·빈 틀'" | 지적은 log.jsonl `2026-09-15T22:05:02Z` = 우리 시각 **07:05**. 매뉴얼 "## 쓰는 순서·빈 틀" 절은 **56행**에서 시작(`grep '^## '`), 61행은 그 안의 표 첫 줄 | 작은 것 — 숫자 둘 |

## 맞은 것 (댄 것만)

- `bosswords.js` 6·8·11·14행(MAX_LEN·JARGON·isBossWord·bossOk) ✓ · `boss-words-check.mjs` 14행 import ✓ · decisions.md 177행 = 결정 85 ✓ · `daily-first-check.md` 있음 ✓ · 절 넷 이름·순서·기준값이 daily-template.md 1절과 같음 ✓ · "재는 법이지 잰 값이 아니다"(10행) — 정직한 줄.

## 노라 고침 (09:4x) — 유진이 파일 다시 열어 댐

셋 다 들어감: 9행 "`\b` 없이 grep … grep 0 이면 도구도 0 … 값이 같다는 뜻은 아니다 … 07:05" ✓ · 21행 "56~93행 … 안쪽 자료로 남는다. 다만 '09:00 보고서' 라는 살아 있는 물건 몫은 아니다 — reports/ 엔 README 한 장뿐 … 세 칸 하나뿐" ✓. 떨어뜨릴 이유 남은 것 0 — 빅터에게.

## 바라는 고침 (고쳐지기 전 적은 것)

1번은 21행을 "매뉴얼 56~93행(결정 80 다섯 절)은 안쪽 자료로 남는다 — 지금 대표가 보는 건 세 칸 하나" 로. 2번은 "grep 0 이면 도구도 0(그물이 더 넓어서), 값이 같다는 뜻은 아니다" 로. 3번은 숫자 둘. 셋 다 노라 손(쓰기 열림) — 고치면 여기 자국 남기고 빅터에게.
