# 폴드 QA 2판 — 412 · 750 · 1280 세 폭 (노라, 안젤 잣대 셋)

2026-09-16 · R7 · 노라(경영, req_d9bfa9c2 — 나리 지시, 대표 폰은 갤럭시 Z 폴드7: 닫힘 411·펼침 750). 잣대 셋: ① 판정은 못 씀·헷갈림·괜찮음 중 하나 ② 근거는 사진 경로 한 장 ③ 대표 페르소나로 "30초 안에 할 일이 보이나".
1판(`폴드QA-1판.md`)은 테라 고침 전(12:52~14:11) 사진이라 낡았다. 테라가 두 차례 고치고(1506862 14:16 · ea97659 14:18 · d31e5e1 14:21) 새로 찍은 사진 셋(`r33-qa1-tower-750.png`·`r33-qa2-sera-412.png`·`r33-qa3-room-750.png`)으로 다시 잰다.
재료: 대표님이 직접 찍은 사진 셋(`teams/sera/in/2026-09-16T05-11-2[17]…jpg`) · 테라 사진(`teams/dev/out/shots/`). 테라 카드(`apr_d070c79f`)가 처음 적은 경로 둘은 실제와 달랐다 — `r33-c13-room-412.png`·`r33-c13-slash-412.png` 는 파일이 없고 실제는 `r32-c13-room-412.png`·`r32-c13-slash-412.png`.

## 버그 목록 — 1판 판정 vs 지금

| # | 1판 판정 | 지금 | 근거 |
| --- | --- | --- | --- |
| 1 | 못 씀 — 팀 알약 다섯이 다 들어가면 알림 종이 화면 밖으로 밀려 반만 보임(412·750 둘 다) | **고쳐짐(종만)** | `r33-qa1-tower-750.png` — 종 완전한 원, 안 잘림(테라 실측 750 종 오른끝 738/750, 1506862·ea97659). 다만 아래 4번 참고 |
| 2 | 헷갈림 — 닫힌 폭(411)에서 비서실 상단 탭 줄이 오른쪽에서 잘려 세라 설명·차례 표시가 안 보임 | **고쳐짐** | `r33-qa2-sera-412.png` — "대표님과 세라 · 세라가 다섯 팀 상황을 요약해서 알려 드려요" 한 줄로 안 잘리고 다 보임(넘치면 둘째 줄로 감, d31e5e1) |
| 3 | 거슬림 — 펼친 폭(750)에서 말풍선이 화면 끝까지 늘어나 한 줄이 너무 길다 | **고쳐짐(폭만) — 옆판은 그대로 없음** | `r33-qa3-room-750.png` — 말풍선이 560px 로 잡혀 줄 길이는 짧아졌다. 옆에 상황판을 붙이는 건 이번에 안 건드림, 테라가 "디자인 판단" 으로 남겼다(d31e5e1) |
| 4 | (2판에서 새로 봄) | **헷갈림 — 안 고쳐짐** | 팀 알약 줄 맨 끝 "비서실" 이 412·750 둘 다에서 "비서" 까지만 보이고 잘린다(`r33-qa2-sera-412.png`·`r33-qa1-tower-750.png` 둘 다 같음, 유진이 직접 열어 잡음). `server/public/style.css` 1108·1132행 `.rail__list`(팀 알약 줄) 는 `overflow-x: auto` 라 진짜로 잘린 건 아니고 오른쪽으로 밀면 나온다 — 다만 1134행 `scrollbar-width: none` 이라 스크롤 표시가 아예 없어서, 대표님 눈엔 그냥 잘린 것처럼 보인다. 페이드·화살표 같은 "더 있다" 표시가 없는 게 진짜 문제 |

넷 다 파일을 직접 열어 눈으로 대조했다(사진 셋 모두 새로 확인, 4번은 유진 재확인 뒤 CSS 대조로 원인까지 확인).

## 남기는 것

- #3 "옆판"은 버그가 아니라 미정 디자인 결정으로 옮긴다 — 지금 화면(말풍선 폭만 줄어든 채)은 통과 조건에 걸리지 않는다. 필요하면 디자인팀에 새 요청.
- 테라 카드 경로 오탈자(r33→r32)는 개발 쪽에 알려 둠(1판에 적음, 여기서 다시 안 남김).

## 이번에 못 본 것

실제 폴드 기기 터치·힌지 각도 · 설정 탭 스크롤(길이 못 봄) · 닫힘·펼침 전환 애니메이션 중 레이아웃(정지 사진만 있음).

## 닫힘 조건

못 씀 0 · 헷갈림 1(#4, 테라 몫 — 스크롤 표시 하나만 있으면 됨) · 거슬림 0(#3은 디자인 판단으로 옮겨 이번 판 밖). #4 고침 뒤 3판, 그 전엔 빅터 판정 보류.
