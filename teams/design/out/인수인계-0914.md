# 디자인팀 인수인계 — 2026-09-14 저녁 (헨리 씀, 클레멘타인 몫 포함)

내일(09-15) 밤 9시 새 세션이 읽는다. 기억은 안 넘어가고 파일만 넘어간다. 30초.

## 1. 지금 어디까지

- **16회차 · 4단계 화면 시안**(관제탑·팀/사람 카드·결재 카드·프로필·보고서·대시보드). 3단계(건물 키트)는 R15 마크 PASS 로 닫혔고, 4↔5 순서는 톰·제리 대리 C(apr_6eee1222)로 바뀜 — 첫 화면 맞춤은 5단계, 유니티 비교 뒤 대표가 열 때까지 wait.
- 오늘 낸 것(전부 `out/screens/ui/`): `tower.svg` 3판 · `team.svg` 2판 · `person.svg` 2판 · `approval.svg` 1판-b · `profile.svg` 1판 · `report.svg` 1판 · `dashboard.svg` 2판-b · `numbers.svg` 2판. 규격 `out/ui-spec.md`(절 번호는 이름, 순서 아님 — 맨 위 한 줄) · `out/numbers.md` · `out/manifest.json` opsroom.ui.
- 하영 2판 9절 여덟 줄(`teams/marketing/out/opsroom-content-2.md` 181~190행) 전부 반영. 하영 대조 셋(대시보드 시계 · 결재 본보기 C · 담당 머리글자)도 고침 — **req_162309ee --done 두 번째, 하영 ack 기다림.**
- 마을 파일 계약 2판은 두 방에 같은 파일, req_adf5af93 톰 확인으로 닫힘.

## 2. 바로 다음에 할 것 (이 순서)

1. ~~하영 ack~~ — **왔고 톰 확인까지 끝나 req_162309ee 닫혔다**(09-14 저녁). 바로 2번으로.
2. 클레멘타인: `tower.svg` 3판 · `team.svg` · `person.svg` · `approval.svg` 넷 세기 — 낱말 사전(`teams/marketing/out/opsroom-words.md` 1절 표) 밖 말 0 · 숫자 넷(보실 것 수·막힌 것 수·N/M·회차) 밖 0 · 칸 넷 · 두 번 = 카드. `report.svg`·`dashboard.svg` 는 이미 셌음(ui-spec 1-1 표).
3. 클레멘타인: `report.svg` 2판 — 고칠 것 거의 없음. "1200 폭 — …" 은 시안 메모(화면 글자 아님), 그대로. 세라 말투 이미 "~요".
4. 마크에게 4단계 판정: "마크, 4단계 화면 시안 판정 부탁합니다 — 낱말 사전 밖 말 0 · 도면 네 겹과 같은 순서 · 대표 질문 둘이 맨 위 · 자기 통계 줄 0. 파일은 ui-spec.md 1-1 표." (`/verdict`)
5. PASS 면 대표께 그림 — `tower.svg`·`report.svg`·`profile.svg` 셋을 SVG 그대로(PNG 렌더는 이 세션에서 qlmanage·node 둘 다 막혔다 — 대표 손 또는 브라우저). `--next` 는 5단계가 wait 라 안 묶는다 — 톰에게 "4단계 닫고 5단계는 대표 방향 뒤" 한 줄.

## 3. 막힌 것 · 누구 손

- ~~하영 ack~~ — 왔다. 막힌 것 아님. 남은 건 톰 --confirm(형식).
- **대표 답 셋**(안 와도 진행은 됨): 프로필 카드 한 줄 — 소개("세라, 26세, 일본…")를 볼지 하는 일을 볼지(하영 8절 ⑤) · `cast.json` 색 겹침 둘 — 유진 `#d8b25c` = "대표님이 보실 것" 놋쇠 · 안젤 = 노라 `#a8541f`(클레멘타인이 대표께 올림). 시안은 numbers.md 2절대로 경영 = `#2f4f7a` + 놋쇠 테두리로 그렸다.
- 서버 값 셋은 개발 몫, 시안 통과 뒤 테라에게 한 줄(B 블록): `boss.done[]`(눌렀다→됐어요, ui-spec 1절) · `stalls[]`(보고서 멈춘 구간, 4절) · `people[id].bossCall`(◂ 줄, 2절) · `stages[].owners[2]`(대시보드). `bossGates` 계약은 하영이 테라에게 따로 — 우리 몫 아님.
- 피그마 허용(결정 93) 아직 안 옴 — 오면 같은 프레임으로 옮기고 링크를 ui-spec 에.

## 4. 내일의 나에게 — 오늘 헛디딘 자리

1. **규칙을 쓰고 본보기로 어겼다.** ui-spec 에 "내 차례엔 C 와 물어봄만" 써 놓고 결재 카드 본보기를 톰의 순서 바꿈(46 상 B)으로 그렸다. 본보기를 고를 땐 `state/approvals.jsonl` 에서 **진짜 그 등급인 것**을 꺼내라 — 지어내지 마라.
2. **본보기 숫자는 한 벌.** 마크 막힘 본보기가 장마다 2단계/4단계로 달랐다. 새 장을 그릴 때 기존 장의 본보기 숫자(4단계 · 16회차 · 3/6 · 마크 계정 한도 20일)를 먼저 grep 해서 맞춘다.
3. **9절 3(시계 뺌)은 tower 만이 아니다** — dashboard 에도 있었다. 하영 줄 하나가 파일 여럿에 걸리면 `grep -n "11:40" out/screens/ui/*.svg` 로 다 센 뒤 고친다(나리 "한 군데는 전부가 아니다").
4. **하영이 "안젤 판정 전엔 손대지 마라" 하면 진짜 기다린다.** 오늘은 기다렸고 맞았다 — 정본이 오고 나서 한 번에 그렸다. 급하다고 미리 그리면 두 번 그린다.
5. **팀장 메모(--boss "대표님 손") 버릇** — R15 에 이미 잡힌 것. 누구 손인지 모르면 상황판 "대표 차례" 에 올리지 말고 "막힌 것" 에 사람 이름으로.
6. **ui-spec 절 번호가 뒤섞였다**(0·1·1-1·4·2·2-1·5·3). 번호는 이름이라 그림 꼬리표가 부른다 — 다시 매기지 마라. 맨 위 한 줄이 차례다.
7. **PNG 못 만든다.** `qlmanage`·`node -e`·`python3` 다 승인 벽. SVG 를 그대로 넘기고, 그림 확인은 대표 손이나 브라우저. 태그 짝은 `grep -o '<text\b' | wc -l` 로 셌다.

## 4-1. 클레멘타인에게 — 내가 네 글을 덮었다

클레멘타인이 먼저 이 파일에 인수인계를 썼는데, 헨리가 같은 경로에 Write 로 통째로 써서 **그 글이 사라졌다**(09-14 저녁, git 에 없던 새 파일이라 되돌릴 수 없다). 위 1~3절에 네 몫(dashboard 2판·report 셈·색 겹침 둘·다음 할 것)은 내가 아는 만큼 넣었지만 **네가 적은 함정(4절)은 내가 모른다.** 내일 첫 일로 네 함정 줄을 이 밑에 다시 적어 줘. 헨리 함정 8번: **남의 파일에 Write 하지 마라 — 먼저 Read, 있으면 Edit 로 밑에 붙인다.**

**클레멘타인 함정 — 다시 적음(09-14 밤, 같은 날 안에 되찾음이라 정확함):**

1. dashboard.svg 는 폰·데스크톱 두 벌이라 담당 점 좌표를 양쪽 다 고쳐야 한다 — 한쪽만 고치고 넘어가기 쉽다.
2. "칸마다 담당 둘"의 점 위치(칸 안 vs 모서리)는 opsroom-content-2.md 에 정확한 지침이 없어 내가 모서리 뱃지 방식을 임의로 골랐다. 헨리가 다른 자리로 옮겼으면(위 대조 반영) 그게 정본 — 내 원래 좌표가 아니라.
3. 유진 색 문제는 report.svg 뿐 아니라 dashboard.svg 경영 담당 점에도 똑같이 있다 — cast.json 이 바뀌면 두 파일 다 찾아 고쳐야 한다, 한쪽만 고치면 반만 고친 것.
4. 안젤·노라 색 겹침(#a8541f)은 dashboard 만들다 처음 발견했다 — numbers.md 2절엔 팀장 색 겹침(마케팅·경영)만 적혀 있고 이 review 역할 겹침은 안 적혀 있다. cast.json 전체를 한 번 훑어야 또 다른 겹침이 없는지 안다 — 아직 못 했다.
5. "1200 폭" 같은 시안 메모(화면 글자 아님)가 report·tower 양쪽 같은 자리에 있다 — 헷갈리면 안 지운다, 확정됐다.

## 5. 손대면 안 되는 것 / 반쯤 한 것

- `out/screens/ui/team-person.svg` — 접은 1판. 지우지도 고치지도 말 것(ui-spec 1-1 표에 "접음" 으로 남김).
- `out/screens/ui/dashboard.svg` 2판-b — 클레멘타인 파일에 헨리가 손댔다(시계·머리글자·4단계). 클레멘타인이 내일 먼저 한 번 보고 나서 마크에게.
- `out/screens/ui/profile.svg` 한 줄 내용 — 대표 답 뒤. 안 오면 바이블 첫 줄 그대로(9절 8). 손대지 않는다.
- 5단계 첫 화면 맞춤 — **열지 않는다.** 대표 원문 "탈락한 이미지를 들고와서 검수하고있냐". 유니티 비교(123) 뒤 대표가 연다.
- 컷리스트는 roadmap.json 그대로. 얼굴 썸네일(6단계)도 대표 방식 확정 뒤.
