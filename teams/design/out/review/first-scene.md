# 첫 화면 맞춤 — 1차 (대표 "승인 시안과 너무 다르다", 09-14 오전)

헨리. 놓고 본 것: 승인 시안 `out/screens/seoul-toy-concept.png`(결정 62) ↔ 실물 `teams/dev/out/shots/r26-village-day-412.png` · `r25-dolls-lineup-1920.png`. 기준은 `out/direction.md` 3~5절과 8줄 채점표(결정 56 — 전체 인상이 먼저).
**한 줄 진단** — 부품과 자리는 맞는데, 시안을 "장난감" 으로 읽히게 하는 넷(재질·빛·크기감·소품)이 하나도 안 들어갔다. 그 넷은 전부 direction.md 에 숫자로 있고 코드 몇 줄이다. 개발 코드는 안 만진다 — 아래를 city.json / parts.json 값과 렌더 설정으로 돌려준다.

## 8줄 — 지금 실물

| # | 줄 | 시안 | 실물 | ✓/✗ |
| --- | --- | --- | --- | --- |
| 1 | 재질 — 무광 점토, 둥근 모서리 | ✓ | 단색 플라스틱, 각진 모서리, 반사 | ✗ |
| 2 | 빛 — 따뜻한 키 하나, 부드러운 그림자 | ✓ | 고른 흰 빛, 그림자 거의 없음 | ✗ |
| 3 | 카메라 — 좁은 시야, 위아래 흐림(틸트시프트) | ✓ | 직교, 흐림 없음 | ✗ |
| 4 | 크기감 — 둥근 받침 위 미니어처 | ✓ | 바닥이 화면 밖까지, 받침 없음 | ✗ |
| 5 | 컷어웨이 | — (확대 ③단) | 있음(r27) | ✓ |
| 6 | 색 — 베이지·크림·청회색 셋 + 점 | ✓ | 회색 Kenney 원색 벽·파란 유리창(M3 색표 전) | ✗ |
| 7 | 소품 밀도 — 빈 땅 없음 | ✓ | 나무 몇 그루, 골목 틈이 비어 있다 | ✗ |
| 8 | 사람 — 둥근 인형 | ✓ | ✓ (오늘 줄서기 렌더) | ✓ |

2/8. 6개 미만 → 다시. 그런데 ✗ 여섯 중 넷(1·2·3·4)은 **부품이 아니라 렌더 설정**이고, 하나(6)는 M3 색표, 하나(7)는 city.json 소품 줄이다.

## 고칠 것 — 숫자로 (direction.md 3~5절 그대로)

| 무엇 | 지금 | 이렇게 | 어디 |
| --- | --- | --- | --- |
| **재질** | 기본 재질 | `MeshStandardMaterial` `roughness 0.9` `metalness 0` 전부. 유리창만 roughness 0.6 | parts.json `material` 한 줄 |
| **모서리** | 각진 Box | 코드로 짓는 도형은 `RoundedBoxGeometry` 반지름 0.05, Kenney 상자엔 기단(사방 +0.2, 높이 0.15)과 기와 지붕 층(처마 0.5, 반지름 0.08) | buildings.json 초안에 이미 있음 |
| **키 라이트** | 흰색, 위 | `DirectionalLight` 색 `#ffe1bf`, 방향 서·위·남(화면 왼쪽 위), 세기는 흰 벽이 희게 | parts.json `light.day` |
| **환경광** | 고른 흰 | `HemisphereLight` 하늘 `#dfe9f3` · 땅 `#e9dccb`, 그림자 안 밝기가 바깥의 60% 이상 | parts.json `light.day` |
| **그림자** | 없거나 딱딱 | `PCFSoftShadowMap`, 2048, 반지름 크게 — 검은 그림자 없음 | 렌더 설정 |
| **창** | 파란 유리 | `emissive #ffd27a` 세기 0.6, 낮에도 켜 둔다 | parts.json 창 재질 |
| **카메라** | Orthographic 35°/30° | `PerspectiveCamera` fov **25°**, 남동(yaw 45°)에서 북서로, 기울기 멀리 35° · 가까이 45°. 확대 세 단에서 멈춤 | city.json `camera` |
| **흐림** | 없음 | `BokehPass`(또는 위아래 블러 띠) — 선명한 띠 = 화면 높이 40%, 사람은 늘 그 안 | 렌더 설정 |
| **받침** | 없음 | `RoundedBox` 60×34×1, 반지름 0.3, 색 `#d9d1bf`, 마을 56×30 이 그 위. 받침 밖은 스튜디오 바탕 `#e9dccb`, 하늘·안개 없음 | buildings.json `base` |
| **바닥** | 잔디 초록 넓게 | 받침 위 바탕 `#e9dccb`, 광장 `#d9d1bf`, 길 `#b7ab92`, 잔디는 한 색 `#a9c86a` 조각으로만 | city.json 바닥 |
| **색** | Kenney 원색 벽·창 | M3 색표 둘(commercial·suburban): 벽 `#f4efe4` · 지붕 `#7a8797` · 창 `#ffd27a` — 인형과 같은 방법(칸 칠하기) | 디자인 M3 — 다음 |
| **소품** | 나무 27 | 골목 틈마다 전봇대·화분·계단 하나 이상, 광장 벤치 넷·가로등 둘·분수, 찻집 파라솔 다섯 — 빈 땅 없음 | buildings.json 초안 그대로 |

순서 제안 — 위 표에서 **재질·빛·그림자·카메라·받침** 다섯을 먼저(한 번에 코드 값 열 줄 안). 그것만으로 8줄 1·2·3·4 가 ✓ 가 되어 6/8. 그다음 색표(6), 소품(7).

## 인형 줄서기 — 오늘 렌더

열여섯 다 서고 색·소품이 characters.json 대로다. 남은 것 하나 — 세라 리본이 뒤에 있어 정면에서 안 보인다 → 오른쪽 머리 뭉치 옆 (0.12, 0.62, 0.05)로. 인형도 위 재질·빛을 받으면 시안의 "점토" 로 읽힌다 — 지금은 같은 흰 빛이라 플라스틱이다.

## 다음

개발이 다섯을 넣고 다시 찍은 한 장을 여기 2차로 놓고 8줄을 다시 센다. 6/8 이상이면 색표·소품으로 3차. 랜드마크가 "그것" 으로 안 읽히면 그때 생성 서비스(결정 55, 비용은 C).
