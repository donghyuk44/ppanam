# 화면 시안 — 규격 (M5, 헨리)

2026-09-14 오전. 대표 결정 92("지금 UI 탈락, 새로 짠다")·105("손잡이 안 만지고도 읽힌다")·100(도면)·디자이너 둘 분리(09-14 오전). 바탕은 도면 1판(`out/screens/blueprint-1.svg`, 대표 PASS)과 하영 낱말(`teams/marketing/out/opsroom-words.md`·`blueprint-words.md`·`opsroom-content.md`). 그림은 `out/screens/ui/`. 피그마 허용이 오면 같은 프레임으로 옮긴다(결정 93).

절 번호는 이름이지 순서가 아니다(그림 꼬리표가 번호를 부른다): 0 규칙 · 1 현황(관제탑) · 1-1 있는 그림 · 4 보고서 · 2 팀 카드·사람 카드 · 2-1 프로필(미니 카드) · 5 결재 카드 · 6 앞으로 · 7 분석 · 3 안 하는 것.

## 0. 규칙 넷 — 화면 넷이 다 지킨다

1. **맨 위는 대표 질문 둘** — "뭐가 막혔나" 와 "누가 뭘 했나". 그 위에 있을 수 있는 건 "대표님이 보실 것"(누를 것) 하나뿐.
2. **칸은 넷을 넘지 않는다.** 없으면 그 칸이 사라진다(0 은 안 보인다). 서랍장이 아니라 한 장.
3. **숫자는 넷뿐** — 보실 것 수 · 막힌 것 수 · 단계 N/M · 회차(장·단계·회차는 구조 번호라 센다, 결정 99). 발언 수·대화 수·검토 결과 수·게이지 같은 자기 통계는 없다(하영 관제탑 목록 "안 쓴다"). — 클레멘타인 감사(09-14)로 셋→넷 고침: 그림에 회차가 이미 있었다.
4. **말은 낱말 사전대로** — 현황 · 대표님이 보실 것 · 결재 기다림 · 대표님께 물어봄 · 다른 팀에 부탁한 일 · 통과/다시 · 장·단계·회차 · 일하는 중·쉬는 중·자리 비움·멈춤(대표님 답 기다림) · N분 전에 움직임. 탭 이름(현황·앞으로·설정집·팀 방·마을·총괄실)과 직책(0-2 표 ① 열)은 결정 136 으로 확정 — "(후보)" 표시는 뗐다(09-15 밤).
5. **새 말은 사전 먼저** — 사전(`teams/marketing/out/opsroom-words.md`)에 없는 글자를 시안에 넣으려면 먼저 하영에게 한 줄 보내 뜻과 함께 사전에 넣은 뒤 그린다. 본보기 글자도 우리 말(판정·정본·인격·시스템)은 안 쓴다. (하영 5판 req_99245c64, 헨리 받음 09-15)

## 1. 현황 첫 화면 — `ui/tower.svg`

2판(09-14 오후) — 순서를 나리 `teams/hq/out/화면이-답하는-질문.md`(톰·제리 B 통과)에 맞췄다: 막힌 것이 맨 위(스크롤 없이), 상황판 탭은 없어지고 팀 줄의 펼친 줄이 된다.
3판(09-14 오후, 하영 2판 9절 — 안젤 판정 뒤 정본, req_162309ee) — 시계 뺌 · ② "더 보기" 만(어제는 보고서) · ③ 등급 말 뺌, C 와 물어봄만 · ③ 물어봄에 선택지 버튼 · ③ 누른 것은 "됐어요" 한 줄로 남음 · 결재 카드 `ui/approval.svg`(5절) · 사람 카드 ◂ 줄(2절).

| 순서 | 칸 | 한 줄 = | 누르면(펼친 줄) → 한 번 더(카드) |
| --- | --- | --- | --- |
| ① | **뭐가 막혔나 N** | 빨간 점 · 팀 · 사람 · 왜(한 마디) · 무엇이 못 가나 · 멈춘 띠 + N시간째 | 그 자리에서 막힌 까닭 한 줄 더 → 사람 카드 |
| ② | **누가 뭘 했나 · 오늘** | 직책 색 점 · 이름 · 한 것 한 줄 · N시간 전 — 최근 것부터, 여섯 줄. 오늘 것이 여섯을 넘을 때만 "더 보기"(오늘 안에서만 — 어제는 보고서, 9절 2) | 낸 것 이름 → 방의 그 말 |
| ③ | **대표님이 보실 것 N** (나리 문서의 절 이름 "내 차례" 는 화면 글자가 아니다 — 낱말 사전 74행, 클레멘타인 셈 09-15) | 팀 · 사람 · 무엇(결재 기다림 / 대표님께 물어봄) · 한 줄 요약 · 버튼. **등급 말 없음** — 여기 오는 건 대표님이 정할 것(C)과 물어봄뿐, 총괄이 보는 것(B)은 안 뜬다(결정 46·52, 9절 1). 물어봄은 **선택지가 곧 버튼**(둘이면 둘, "살까요 / 기다릴까요") + "다른 말로 → 방으로". 누른 것은 사라지지 않고 **"됐어요 — 무엇, 어디에 전해졌어요 · 시각" 한 줄**로 남는다(7절 ③, N 에는 안 센다) | 읽고 답하기 → 결재 카드(5절) / 선택지 → 그 답이 방에 대표 말로 |
| ④ | **팀** | 팀 이름 · 장·단계·회차 · 막대 N/M · 사람 점 셋(+ 작은 점: 초록 일하는 중 · 빨강 멈춤) — 도면 순서 고정(총괄실·마케팅·개발·디자인·경영) | **상황판 네 칸이 그 자리에서 펼쳐진다**(하는 것 · 막힌 것 · 대표님이 보실 것 · 다음 — progress.mjs 의 CLI 라벨 "대표 차례" 는 실무 터미널 글자라 그대로, 화면만 사전 글자로. 5판 3-5-3 ①) → 팀 카드 |

- 폰 412: 한 열, 카드 사이 6~8, 카드 패딩 12·14. 데스크톱 ≥1200: ①②는 왼쪽 단, ③은 오른쪽 단, ④는 아래 한 줄씩. 같은 칸·같은 글자.
- 글자: 제목 Gowun Batang 17 · 본문 Plex Sans 12.5 · 부제 11 --ink-4 · 시각 Plex Mono 10.5 · 칸 머리 mono 10 자간 .12em.
- 색: 바탕 `--bg` · 카드 `--panel` + `--line` · 보실 것 카드만 `--me-line`(노란 테두리 + 왼쪽 띠) · 막힘 점 `--bad` · 일하는 중 점 `--live` · 사람 점 = 직책 색(`cast.json`, 마을 인형과 같은 색). 시안의 hex 는 임시 — 개발이 토큰으로.
- 시각은 우리 시각(결정 101). "N분 전에 움직임" 은 개인 카드에서, 첫 화면엔 "N시간 전" 만. **머리에 시계 없음**(9절 3) — 폰·컴퓨터에 이미 있다.

### 서버가 줘야 하는 값 (개발)

- `boss.items[]` — `{ team, actor, kind: "approval"|"ask", text, ts, id, choices?: string[] }` — **C 결재 대기와 결정 청한 부름(bossCall.ask)만. B 는 안 든다**(9절 1, 결정 46). 보고는 안 든다(결정 52). `choices` 는 물어봄의 선택지(있으면 버튼, 없으면 "방으로" 만).
- `boss.done[]` — `{ id, what, decision, decidedAt, deliveredTo }` — 오늘 대표가 누른 것(눌렀다 → 됐다 줄, 7절 ③). 자정에 비운다 — 어제는 보고서.
- `blocked[]` — `{ team, actor, why: "boss"|"approval"|"session"|"round"|"other", what, since }` — 사람 단위 `why`(1판 7절 제안 그대로).
- `did[]` — `{ team, actor, text, ts, id }` — 오늘 낸 것·닫은 것·통과한 것을 최근순. 어떤 말이 "한 것" 인가: 상황판 `doing` 이 바뀐 것 · 요청 블록 done · 결재 통과 · 회차 마무리.
- `teams[]` — `{ id, name, chapter, stage, stageTitle, round, stageIndex, stageTotal, people[{ actor, state }] }`.

## 1-1. 지금 있는 그림 (09-14 오후)

| 파일 | 무엇 | 감사 |
| --- | --- | --- |
| `ui/tower.svg` 3판 | 현황 첫 화면 — 막힌 것 → 한 것 → 내 차례 → 팀(펼친 줄 = 상황판 네 칸). 3판 = 하영 9절 1·2·3 + 7절 ①·③ | 클레멘타인 셈 ✓ — 칸 넷·도면 순서(①②③④) 폰·데스크톱 다 지켜짐, 숫자 넷 밖 0. 걸린 것: ③ 칸 글자가 "내 차례"인데 낱말 사전(opsroom-words.md 1절 74행)은 이 자리를 "대표님이 보실 것"이라 한다 — "내 차례"는 나리 문서의 절 이름이지 화면 글자가 아니다. → **헨리 고침(09-15 밤, 3판-b)**: ③ 머리 "대표님이 보실 것 2" 폰·데스크톱 둘 다, 읽는 법 세 줄도. approval.svg 의 같은 말 둘도 같이 · 마크 전 |
| `ui/team.svg` 2판 | 팀 카드 (team-person.svg 1판에서 갈라 냄, 단계 칸은 지금 계획표 6단계) | 클레멘타인 셈 ✓(낱말·숫자 이상 없음) — 걸린 것 둘: ① "이 회차" 절의 "하는 일"이 progress.mjs 실제 라벨 "하는 것"과 다르다(bus/progress.mjs 30행, "글자 그대로" 원칙 위반) ② tower.svg 는 이 카드로 이어지는 펼친 줄을 "상황판 네 칸"이라 부르는데(자기 주석 2행) 정작 이 카드엔 "대표 차례" 줄이 없이 셋(하는 일·막힌 것·다음)뿐 — 네 칸 약속과 실제 셋이 안 맞음. → **헨리 고침(09-15 밤, 2판-b)**: "하는 것" 으로, "대표 차례" 줄 넣어 넷(하는 것 · 막힌 것 · 대표 차례 · 다음 — 현황 ④ 펼친 줄과 같은 글자). 카드 20 길어짐(696) · 마크 전 |
| `ui/person.svg` 2판 | 사람 카드 — 세 줄 + ▸ 물어봄 줄 + **◂ 대표님이 불렀음 → 받았나 줄**(7절 ⑤) | 클레멘타인 셈 ✓ — 낱말·시각 이상 없음. 참고만: 오프스룸 낱말 사전 3-1절은 "멈춤 알약 밑에 왜 한 줄"이라 하는데 이 카드는 왜를 알약 안에 붙였다(예: "멈춤 · 계정 한도") — tower·team 카드도 똑같이 압축해서 그렸으니 통일된 디자인 선택으로 보임, 막는 건 아님 · 마크 전 |
| `ui/approval.svg` 1판-b | 결재 카드 한 장 — 주제 · 왜 · 바뀌는 것(단계 표) · 원문 · 확인/돌려보냄 → 누른 뒤 "됐어요" (7절 ②③, 5절). 본보기는 실제 C apr_dd13a64c(하영, 마케팅 계획표 2~5단계) — 1판의 톰 순서 바꿈은 B 라 하영 대조에서 바꿈 | 하영 대조 ✓(1판-b) · 클레멘타인 셈 ✓ — apr_dd13a64c 를 state/approvals.jsonl 에서 직접 열어 인용문·등급(C)·시각 다 맞음 확인(결정 ts 03:13:52Z = 낮 12:13 우리 시각, 맞게 옮김). 걸린 것 없음 · 마크 전 |
| `ui/profile.svg` 1판 | 미니 카드(게이트) — 방·마을 같은 카드. 하영 4절 전부 "쓴다", 한 줄 내용만 대표 답(8절 ⑤) 뒤 | — |
| `ui/dashboard.svg` 2판-b | 앞으로 — 다섯 팀 예정 한 화면, 앞날 띠. 2판(클레멘타인, 9절 4): 칸마다 담당 점 둘(자리 색) · "대표가 정해야 열리는 것" 목록 뺌(점선 칸과 중복). 2판-b(헨리, 하영 대조 ①③ — 클레멘타인 멈춘 사이): 시계 뺌 · 담당 점에 머리글자(지름 12/11, 글자 7.5/7 — 대표는 색을 안 외운다) · 디자인 줄 4단계로(본보기 숫자 한 벌) · 서버 값 `stages[].owners[2]` | 클레멘타인 ✓(숫자 문구 고침 · 2판 담당 점) — 세다가 안젤·노라 cast 색 겹침(#a8541f) 발견, 유진 건과 같이 대표께 올림 |
| `ui/numbers.svg` 2판 + `out/numbers.md` | 숫자 한 벌 — 큰 숫자 둘(오늘 한 것 뺌, 9절 5) · 작은 그림 다섯은 분석에서만(9절 6) | — |
| `ui/report.svg` 1판 | 보고서 한 장 — 정하실 것 → 멈춘 것(어젯밤 띠) → 팀마다 한 것 → 오늘 (4절). 2판부터 클레멘타인 | 클레멘타인 셈 ✓ — 낱말 사전 밖 말 0 · 숫자 넷 밖 0(장·단계·회차는 구조 번호, 시간 띠 라벨은 numbers.md 6절 요구) · 칸 넷 · 클릭 두 번=카드 지켜짐. 걸린 것: 데스크톱 머리 "1200 폭 — …" 이 진짜 화면 글자인지 메모인지 헨리 확인 대기. 색(유진 놋쇠)은 numbers.md 2절대로 맞게 그림 — 대표께 한 줄 올림 |
| `ui/team-person.svg` | 1판 — 접음. team.svg·person.svg 가 이어받았다 | — |

**09-15 밤 한 판(5판 낱말)** — 여덟 장 전부에 하영 5판 3-5-3 열네 자리(대표 차례→대표님이 보실 것 · 2h→2시간째 등 약자 여섯 · 물어봄→대표님께 물어봄 · 불렀음→부르셨어요 · 세라 비서 확정 · 쉬는 중/자리 비움 밑 줄 · 바깥→밖·확인함→확인 · 대표가→대표님이 · 못 닫음→못 끝남(계정 한도) · 막힘→멈춤·없어요 · 자리 색→직책 색 · 보고서 멈춘 것→막힌 것 · 시스템 손→나리 손 · 오늘 오전→오늘) + 3-5-4(주석 다섯 프레임 밖 · 본보기 우리 말 넷 갈음 · 결정 번호 뺌) + 결정 136(후보 뗌 · 비서실→총괄실) + 나리 대리 결정(유진 #6f9a4a · 프로필 한 줄 = 하는 일). 나리 PNG 로 보고 고친 것 넷(tower ④ N/M 겹침 · 메모 프레임 밖 · dashboard 데스크톱 칸 글자 · report 한 줄). 판: tower 3판-c · team 2판-c · person 2판-b · approval 1판-c · profile 1판-b · report 1판-b · dashboard 2판-c · numbers 2판-b.

남은 것: 분석(대표 질문이 정해지면) · 피그마 프레임(허용 오면) · 경영 보고서 틀 오면 report 절 이름 대조.

## 4. 보고서 한 장 — `ui/report.svg` (1판, 09-14 오후)

나리 정본: 보고서는 **"어제 하루가 어땠나"** — 다른 넷은 기계가 지금 값을 보여 주고, 보고서만 사람이 쓴 글이다. 결정 80 의 다섯 절(① 사이 있었던 일 ② 대표님 차례 ③ 오늘 할 일 ④ 톰의 한마디 ⑤ 사이 나온 그림)과 결정 85 ③(대표님 대신 정한 것은 맨 위)을 0절 규칙(칸 넷·숫자 넷·대표 손이 맨 위)에 앉혔다. **폰에서 30초** — 한 장이 폰 1100 안에 다 든다(위 그림이 그 자다).

| 순서 | 칸 | 한 줄 = | 그림 | 누르면 |
| --- | --- | --- | --- | --- |
| ① | **오늘 정하실 것 N** | 팀 · 사람 · 무엇(결재 기다림 / 대표님께 물어봄) · 한 줄 · 버튼 하나 — 관제탑 ③ 과 같은 목록, C 와 물어봄만(하영 9절 1). 0 이면 칸이 없고 "오늘 정하실 것 없어요" 한 줄. **상황판 "대표님이 보실 것" 줄도 같은 꼴** — 한 팀 한 줄(60자, 결정 140), 버튼은 "방으로". 원문은 안 편다(09-16 diff-0916.md 7-2, 1판에 빠졌던 것) | 놋쇠 테두리 + 왼쪽 띠(`--me-line`) | 읽고 답하기(결재 카드) |
| ①-b | **대신 정한 것** (수를 안 센다) | "톰·제리가 대표님 대신 정했어요 — 되돌리시려면 방에 한마디" 밑에 카드 하나 = 대리 결정 하나: 팀 · 무엇 · 원문 한 마디 · 시각 · 톰·제리 둘 다 확인 | 회색 왼쪽 띠(`--ink-4`) — 놋쇠는 대표가 누를 것에만 | 방으로 |
| ② | **막힌 것 N** — 어젯밤 띠 한 장 (5판 3-5-3 ⑫: 막힌 것 = 일, 멈춤 = 사람 — 관제탑 ① 과 같은 목록. 절 이름은 경영 틀이 오면 그쪽) | 카드 위에 **시간 띠**(numbers.md 1절): 가로 = 저녁 6시 → 아침 9시, 팀마다 한 줄(도면 순서 비서실·마케팅·개발·디자인·경영), 점 = 낸 것 하나(팀 색), 빨간 점 + 빨간 띠 = 멈춘 구간. 눈금 글자는 양 끝 둘뿐. 그 밑에 멈춘 것 하나 = 카드 하나(관제탑 ① 과 같은 칸 + "새벽 3시 → 5시 20분 · 지금은 풀렸어요" / 아직이면 관제탑 ① 그대로) | 띠 290(폰)/370(데스크톱) · 점 지름 8 · 멈춘 띠 높이 5 · 길이 = 멈춘 시간(폰 1분 = 1px, 데스크톱 2분 = 1px, numbers.md 6절) | 점에 손을 올리면 한 줄(사람 · 무엇 · 언제) · 카드 → 사람 카드 |
| ③ | **팀마다 한 것** — 한 줄(결정 80 "팀마다 한 줄") | 팀 이름 · 장·단계·회차 · 진행 막대 N/M · 한 것 한 줄 · 오른쪽 아래 **작은 네모 = 사이 나온 그림**(창 안 mtime 의 그 팀 `out/` 그림, 최대 셋). 없으면 "이 사이엔 한 일이 없어요." | 막대 90(폰)/100 × 5 · 그림 네모 20(폰)/24 | 펼친 줄 = **세 줄**(한 것 · 멈춘 것 · 검토 결과) → 팀 카드. 네모 → 그림 |
| ④ | **오늘 — 각 방이 먼저 할 일** + 한마디 | 사람 점(자리 색) · 이름 · 상황판 "다음" 첫 줄. 맨 밑 **톰의 한마디** 두 줄(회색 왼쪽 띠, 끝에 "— 톰") — 글 있는 날만, 없으면 칸이 할 일에서 끝난다 | — | 방으로 |

- 머리: 제목 `보고서 (어제 하루)` · 오른쪽 날짜(`9월 14일 아침` — 시계가 아니라 보고서 이름, 파일 `<날짜>.md` 와 같다) · 창 한 줄(`어제 저녁 6시 → 오늘 아침 9시`) · 쓴 사람 한 줄(톰이 쓰고, 세라가 대표님 말로 고치고, 나리가 숫자를 맞춰 봤어요 — 결정 80·98).
- 숫자는 넷 안: 정하실 것 수 · 멈춘 것 수 · 단계 N/M · 회차. 대신 정한 것·그림·한 것은 **안 센다** — 카드와 네모가 수다.
- 데스크톱은 **한 단 그대로** — 편지는 두 단으로 안 읽는다. 띠·그림 네모·카드 폭만 커진다(506 → 1200 폭에서 카드 한 단 720 이하).
- 글의 틀(다섯 절의 문장·"12살도 읽는 말")은 **경영팀 몫**(대표 09-14 나리에게). 경영 틀이 오면 이 칸의 글자가 바뀌고 칸·그림은 이대로 — 경영이 그림을 더 원하면 numbers.md 에 먼저 더한다(numbers.md 5절).
- 색: 경영(유진) = **`#6f9a4a`** — 나리 대리 결정 09-15 밤(놋쇠 `#d8b25c` 는 "대표님이 보실 것" 에만, 마케팅 `#2f4f7a` 와도 갈림). 시안 넷(tower ④·report·dashboard·numbers) 다 바꿈. `teams/finance/cast.json` 유진 색은 개발이 바꾼다(테라 — 나리 결정이라 바로).

### 서버가 줘야 하는 값 (개발)

- 있음 — `GET /api/report?since&until`(테라 c5480a2): `done[{team,name,items[{id,kind,by,name,ts,text,ref}]}]` → ③ 한 줄과 ② 의 점 · `next{team:[…]}` → ④ · `images[{team,name,at,url}]` → ③ 작은 네모 · `proxy[{team,id,ts,text,approval}]` → ①-b · `chief`(글, `hq/out/daily/<날짜>.md`) → ④ 한마디와 ③ 한 줄의 글자(경영 틀 뒤) · `since/until` → 머리 창 한 줄. 기본 창 어젯밤(어제 18시 → 오늘 9시, 우리 시각).
- 없음 둘 — ① `boss.items[]`: 관제탑 ③ 과 같은 값(1절 서버 값), 보고서는 같은 걸 읽는다 — 새로 안 만든다. ② `stalls[{team, actor, why, what, from, to}]`: 창 안 멈춘 구간(사람 단위, 관제탑 `blocked[]` 와 같은 `why`, 풀렸으면 `to` 있음) — ② 의 빨간 띠와 카드. 지금 `/api/report` 에 없다 → 테라에게 한 줄(B 요청 블록은 시안이 통과된 뒤).

## 2. 다음 셋 — 같은 규칙으로

- `ui/team.svg` 팀 카드(방 오른쪽 첫 카드·현황 ④ 를 펼친 것) — **2판 그렸다**: 단계 칸(도면 2겹 그대로, 지금 계획표 6단계 · 회차 네모는 지금 단계 오른쪽) + 이 회차 하는 일·막힌 것·다음(상황판 글자 그대로) + 다른 팀에 부탁한 일 + 사람 셋(원 = 얼굴 자리).
- `ui/person.svg` 사람 카드 — **2판 그렸다**: 직책 색 원 · 이름 · 직책(후보) · 상태 알약(멈춤이면 왜) / 지금 하는 일 / N분 전에 움직임. 있을 때만 붙는 줄 둘: **▸ 대표님께 물어봄**(결정 청한 것만, 원문 한 줄 + N분 전) · **◂ 대표님이 불렀음 · N분 전 → 받았어요, 답 쓰는 중 / 자리 비움 / 못 와요 · 계정 한도**(하영 7절 ⑤ — 부르고 나서 오는지 안 오는지). 자리 비움 카드에도 ◂ 줄이 붙어 부르기 전에 안다.
  - 서버 값 하나 더 — `people[id].bossCall: { ts, received: bool, state: "writing"|"away"|"limit"|null }` — 대표가 그 사람 이름을 첫머리에 넣어 부른 마지막 말과, 그 세션이 받았는지(턴이 열렸는지)·지금 상태. 없으면 ◂ 줄 없음.
- `ui/report.svg` 하루 보고서 한 장 — **그렸다(4절)**: 정하실 것 → 멈춘 것(어젯밤 띠) → 팀마다 한 것 한 줄(누르면 세 줄) → 오늘. "내일" 은 "오늘 — 각 방이 먼저 할 일" 로(아침에 읽는 보고서라 오늘이 맞다, 결정 80 ③).

## 2-1. 프로필 카드 = 미니 카드 (결정 130·131 + 대표 09-14 "앞단에 게이트가 필요")

- `ui/profile.svg` 가 **미니 카드**다 — 누르자마자 뜨는 건 이것뿐: 얼굴 · 이름 · 직책 · 한 줄 · 지금 뭐 하는 중(+ 상태 알약). 글자 다섯 줄, 화면 반을 안 넘는다.
- **한 줄 = 하는 일** — 직책 표(`opsroom-words.md` 0-2) 넷째 칸 글자 그대로("대표님이 볼 것을 세어서 놓는다" · "그림의 기준을 정하고 고른다"). 소개(나이·나라)가 아니다 — 하영 8절 ⑤ 를 나리가 대표 위임(12시간)으로 09-15 밤에 정함. 서버 값: `cast` 의 직책 표 넷째 칸(테라가 표를 어디 두든 그 글자).
- 지금 마을 카드처럼 "누구" 절·말투 다섯 줄·일지 문단이 한꺼번에 쏟아지는 건 **미니 카드 뒤**로 — 카드 아래 "사람 카드 보기" 하나가 게이트고, 거기를 눌러야 펼친 카드(사람 카드 `ui/team-person.svg` 오른쪽 + 바이블 열 줄)로 간다.
- 두 자리 같은 카드 — 방(이름·썸네일 누름): 폰 320 정사각 가운데 팝업 / 데스크톱 360×220 썸네일 옆. 마을(인형 누름): 같은 320 카드가 인형 위에 뜬다, 글자·순서 같음. 크기만 자리에 맞춘다.

## 5. 결재 카드 한 장 — `ui/approval.svg` (1판, 09-14 오후)

하영 2판 7절 ② — 대표 원문(결정 20-2) "뭐에 대한 승인인지 주제, 목적, 이유, 이런것도 없고 그냥 승인 반려만 있는것도 고쳐야함" + 7절 ③ "눌렀다 → 됐다"(evt_f054428919 "승인버튼 눌렀는데 아침에, 전송이 안된거야?"). 현황 ③ "읽고 답하기" 를 누르면 뜬다. 여기 오는 건 대표님이 정할 것(C)뿐이라 등급 말이 없다.

| 순서 | 칸 | 무엇 |
| --- | --- | --- |
| 머리 | 누가 · 언제 | 요청한 사람 점(자리 색) · 이름 · 방 · "결재 기다림 · N시간 전". 오른쪽 위 × |
| ① | **주제** | 제목 글자(Gowun Batang 16) — 무엇을 정하는지 한 줄 |
| ② | **왜** | 두 줄 안 — 대표 원문이 있으면 그 말부터 |
| ③ | **바뀌는 것** | 계획표면 **단계 표**(지금 / 바뀐 뒤, 도면 2겹 칸 그대로 — 지금 단계 굵은 테두리·남은 것 점선) · 원격 올리기면 **파일 목록** · 그 밖은 한 줄 |
| ④ | **요청한 사람의 원문** | 줄이지 않는다(잎 층 — numbers.md 0절). 길면 상자 안에서 스크롤, 밑에 "방에서 보기 →" |
| ⑤ | **버튼 둘** | `확인` (채움) · `돌려보냄` (테두리) — 낱말 사전 1절. 돌려보냄을 누르면 한 줄 칸이 열린다("왜" 한 마디, 비우면 못 보냄). 밑에 결정 85 한 줄: "10분 안 누르시면 톰·제리가 대신 정해요 — 돈이 나가거나 바깥으로 가는 일은 빼고" |
| 누른 뒤 | **됐어요 줄** | 카드는 사라지지 않고 버튼 자리에 초록 상자 한 줄 — "됐어요 — 확인, 디자인에 전해졌어요 · 아침 8:41". 왼쪽 띠가 놋쇠 → 초록. 현황 ③ 에도 같은 한 줄이 남는다(1절 ③) |

- 폰 388 한 장, 데스크톱은 같은 카드가 가운데 480. 카드 안에서 누르면 옆으로만(원문 → 그 방).
- 서버 값 — `approval: { id, by, team, ts, what, why, change: { kind: "roadmap"|"push"|"other", before?: stage[], after?: stage[], files?: string[] }, original, decidedAt?, decision?, deliveredTo? }`. `what·why·original` 은 지금 approvals.jsonl 의 `what·detail` 을 세라가 대표 말로(결정 98) — 원문은 `original` 에 그대로. `deliveredTo` 는 통과 뒤 서버가 알린 방 이름.

## 6. 앞으로 한 장 — `ui/dashboard.svg` (3판, 09-16)

하영 글 틀 1판(`teams/marketing/out/screen-text-frames.md` 1·2·5절, R35)에 맞춰 2판-b 를 고쳤다 — H1 잡힌 예정은 채움 옅게(점선은 셀 수 없는 것만) · H2 총괄실 빈칸 "계획표 아직 없어요"(대표 문 아님) · H3 범례 "칸 오른쪽 점 둘 = 그 팀의 둘". 같은 글 틀에서 온 것 셋 더: 늦음 글자는 단계 번호("6단계 40분 늦음" — 무엇이 늦었는지 서버는 모른다) · 경영 빈칸 "다음 단계 아직 없어요" · 점선 칸의 펼친 줄 "계획표에 적힌 조건 — "…"". H5(6단계 timebox)는 계획표 글이라 로드맵 카드 apr_8e62ce66 로 고쳤다.

| 상태 | 모양 | 칸 글자 |
| --- | --- | --- |
| running | 채움, 팀 색 | N단계 · {날}까지 |
| planned | 채움 옅게(opacity .35), 팀 색 | N단계 · {날}까지 — 눈금 밖으로 넘치면 "{날}까지" 만 |
| gated | 점선 | N단계 — 대표님이 정한 뒤 / N단계 — {무엇} 뒤 |
| blocked | 빨강 칸 | N단계 — {날}까지 못 끝남 ({이유}) |
| late(running 위) | 빨간 띠 | N단계 N분 늦음 |

- 빈칸 말 둘 — 계획표 파일이 없으면 **계획표 아직 없어요**, 남은 단계가 없으면 **다음 단계 아직 없어요**. 둘 다 대표 문이 아니다.
- 점 둘 = 그 팀의 둘 — `cast.json` 에서 다른 회사 뺀 둘, 팀장 먼저(하영 1-3). 유진은 `#6f9a4a`.

### 서버가 줘야 하는 값 (개발)

- `teams[].stages[{ n, title, status: running|planned|gated|blocked, plannedFrom, plannedTo, late, blockedWhy, gate, owners[2] }]` · `roundMs` — 계약 `docs/event-schema.md` 329·334~338행 그대로 + `owners`(하영 T6). 계산·어긋남(T1~T9)은 하영 5절 테라 줄 — 여기서 다시 안 적는다.

## 7. 분석 한 장 — `ui/analysis.svg` (1판, 09-16)

질문이 왔다 — 나리 `화면이-답하는-질문.md` 41~44행(톰·제리 B 통과) "왜 자꾸 이렇게 되나 — 세는 화면이 아니라 까닭을 찾는 화면. 숫자 한 개는 안 온다, 다른 숫자와 견줘질 때만". 틀은 하영 글 틀 1판 3절, 값은 4절 실물(지어 넣은 값 0).

| 순서 | 칸 (나리 43행 글자 그대로) | 그림 (첫 층에 하나) | 문장 틀 |
| --- | --- | --- | --- |
| ① | **어디서 자꾸 막히나** | 팀 다섯을 같은 자로 — 옅은 띠 = 올린 결재, 짙은 띠 = 돌려보낸 것. 튀는 팀 하나만 빨강(numbers ⑥) | ㄴ **{무엇} — {팀} a건 중 b · …** 다섯 팀 다, 도면 순서 |
| ② | **무엇이 느려졌나** | 지난(회색) · 이번(위·아래 색) 띠 둘, 같은 자 | ㄱ **{팀} {무엇} — 지난 회차 N → 이번 회차 M** — 나빠짐 빨강 · 좋아짐 초록 · 같음 회색. 멈춘 시간을 뺐다는 한 줄이 밑에 |
| ③ | **같은 일이 몇 번째인가** | 회차 네모(numbers ③)를 같은 자리에 겹침 — 세 번째부터 빨강 | ㄷ **{팀} {무엇} — {자리}·{자리}·{자리}, 같은 자리에서 N번째** |

- {무엇}은 사전 말 넷 — 돌려보낸 결재 · 회차 길이 · 대표님이 누르기까지 · 멈춘 시간. "대표님이 누르기까지" 줄은 대표께 여쭌 것(하영 8절)이라 1판엔 없다.
- 칸 셋, 넷을 안 넘는다. 옛 분석 타일(끝난 회차 N · 통과 N · 평균 다시 N번)은 안 온다 — 숫자 하나짜리.
- 세 겹 — 그림 + 문장 → 누르면 그 자리에서 펼친 줄(어느 카드·어느 회차) → 한 번 더 = 결재 카드(5절) / 회차의 그 말.
- 빈칸 말 — **견줄 회차가 아직 없어요 — 회차 둘이 끝나면 떠요** · **같은 자리를 두 번 간 일이 없어요**(사전 6판 6절).
- 폰 412 한 열(칸 셋 세로), 데스크톱 ① 가로 한 줄 + ②③ 나란히. 글자·색 규격은 0절·1절과 같다.

### 서버가 줘야 하는 값 (개발)

- `analysis.stuck[{ team, what, total, count }]` — 팀마다 올린 결재 수와 돌려보낸 수(`state/approvals.jsonl` request ↔ decision REVISE).
- `analysis.slowed[{ team, what, prev, cur, pausedMs }]` — 지난 회차 · 이번 회차(`rounds.jsonl` − `state/pauses.json`).
- `analysis.repeats[{ team, what, at[], nth, passed }]` — 같은 자리(단계·카드)를 돈 회수. 같은 카드 = `what` 첫머리 같고 "(2차 …)" 붙은 것(하영 3-3).

## 8. 카드 체계 — `ui/cards.svg` (1판, 09-16 아침)

대표 원문(07:3x) "상황을 팀별로 요약해서 보여주는 걸 카드로 … 이 디자인 시스템을 모든 팀 판정 뜨는 거에 대체". 구조는 나리 `teams/hq/out/카드-체계-0916.md` 1~3절, 글은 결정 140(60자 사람 말), 꼴은 하영 `boss-lines.md` 셋. 값은 07:2x 다섯 팀 `progress.json` · 결재 큐 · `rounds.jsonl` 실물을 그 규칙으로 옮긴 것 — 지어 넣은 값 0(멈춤 판정만 기록에 없어 머리만).

| 카드 | 블록 | 폰 412 | PC 1200 |
| --- | --- | --- | --- |
| **팀 상황** | 머리(점 · 팀 이름 Gowun 15 · 알약 **진행 중 / 차단됨 / 승인 대기** · 갱신 시각) → **진행 중**(블록 머리) → 된 것(0~3, 앞에 파일 네모) → 막힌 것(0~2, 빨간 왼쪽 띠 3) → **승인 대기**(0~1, 놋쇠 상자 + 단추 — 결재면 승인·반려, 한마디면 "방에 한마디" 하나) → 꼬리(N단계 끝/전체 · 회차 · 자세히 →). 빈 블록은 안 그린다 | 388 폭, 카드 사이 10. 한 줄 60자 = 두 줄(28자/줄). 최고 236(총괄·디자인) < 반 화면 457 | 580 폭 두 단, 사이 14. 한 줄이 한 줄(44자/줄). 최고 203 |
| **판정** | 왼쪽 띠 + 도장 알약(**승인** 초록 `#3a8f5c` · **반려** 주황 `#c9782a` · **보류** 빨강 `#c94a3a`) · 누가 → 누구 · 주제 · 시각 → 한 줄("승인 — …" / "반려 — …", 감사가 쓴다) → 자세히 ▸(접힘) → 반려엔 **반려 N번째 · 다음은 보류** | 96 (한 줄 두 줄일 때) | 76 |
| **결재 C** | 놋쇠 테두리 1.5 + 왼쪽 띠 · 요청한 사람 · 방 · 승인 대기 · 시각 → 무엇(Gowun 17 — 폰은 16 두 줄, `--boss` 한 줄) → 왜(한 줄) → **승인**(채움) · **반려**(테두리) → 자세히 ▸ 요청 원문 · 파일. B 는 카드가 아니라 "**나리·제리 검토 중 N**" 한 줄(회색) | 234 | 196, 팝업이 아니라 그 자리 카드 |

글자는 전부 하영 `card-words.md` **2판 머리**(09-16 08:xx, 대표 원문 07:4x 표준어 잣대) — 1판-b 에서 바꿨다. 걸리는 것 하나: 알약 "진행 중" 과 블록 머리 "진행 중" 이 한 카드에 두 번 선다(상태와 칸 이름이 같은 말) — 하영 판단, 시안은 2판 그대로.

- 세 자리 같은 부품: 첫 화면 '한 장'(다섯 세로) · 팀 방 맨 위(그 팀 하나, 지금·꼬리만 펴고 나머지 "펼치기 ▾ 된 것 2 · 막힌 것 1") · 방 말풍선 사이(판정·결재, 폭 = 말풍선). 세라 06:30·18:30 도 이 다섯.
- 작업 보드 블록 `ui/workboard.svg`(1판, 09-16 — 나리 1-1절, 대표 08:2x "간트 시스템도 대시보드에서") — 대시보드 팀 카드 다섯 **위에** 한 블록. 폰 128 = 다섯 줄(머리 1: "작업 보드" + 진행 N · 감사 대기 N · 통과 N · 지금 시작 가능 N / 줄기 막대 2: 이름 + 막대 48×8, 칸 = 일 수 비율 / 기다림 2: work.json `after` 를 사람 이름으로). PC 는 줄기마다 한 줄, 칸 하나 = 일 하나(22×10) + 수 + 지금 시작 가능 누구. 상태 색 통과 `#3a8f5c` · 감사 대기 `#d8b25c` · 막힘 `#c94a3a` · 대기 `#e3ddd0` + 진행 먹 `#4a443b`(팀 색은 안 쓴다). 막힘 0 이면 안 쓴다, 빈 줄기(M0)는 안 그린다. 누르면 타임라인 탭. 서버 값(W1 솔라) `/api/work` — `{ streams:[{ name, pass, run, audit, wait, stuck, startable }], waits:[{ who, on, what }], total }`.
- 팀 방 한 장 `ui/room.svg`(1판, 09-16) — 위 ③ 이 방 안에 어떻게 서나: 방 머리(이름 · 듣는 중) → 고정 카드(접힌 꼴 120, 회차 줄·'회차 마무리' 단추는 카드 꼬리로) → 말풍선(이름표 32 **원**(1판-c — 나리 09-16 '칩은 32px 원형', 클레멘타인 `.chip` 50% 2b21a9c; 전엔 r9) 에 `out/portraits/chip/<자리>.png`(394×394 정사각, `chip-crop.md` 값 — 09-16 낮 도트 `out/faces/` 에서 초상으로 넘어감. 대표는 안 골라 faces/boss.png 그대로), 없으면 머리글자) → 판정 카드(같은 폭) → 입력. PC 는 레일 224 + 말풍선 580, 오른쪽 여백은 비워 둔다. 탭 이름은 하영 2판 확정 다섯(대시보드·타임라인·채팅·리포트·설정).
- 숫자는 꼬리 둘뿐(단계 끝/전체 · 회차). 된 것·막힌 것은 줄이 곧 수. 알약 글자·블록 이름은 하영 카드 글 규칙이 오면 그 글자로.
- 서버 값(솔라 `/api/card/<팀>`) — `{ team, name, color, state: working|stuck|boss, at, now:{who,text}, done:[{who,text,file}], stuck:[{text}], boss:{text, kind: approval|ask, approval?}, stage:{n,done,total}, round }`. 판정 사건 — `{ verdict: pass|again|stop, from, to, topic, line, detail, rebuttal?:{n,of:3}, ts }`. 사람 말 검사 통과분만(결정 140 4항).

## 9. 설정 › 세계관 — `ui/world.svg` (1판, 09-16, U1 — 09-16 낮 클레멘타인이 이어받음)

대표 원문(08:3x) "설정 탭에 서브탭으로 각자 전 인원의 프로필과 설정을 확인할 수 있는 세계관 탭을 신설." 카드 하나 = 사람 하나, **열일곱**(대표 포함 — 세었다: 대표 1 + 총괄실 4 + 마케팅 3 + 개발 3 + 디자인 3 + 경영 3 = 17). 순서 대표 → 총괄실(톰·제리·세라·나리) → 마케팅 → 개발 → 디자인 → 경영.

**고침(클레멘타인, 이어받으며 첫 셈).** 1판 머리는 "열여덟(열일곱+대표)" 이었다 — `out/faces/` 의 얼굴 열일곱 장(boss.png 포함, `faces/spec.md` 3절 표 1행이 이미 댄)과 세어 보면 대표를 두 번 센 것. 본문의 순서 나열(대표1·총괄실4·마케팅3·개발3·디자인3·경영3)을 그대로 더하면 17 — 얼굴 파일 수와 맞다. 아래 폰·PC 칸수도 이에 맞춰 고쳤다.

| 칸 | 값 | 출처 |
| --- | --- | --- |
| 얼굴 | `out/portraits/chip/<자리>.png`(파이어 엠블렘풍 실사 초상, 394×394 정사각, `chip-crop.md` 값 — 대표 09:17~09:21 확정) — 폰 40 원 · PC 48 원, 테두리 2 = 팀 색. 대표는 아직 안 골라 `out/faces/boss.png`(도트) 그대로, 골라지면 같은 자리에 `portraits/chip/boss.png` | 09-16 낮 초상 전환(클레멘타인) · 09-16 헨리 고침(card/ 512×430 은 폭이 넓어 원 칩에서 정수리가 잘리고 코가 밀린다 — chip/ 정사각으로. card/ 는 profile.svg 위 띠 자리) |
| 이름 · 직책 · 팀 | "톰 / 실장 · 총괄실" — 직책은 결정 136, 팀은 방 이름 | `cast.json` |
| 한 줄 인격 | 40-persona 열 줄 **2**(바깥눈 다섯은 2가 자리 설명이라 **3**). 대표는 세라 열 줄 2 + "인격 파일 없음(결정 37)" | 바이블 |
| 인격 파일 → | `teams/<팀>/<자리>.md`(세라 `teams/sera/secretary.md` · 나리 `40-persona/hq.system.md` 초안 · 대표 없음) | 파일 |
| 모델 | 클로드 · opus / 클로드 · sonnet(안젤·노라·솔라·클레멘타인) / gemini-3.6-flash(다섯) / 클로드(가정)(나리) / 사람(대표) | `cast.json` model·llm·geminiModel |
| 권한 셋 | **파일 쓰기 · 명령 실행 · 판정** — ○ / —. 파일 쓰기: cast disallow 남은 금지 0, 바깥눈은 파일을 안 쓴다 · 명령 실행: settings allow / agy deny 풀림 · 판정: 감사 자리 + 나리(대리) + 대표(C), 톰은 위임 중 — | `권한-현황-0916.md` 1절 이름 그대로 — 나리 확정(09-16 낮). 표 넷(팀 세션 명령·자리별 도구 금지·외부감사 명령·방 기록 훅)을 사람이 느끼는 축 셋으로 접은 것 — 훅(넷째 행)은 사람 권한이 아니라 기록 배선이라 이 셋에 안 넣는다 |

- 폰 412: 카드 96 세로 열일곱(사이 8) — 한 화면 아홉, 한 번 밀면 나머지 여덟. 얼굴 40 · 이름 15 · 직책 11 · 한 줄 12.5 두 줄 · 칩 다섯(mono 9.5, 높이 16). PC 1200: 레일 224 + 세 단(사이 14) 여섯 줄 자리에 열일곱 — 마지막 줄은 두 칸만 차고 한 칸은 빈다. 얼굴 48.
- 얼굴을 누르면 프로필 카드(2-1절), 인격 파일 → 은 그 파일 그대로(둘째 층). 편집 없음 — 설정 값(모델·권한)을 바꾸는 손잡이는 여기 없다(캐릭터는 파일이다, 컷리스트).
- 서버 값 — `people[{ id, team, name, title, room, line, personaPath, model:{ kind, name }, can:{ write, cmd, verdict } }]` 열일곱. 사람 말 검사는 안 건다(한 줄은 바이블 글).

## 10. 타임라인 탭 — `ui/timeline.svg` (1판, 09-16, TL1)

작업판 전체 — 8절 작업 보드 블록(`workboard.svg`)의 전체 판. 대표 원문(08:0x) 넷(동시에 · 병목 안 겹치게 · 남의 수순을 봄 · 끝나면 넘기며 알림)과 나리 `작업보드-0916.md`(날짜 없음 — 상태와 '뒤에'만). 값은 `state/work.json` 09:0x 실물 36(M0 안 함은 안 그림): 완료 9 · 진행 중 6 · 검토 중 11 · 대기 10 · 이슈 0.

| 보기 | 무엇 | 폰 412 | PC 1200 |
| --- | --- | --- | --- |
| **줄기 × 수순** | 줄 = 줄기 일곱(토큰·권한·화면 글·카드 체계·시안 대 실물·운영·인격), 가로 = **수순**(`after` 깊이: 0 지금 시작 가능 · 하나 뒤 · 둘 뒤 · 셋 뒤 — 날짜 아님), 칩 = 일 하나(번호 · 무엇 짧게 · 누구 · `← 뒤에`), 색 = 상태 | 줄기 블록 여덟, 칩은 글자 폭대로 두 줄 접힘(22 높이) — 수순 열 없음, `←` 로만 | 격자 이름 110 + 열 넷(칩 200×22, 사이 4), 점선으로 열 가름. 줄 높이 = 제일 많은 칸 |
| **자리별** | 사람마다 한 줄, 칩(번호만 70×22)은 상태 순(진행 중 → 검토 중 → 대기 → 완료) + 오른쪽 한 마디 | 선택 줄 둘째(그림엔 안 폄) | 격자 밑 한 카드 |
| **기다림** | `after` 를 사람 말로 — "테라 → 솔라·테라 자신 — C5·C6 검토가 끝나야 C7". 끝난 것은 ✓ | 선택 줄 셋째 + 미리 보기 다섯 줄 | 자리별 밑 한 카드 |
| 레일(PC) | 병목 여섯(서버·app.js·헨리 시안·하영 글·나리 판정·대표)과 그 병목에 걸린 안 끝난 일 수 — 누르면 그 칩만 | — | 왼쪽 레일 |

- 상태 색 다섯: 완료 옅은 초록 `#e6efe8`/`#3a8f5c` · 진행 중 먹 `#1b1a1f`(글자 흰색) · 검토 중 놋쇠 `#fdf3d6`/`#d8b25c` · 대기 흰 바탕 테두리 `#c9c2b4`(글자 `--ink-4`) · 이슈 `#f7e0dc`/`#c94a3a`. numbers.md 넷 + 먹.
- 상태 낱말 **완료 · 진행 중 · 검토 중 · 대기 · 이슈** 는 표준 제품 낱말(대표 08:3x, 하영 L1 후보) — work.json 의 통과·진행·감사 대기·대기·막힘 을 옮긴 것. L1 이 정하면 그 글자.
- 칩을 누르면 그 일의 카드(무엇 · 누가 · 뒤에 · 병목 · 메모 · 시작·끝 시각). 줄기 이름을 누르면 그 줄기만.
- 서버 값 — `work.json` 그대로(W1 솔라) + `depth`(after 깊이, 서버가 센다) + 칩 카드 `{ id, what, who, after[], bottleneck, note, status, startedAt?, endedAt? }`.

## 11. 리포트 탭 — `ui/report2.svg` (1판-b, 09-16, R1, 클레멘타인)

나리 배정(08:5x) "아침·저녁 한 장 — 유진 세 칸 틀을 화면으로, 분석은 둘째 탭." 재료는 `teams/finance/out/daily-template.md` 1절 그대로(된 것·막힌 것·대표님 손 셋 + 대신 정한 것 조건부) — 4절 report.svg 의 시간 띠·팀별 그림 네모·진행 막대는 여기 안 옮긴다(아래 "안 옮긴 것").

**한 화면 = 카드 하나, 블록 넷.** 8절 카드 체계와 같은 부품(`.card__block` + `.card__item` — `personLine` 자리에 팀 이름이 선다)이라 새 CSS 가 거의 없다.

**1판-b — 헨리 지적 셋 고침(`out/review/report2-1판-헨리.md`).** ① 단추는 줄마다 하나 — "대표님 손" 세 줄이 다 한마디 꼴(C 아님)인데 밑에 승인·반려 한 쌍만 떠 있던 것을 뗐다. C 등급 줄엔 승인·반려, 아니면 그 줄 밑에 "방에 한마디 →"(`--seal`, `dash__go`·`card__more` 와 같은 부품) 하나씩. ② "된 것" 예시가 표는 `{사람}·{무엇}` 인데 그림은 `{팀}·…닫음`(우리 말)이던 것을 daily-template 3절의 실제 통과분 본보기로 바꿨다 — 머리도 "톰·제리"→"나리·제리"(위임은 지금 나리). ③ 카드 388×900 에 내용이 554 로 250 비던 것을 490 으로.

| 블록 | 있을 때만/항상 | 한 줄 = | 재료 |
| --- | --- | --- | --- |
| 대신 정한 것 | 지난 창에 대리 결정이 있을 때만 — 없으면 블록 자체가 없다(daily-template 규칙 그대로) | 머리 "나리·제리가 대표님 대신 정했어요 — 되돌리시려면 방에 한마디" + `{정한 사람} · {무엇을 정했어요}` | `card__block` 재사용, 색만 회색(`--ink-4` 왼쪽 띠) — 놋쇠는 대표가 누를 것에만(section 4 ①-b 그대로 옮김) |
| 된 것 | 항상, 팀당 한 줄(다섯) | `{사람} · {무엇}` — 없으면 "{팀} · 어제는 낸 게 없어요" | daily-template 2절 첫 행, 예시는 3절 본보기 그대로(자를 이미 통과한 실제 줄) |
| 막힌 것 N | 항상(0 이면 "막힌 것 0" 한 줄만 — **정본 다툼, 아래 참고**) | `{팀} · {무엇이} {왜 멈췄어요} — {누가} {무엇을 해야 풀려요}` | daily-template 2절 셋째 행 — 카드 체계 8절 "막힌 것" 블록과 같은 빨간 왼쪽 띠 3px 재사용 |
| 대표님 손 N | 항상(0 이면 "대표님 손 0 — 오늘은 없어요") | `{팀} · {대표님이 할 동작 하나}` — **단추는 줄마다 하나**: C 등급이면 승인·반려, 아니면 "방에 한마디 →" | daily-template 2절 넷째 행 — 카드 체계 8절 "승인 대기" 놋쇠 상자 재사용 |

**정본 다툼 — "막힌 것 0" 을 보여주나.** daily-template 은 늘 그린다(0 이 기준값), 0절 2 는 "0 은 안 보인다"(칸 자체가 사라진다). 헨리와 나 둘 다 "보여주는 쪽"(좋은 소식 한 줄은 대표가 읽어야 한다) — 나리가 정할 자리, 지금 시안은 보여주는 쪽으로 그렸다.

- 머리: "리포트 — 아침" 또는 "리포트 — 저녁"(Gowun 17) + 창 한 줄("어제 저녁 6시 → 오늘 아침 9시", Plex Sans 11 `--ink-4`) — daily-template 은 아침만 정했지만 결정 143 세라 06:30·18:30 두 창이라 저녁판도 같은 틀, 창만 다르다.
- 절 이름 넷은 `grep '^## '` 로 재는 잣대라 화면 글자도 그대로: **대표님 대신 정한 것 · 된 것 · 막힌 것 · 대표님 손**(daily-template 1절).
- 폰 412: 카드 한 장 폭 388, 블록 사이 10(8절과 같음). PC 1200: 같은 카드 폭 720 — **한 단 그대로**(section 4 규칙 계승, 편지는 두 단으로 안 읽는다).
- 숫자는 둘뿐(막힌 것 N · 대표님 손 N) — 나머지 칸은 줄이 곧 수. 자는 `bosswords.js` 하나(daily-template 2절) — 60자 넘거나 사람 말이 아니면 그 줄만 "아직 쉬운 말로 안 적음"(card.js `said()` 그대로 재사용).
- 서버 값 — `GET /api/daily?window=morning|evening`: `{ proxy?:[{who,text}], done:[{team,who,text}], blocked:[{team,text}], boss:[{team,text,approval?}] }` — daily-template 2절 표의 재료·규칙을 서버가 미리 걸러 낸 것(자 통과분만). **새 값** — `/api/report`(4절, 이미 있음)와 다른 재료라 테라에게 B.

**안 옮긴 것(section 4 대비 뺀 것).** 시간 띠(어젯밤 몇 시부터 멈췄나 그래프) · 팀별 진행 막대 N/M · 사이 나온 그림 네모 · "오늘 — 각 방이 먼저 할 일"(④, 타임라인 탭 자리별 보기가 이미 함, 09-16 결정 인용 — 8절 표 위 문단) · 톰의 한마디(세라 카드가 맡음, 결정 143). 유진 틀에 없는 것은 화면에도 없다 — 틀이 먼저다.

## 3. 안 하는 것

- ~~분석 탭 — 질문이 오면 그린다~~ → 질문이 와서 그렸다(7절, 09-16).
- 마을 화면 손잡이(결정 105) — 클레멘타인 몫(마을).
