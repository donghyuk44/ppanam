# 초상 크롭 값 — 채팅 칩·U1 얼굴 (클레멘타인, 09-16)

나리 물음: "칩은 머리 위주로 잘라야 하니 자르기 값(위 여백)을 한 줄로 정해 주세요." 재료는 `gpt/requests/portrait-boss.json` 의 앞머리 구도 수치(머리 세로 60%, 정수리 8% 밑, 어깨에서 끊김) — 그 512 원본을 32px 원 칩에 그대로 넣으면(그냥 cover) 어깨까지 다 들어가 머리가 원 안에서 작아진다(도트 인형 때 `head=1` 을 따로 넣어야 했던 것과 같은 문제).

## 크롭 한 줄

**512 원본에서 정수리 위 여백 4%(20px)만 남기고, 그 밑으로 394×394 정사각(가로 중앙, x 59~453 · y 20~414)을 잘라 쓴다.** 어깨 대신 턱 밑~목 위에서 끊기고, 머리가 크롭 안에서 60%→약 78%로 커진다.

| 값 | 원본 512 | 크롭 394 |
| --- | --- | --- |
| 위 여백(정수리 위) | 8%(41px) | 4%(20px) |
| 머리 세로 비율 | 60%(307px) | 78%(307px) |
| 아래 끝 | 어깨(512) | 턱~목 위(414) |
| 좌우 | 그대로 | 중앙 59~453 |

- 칩(32px)·U1 얼굴(40/48px) 둘 다 이 크롭 하나를 쓴다 — 도트 얼굴과 같은 원칙(한 파일이 모든 크기를 먹인다).
- 배경은 프롬프트가 이미 평면 `#f4efe6`(우리 종이 색)라 원으로 잘라도 테두리가 안 튄다 — 크롭 뒤에도 배경 제거는 안 해도 되지만, 헨리가 이미 "배경 제거·정사각 자르기까지" 한다고 했으니 투명 처리는 그 손에.
- 이 값은 앞머리 구도(60%·8%)가 그대로일 때만 맞다 — 대표님이 구도를 바꾸시면 다시 잰다.
