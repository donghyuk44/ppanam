# 디자인 인수인계 — 헨리 (2026-09-16 저녁, 22·23회차 뒤)

새 세션은 `teams/design/shelf.md`(명령·읽을 것) → 이 한 장 → `teams/design/progress.json` 순으로 읽는다. 앞 장 `인수인계-0916.md`(세션 리셋 전)는 이 장이 이어받는다 — 거기 "하는 중인 일" 둘은 아래 "끝낸 일"로 옮겨졌다. 세 묶음뿐이다.

## 끝낸 일

- **34회차 셋 대조** — 테라 사진 셋(비서실 머리 · 레일 팀 줄 · 종 창)을 시안과 대봤다. 비서실 머리·레일 줄은 같다, 종 창은 시안에 없어 실물이 정본(나리). 글자 둘("확인할 항목 없음"·"모두 읽음")은 사전 문제로 테라·하영 몫 → 테라 570e9d9 로 고침. `screens/diff-0916.md` 10절.
- **TL1 2판-b (타임라인 시안)** — 결정 187·188 대로 열 머리 "오늘·내일…"·점선 기둥·"오늘까지/내일까지"·"44분 지연" 뺌, "대표님이 시킨 일" 카드(하영 다섯 줄) 맨 위, 범례 "채움 = 지금 단계 · 점선 = 대표님이 정한 뒤 · 빨강 = 막힌 것"(실물 bf13143·570e9d9 말로 맞춤). 피그마 폰 `58:299` · PC `58:685`, 그림 `fig-0916-timeline-412·1280.png`(1280 은 100KB 넘어 이 PC 에만). 마크 통과(63738a6), **실물 r22-timeline-1280 = 시안 "같다"**(22c4f2d, diff-0916 10절). 22회차는 그 줄이 선 뒤 닫았다.
- **대시보드 3판 (대표 답 ④, 결정 192)** — 대표 단추 셋(17:15) 그대로: ① 큰 숫자 셋(정할 것·막힌 것·오늘 끝난 것) ② 작업 보드·최근 활동은 첫 화면에서 뺌 ③ 띠를 채팅 머리에도. 폰 `77:2` · PC `78:150`, 그림 `fig-0916-dash3-412·1280.png`, 채팅 머리 띠는 `65:2`·`67:2`(2판-b) 에 얹어 `fig-0916-kakao-room-412·1280.png` 다시 뽑음. 규격 `ui-spec.md` 12절, 낱말 사전 191행. 클레멘타인 셈 통과 · 마크 통과(b0de7b5). 테라에게 B 블록 apr_311eadaf — 나리 통과, 개발이 붙이는 중.
- **얼굴 열일곱 꽂기(req_99dbd1fe)** — 테라 07be18b, 이름표·방 사람 칩·사람 카드 머리·종 목록에 17/17. 톰 닫음. 시안과 다른 셋(이름표 r9 → 32 원 · 사람 카드 얼굴 폰 40/PC 48 원 + 팀 색 테두리 2 · 프로필 띠 지름 폰 110/PC 90)은 클레멘타인 CSS — 프로필 띠는 붙었다(`.pcard__chip` 977행·819 블록), 나머지 둘은 클레멘타인이 이어 감.
- 대시보드 제안 한 장(`대시보드-제안-0916.md`) — 세라 검수 통과, 블록 req_417da033 닫힘.

## 하는 중인 일

- **테라 사진 기다림** — 대시보드 3판 실물 412·1280 이 오면 `fig-0916-dash3-*` 와 대보고 diff-0916 에 "같다" 한 줄. 프로필 띠(클레멘타인 CSS 뒤 테라 사진)도 같이. 그 줄이 서면 나리가 디자인을 끈다 — 회차 닫기는 나리에게 묻는다.
- 클레멘타인 CSS 둘(이름표 32 원 · 사람 카드 얼굴 40/48 원 + 테두리) — 디자인 안, 블록 밖.
- 1280 그림 둘(dash3·timeline)이 100KB 를 넘어 ignore 줄엔 들었으나 아직 추적 중 — 관리 창 나리가 `git rm --cached` 목록에 올림. 그림은 이 PC 에 그대로.

## 다음 할 일

- 테라 사진 ↔ 시안 "같다" — 대시보드는 띠 셋(사전 191행 셈, `doneAt` 새 칸)·결재 카드·팀 카드 세 줄·오늘 끝난 것 넷과 채팅 머리 띠. 다르면 diff-0916 10절 밑에 한 줄씩.
- 마크 판정을 청할 땐 `node bus/round.mjs verdict --target guide "…"` — 대상을 직접 적는다.
- 새 화면 글자는 그리기 전에 사전(`opsroom-words.md` 0-3·1절)에 대보고, 없으면 마케팅 방에 한 줄(결정 190).
- 회차는 마크 통과 뒤에도 나리에게 묻고 닫는다 — 닫는 잣대는 "실물 = 시안" 한 줄. 약속으로 닫지 않는다(22회차에서 배운 것).
- 다음 계획표는 나리가 낸다 — 마을·유니티는 컷 그대로. 종 창은 M7 밖이라 안 그린다.
