# 색·글꼴·간격 — 시안 대 실물 CSS (2026-09-16, 클레멘타인)

시안 잣대: `teams/design/out/ui-spec.md` 1절. 실물: `server/public/style.css` (현황 `.tower__head`·`.dash__*`, 앞으로 `.dash__tile`·`.pgrid`). 헨리 diff-0916.md 는 배치·글, 이건 숫자만.

| # | 무엇 | 시안 | 고치기 전 | 고친 값 | 상태 |
| --- | --- | --- | --- | --- | --- |
| T-1 | 제목(Gowun Batang) | 17px | `.tower__head h1` 19px | 17px | **고침** |
| T-2 | 부제(--ink-4) | 11px | `.tower__head p` 12px | 11px | **고침** |
| T-3 | 본문(Plex Sans) | 12.5px | `.dash__row` 12.5px | — | 같다(안 건드림) |
| T-4 | 부제 두번째 자리 | `.dash__sub` 는 ①"막힌 것" 부제도 되고 ②"누가 뭘 했나" 한 것 줄도 된다 — 하나로 못 씀 | 12px, --ink-3 (① 도 ② 도 같이) | 기본 12.5px --ink-2(② 자리, dash__row 짝) · `[data-block="stuck"/"mine"] .dash__sub` 만 11px --ink-4(① 자리) | **다시 고침** — 첫 판(11px --ink-4 전체)은 헨리가 떨어뜨림: 그 값이 ②에도 먹어 어두운 판 명암 3.4:1까지 옅어짐(내 규격이 어느 줄이 부제인지 안 갈라 둔 탓) |
| T-5 | 칸 머리(mono, 자간 .12em) | 10px | `.dash__k` 9.5px, 자간 .13em | 10px, .12em | **고침** |
| T-6 | 시각(Plex Mono) | 10.5px, 따로 선 스타일 | 시각이 `.dash__sub` 본문 글자에 섞여 나온다(app.js) | — | **못 고침** — CSS 값이 아니라 app.js 가 시각을 따로 안 뽑아 쓴다. app.js 는 테라 것이라 여기서 손 안 댐 · 개발에 다시 넘김 |
| T-7 | 카드 패딩 | 12·14 | `.dash__card`·`.dash__tile` 12px 14px | — | 같다(안 건드림) |
| T-8 | 카드 사이 | 폰 412 는 헨리 실측 10, 1280 은 14 — 하나의 "6~8" 이 아니라 폭마다 다른 값 | `.tower__grid` 14px(전체 폭 동일) · `.dash__stats`·`.pgrid` 10px | `.tower__grid` 기본 14px 그대로 두고 `@media (max-width: 819px)` 안에서만 10px. `.dash__stats`·`.pgrid` 는 애초 10px 이 맞았다 — 되돌림 | **다시 고침** — 첫 판(셋 다 7px)은 헨리가 떨어뜨림: 7 은 칸 안 줄 사이 값이지 칸 사이 값이 아니었고, 1280 은 원래 14 가 맞는 값이었다 |
| T-9 | 보실 것 카드 테두리 | --me-line 노란 테두리 **+ 왼쪽 띠** | `[data-block="mine"]` 테두리만 | `box-shadow: inset 3px 0 0 var(--me-line)` 로 왼쪽 띠 붙임(good 카드가 쓰는 것과 같은 방식, style.css:735 참고) | **고침** |
| T-10 | 색 토큰 이름 | --bad(막힘)·--live(일하는 중) | 둘 다 이름 그대로 있음 | — | 같다(안 건드림) |

**셈.** 고침 4건(T-1·2·5·9) · 헨리가 떨어뜨려 다시 고친 것 2건(T-4·T-8) · 그대로 3건(T-3·T-7·T-10, 이미 시안과 같아서) · 못 고침 1건(T-6, 구조 문제 — app.js 손 필요).

바뀐 파일은 `server/public/style.css` 하나뿐 — 값(토큰)만, app.js 는 안 건드렸다. 나리가 412·1280 새로 찍으면 헨리가 "같다" 확인.
