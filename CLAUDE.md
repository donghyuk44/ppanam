# ppanam

부서별 AI 팀이 라운드 단위로 일하고 서로 감사한다. 진행은 웹 단톡방에 실시간으로 흐른다.

절차는 여기 적지 않는다. 파일에 있다.

- 무엇을 만들고 있는지 → `docs/plan.md`
- 채팅방에 나타나는 것의 계약 → `docs/event-schema.md`
- 이번 팀의 목적지와 마일스톤 → `teams/<팀>/roadmap.json`
- 감사 기준 → `.claude/agents/<팀>-review.md`, `<팀>-outside.md` (없으면 공용)

## 캐스트

메인 세션이 **길잡이**다. 대표가 말을 거는 상대이고, 실제 작업을 한다.
감사는 서브에이전트가 맡는다 — **되짚기**(`review`)와 **바깥눈**(`outside`).

바깥눈이 다른 회사 모델인 데는 이유가 있다.
**클로드 둘이 사이좋게 같이 틀릴 때, 그건 다른 엔진에게만 보이기 때문이다.**
바깥 모델이 연결돼 있지 않으면 그 사실을 작전실에 남긴다. 클로드가 바깥눈인 척하지 않는다.

## 명령

- `/kickoff` — 5단계 결정을 인터뷰로 뽑아 로드맵을 만든다
- `/round` — 라운드를 열고 이번 마일스톤을 작업한다
- `/verdict` — 되짚기·바깥눈을 불러 감사한다
- `/team` — 작전실을 바꾼다

## 지킬 것

**스스로 통과시키지 않는다.** 만든 사람이 판정하지 않는다. 감사역을 부른다.

**컷 리스트를 넘지 않는다.** 로드맵의 cut list 에 있는 일은 하지 않는다.
하고 싶으면 먼저 대표에게 묻는다. 이번 마일스톤 바깥의 일도 당겨오지 않는다.

**산출물은 파일로 남긴다.** 채팅에 붙여넣고 끝내지 않는다.
통과 조건은 완료율이 아니라 **제출 가능한 물건**이다. `teams/<팀>/out/` 에 쓴다.

**근거 없는 수치·인용·링크를 쓰지 않는다.** 파일을 열어 확인하고 쓴다.

**FAIL 이 나오면 멈춘다.** 반박 3회를 채우면 자동으로 FAIL 이다.
고치려 들지 말고 무엇이 막혔는지 대표에게 보고한다.

## 대화록에 대해

세 가지가 다르다. 섞지 않는다.

| | 라운드마다 비우나 |
| --- | --- |
| AI 컨텍스트 | **비운다** — 현재 라운드 이벤트만 읽는다 |
| 대화록 | **안 비운다** — `teams/<팀>/log.jsonl` 에 영구히 쌓인다 |
| 산출물 | 별개 — `teams/<팀>/out/` |

발언을 `bus/say.mjs` 로 직접 옮길 필요는 없다. **훅이 알아서 한다.**
기억해야 하는 규칙은 반드시 빠뜨려지기 때문에 훅에 맡겼다.
단, 라운드가 열려 있을 때만 기록된다.

## 하네스는 얇게

CLAUDE.md 는 60줄을 넘기지 않는다. 새 스킬은 같은 일을 두 번 반복한 뒤에 만든다.
훅은 "에이전트가 기억하지 않아도 되게 만드는 것"에만 쓴다.
